# measurement/advisor_wrapper — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_advisor_wrapper INTERFACE)
target_include_directories(comdare_advisor_wrapper INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
