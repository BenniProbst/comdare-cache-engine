# hardware_isa/hugepage_manager — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_hugepage_manager INTERFACE)
target_include_directories(comdare_hugepage_manager INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
