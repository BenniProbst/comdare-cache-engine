# search_engine/value_handle — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_value_handle INTERFACE)
target_include_directories(comdare_value_handle INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
