# search_engine/node — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_node_concept INTERFACE)
target_include_directories(comdare_node_concept INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
