# cache_engine/concurrency_manager/simd_flow_concurrency — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_concurrency_simd_flow INTERFACE)
target_include_directories(comdare_concurrency_simd_flow INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
