# cache_engine/builder/runtime_micro_benchmarks — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_runtime_micro_benchmarks INTERFACE)
target_include_directories(comdare_runtime_micro_benchmarks INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
