# cache_engine/builder/permutation_engine — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_permutation_engine INTERFACE)
target_include_directories(comdare_permutation_engine INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
