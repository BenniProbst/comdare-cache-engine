# cache_engine/builder/latex_renderer — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_latex_renderer INTERFACE)
target_include_directories(comdare_latex_renderer INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
