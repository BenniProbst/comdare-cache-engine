# cache_engine/builder/measurement_matrix — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_measurement_matrix INTERFACE)
target_include_directories(comdare_measurement_matrix INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
