# cache_engine/builder/live_cpu_model — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_live_cpu_model INTERFACE)
target_include_directories(comdare_live_cpu_model INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
