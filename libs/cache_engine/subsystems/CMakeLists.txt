# cache_engine/subsystems — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_cache_engine_subsystems INTERFACE)
target_include_directories(comdare_cache_engine_subsystems INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
