# cache_engine/subsystems/relocation_manager — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_relocation_manager INTERFACE)
target_include_directories(comdare_relocation_manager INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
