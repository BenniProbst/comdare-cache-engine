# ANKER (exakt, je Host-Klasse) DER CTEST-INVENTUR -- EINE ZAHL JE HOST-KLASSE
# (D2-G5, 2026-08-10; UNTERGRENZE -> ANKER seit #39, 2026-08-13)
# =============================================================================
# WAS DIESE ZAHLEN SIND: je Hardware-Klasse die EXAKTE Testzahl, die ein RICHTIG
# gebauter Baum dieses Repos auf DIESER Klasse hat. Faellt 'ctest -N' darunter,
# ist etwas verschwunden; liegt es darueber, fehlt der Floor-Nachzug --
# scripts/ci_test_coverage_guard.sh meldet BEIDES als NENNER-BEFUND (Exit 4).
#
# WELCHE ZEILE GILT, ENTSCHEIDET NICHT DIE MEINUNG, SONDERN DER BAU-BAUM: die
# Wache liest COMDARE_HOST_RUNS_AVX2 und COMDARE_HOST_RUNS_AVX512F aus der
# CMakeCache.txt DESSELBEN Baums, den sie gerade zaehlt. Laesst sich die Klasse
# nicht bestimmen, ist das ABBRUCH (Exit 2) -- nicht die hoechste und nicht die
# niedrigste Annahme.
#
# WARUM SIE IN EINER DATEI STEHEN UND NICHT IM SKRIPT: die Wache bezieht sonst
# JEDE ihrer Zahlen aus dem Baum, den sie gerade prueft. Schrumpft der Baum,
# schrumpfen Zaehler und Nenner gemeinsam und das Verhaeltnis bleibt 'vollstaendig'
# (V-7 FREMDER NENNER). Diese Zahlen sind die einzigen im ganzen Lauf, die der
# gemessene Baum nicht beeinflussen kann: sie stammen aus einem frueher bewusst
# abgenommenen Zustand und aendern sich nur durch einen Menschen -- seit #39 im
# SELBEN Change wie die Registrierung, als eigener sichtbarer Diff-Teil.
#
# DIE WACHE SCHREIBT DIESE DATEI NIEMALS SELBST. Ein Anker, der sich beim
# Reissen automatisch nachfuehrt, ist keiner, sondern ein Protokoll des
# Verfalls. NACHZUG-PFLICHT (#39, 2026-08-13): wer eine ctest-Registrierung
# anlegt, umbaut oder entfernt, misst die drei Sprossen LIVE nach (Rezept
# unten: cmake-Erzwingen der Klassen, 'cmake -U', comm in BEIDE Richtungen)
# und zieht ALLE DREI im SELBEN Change nach, als eigenen, sichtbaren
# Diff-Teil. Eine Inventur UEBER der Zeile ist seit #39 ein NENNER-BEFUND
# (Exit 4), genau wie eine darunter -- der alte Schlupf 'bei Gelegenheit,
# eigener Commit' hat das Wiederholungsmuster der S-3-Abnahme getragen
# (5 Alt-Pakete, 7 Tests ohne Nachzug). NACHZIEHEN HEISST ALLE DREI: die
# Sprossenabstaende (-4 und -2) sind Teil der Aussage, nicht Beiwerk.
#
# -- HISTORIE (Wortlaut bis #39, 2026-08-13; deprecated, Doku bleibt) ---------
# 'Waechst die Testzahl, sagt die Wache das ausdruecklich an und die Zahlen
# hier gehoeren dann von Hand nachgezogen -- als eigener Commit, damit die
# Aenderung im Diff auffaellt und jemand sie absichtlich verantwortet.'
# Der 'eigene Commit' ist als Pflicht entfallen: der Guard prueft den BAUM,
# nicht die Commit-Granularitaet -- er erzwingt Floor-Datei und Registrierung
# im SELBEN Change; sichtbar im Diff bleibt der Nachzug trotzdem.
#
# -- NACHZUG 2026-08-19 (A2.5/G5 B3 WALLCLOCK-ERBE, EINE NEUE REGISTRIERUNG) --
# 507/503/501 loest 506/502/500 ab. Der Zuwachs ist +1 in JEDER Klasse und stammt
# vollstaendig aus dem B3-Posten (Schalter-Hoheits-Umzug zur CEB, KON37-01/KON34-04):
#     test_b3_wallclock_erbe_ct   (add_test -- die EINZIGE TU im Baum mit w-LOSER
#                                  spezifischer CT-Bestellung "[macro]"; beweist den
#                                  Erb-Anschluss des CT-Zweigs von
#                                  resolve_live_measurement_combo_legend)
# GEMESSEN, NICHT GERECHNET (KON58-13): alle drei Sprossen an DIESEM Baum EINMAL LIVE
# erhoben, nicht aus der 506 fortgeschrieben. Zaehlbasis war die Schlusszeile
# "Total Tests: N"; jede Namensliste hat die Gegenprobe Listenlaenge == Total bestanden
# (507/507, 503/503, 501/501). Die MESS-FALLE der rechtsbuendigen ctest-Nummern hat im
# ersten Zug real gebissen (408 von 507 gematcht) und ist mit 'Test *#' geschlossen.
# ZWEI-SEITIGE GEGENPROBE am Objekt, LC_ALL=C comm in BEIDE Richtungen:
#     avx512f -> avx2  verliert exakt die dokumentierten VIER
#                      (test_ap5_simd_extension_coherence, test_buildvariant_dll_real,
#                       test_simd_add_u64_carry_avx512, test_simd_field_sum_dispatch_avx512)
#     avx2    -> basis verliert exakt die ZWEI
#                      (test_simd_add_u64_carry_avx2, test_simd_field_sum_dispatch_avx2)
#     beide Gegenrichtungen LEER -- kein verstecktes -1/+2.
# test_b3_wallclock_erbe_ct steht in ALLEN DREI Namensmengen je genau 1x und in KEINER
# comm-Verlustliste, ist also weder ISA- noch Pruefling-gegatet -> +1 auf allen drei
# Sprossen; die Sprossenabstaende -4 und -2 bleiben unveraendert.
# MESSHOST prod1, GCC, Ninja, 2026-08-19. Bau-Baum build/gcc-release in der CI-FORM MIT
# Pruefling (COMDARE_CE_PRUEFLINGE:STRING=<repo>/tests/pruefling_fixture, LITERAL aus der
# CMakeCache.txt DESSELBEN Baums; ebenso COMDARE_HOST_RUNS_AVX2:INTERNAL=1 und
# COMDARE_HOST_RUNS_AVX512F:INTERNAL=1 -- die Klasse ist gemessen, nicht angenommen).
# Beide Klassen per cmake erzwungen (-DCOMDARE_HOST_RUNS_AVX512F=0 -> 503; zusaetzlich
# -DCOMDARE_HOST_RUNS_AVX2=0 -> 501), danach 'cmake -U' auf BEIDE Variablen:
# Wiederherstellung BYTE-GLEICH (cmp der sortierten Namenslisten ohne Ausgabe, Total
# wieder 507, beide Cache-Schalter wieder INTERNAL=1).
#
# -- NACHZUG 2026-08-17 (SERIELLE WELLEN-LANDUNG A2.1a, VIER STRAENGE, EINE NEUE REGISTRIERUNG) --
# -- NACHZUG 2026-08-18 (HY-A2 REROUTE-PROXY, EINE NEUE REGISTRIERUNG) --------
# 502/498/496 loest 501/497/495 ab. Der Zuwachs ist +1 in JEDER Klasse und stammt
# vollstaendig aus dem HY-A2-Strang:
#     test_hy_f8_reroute   (comdare_add_test -- F8-Minimal-DoD: EIN Standard-Dock,
#                           ZWEI plain-Tier-Ziele, Roundtrip-Koeder, plus der
#                           dlopen-Beleg der vier ABI-Pflicht-Symbole an zwei
#                           Hybrid-.so)
# GEMESSEN, NICHT GERECHNET (KON58-13 / Landungs-Doktrin "nie Deltas addieren"):
# alle drei Sprossen sind an DIESEM Baum EINMAL LIVE erhoben, nicht aus der 501 fort-
# geschrieben. Zaehlbasis war die Schlusszeile "Total Tests: N", jede Namensliste hat
# die Gegenprobe Listenlaenge == Total bestanden (502/502, 498/498, 496/496).
# ZWEI-SEITIGE GEGENPROBE am Objekt, LC_ALL=C comm in BEIDE Richtungen:
#     avx512f -> avx2  verliert exakt die dokumentierten VIER
#                      (test_ap5_simd_extension_coherence, test_buildvariant_dll_real,
#                       test_simd_add_u64_carry_avx512, test_simd_field_sum_dispatch_avx512)
#     avx2    -> basis verliert exakt die ZWEI
#                      (test_simd_add_u64_carry_avx2, test_simd_field_sum_dispatch_avx2)
#     beide Gegenrichtungen LEER -- kein verstecktes -1/+2.
# test_hy_f8_reroute steht in ALLEN DREI Namensmengen je genau 1x und in KEINER
# comm-Verlustliste, ist also weder ISA- noch Pruefling-gegatet -> +1 auf allen drei
# Sprossen; die Sprossenabstaende -4 und -2 bleiben unveraendert.
# ZWEI NEUE SHARED-TARGETS OHNE ctest-WIRKUNG, ausdruecklich benannt, damit niemand sie
# spaeter fuer eine fehlende Registrierung haelt: hybrid_reroute_searchalgorithm und
# hybrid_reroute_set sind .so-Bauziele (add_library SHARED), keine add_test-Eintraege --
# sie werden VON test_hy_f8_reroute geladen und zaehlen deshalb nicht eigenstaendig.
# MESSHOST prod1, GCC, Ninja, 2026-08-18. Bau-Baum build/gcc-release in der CI-FORM MIT
# Pruefling (-DCOMDARE_CE_PRUEFLINGE=<repo>/tests/pruefling_fixture), J-1-Muster (erst die
# fuenf comdare_-Werkzeuge, dann RE-KONFIGURIERT). Schalter LITERAL aus der CMakeCache.txt
# DESSELBEN Baums: COMDARE_HOST_RUNS_AVX2:INTERNAL=1, COMDARE_HOST_RUNS_AVX512F:INTERNAL=1,
# COMDARE_CE_PRUEFLINGE:STRING=<repo>/tests/pruefling_fixture -- die Klasse ist gemessen,
# nicht angenommen. Beide Klassen per cmake erzwungen, danach 'cmake -U' auf BEIDE:
# Wiederherstellung BYTE-GLEICH (cmp ohne Ausgabe, Total wieder 502).
#     avx512f (gemessen, prod1, ctest "Total Tests")                       -> 503
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                  -> 498
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0       -> 496
#
# -- NACHZUG 2026-08-18 frueh (Welle-2-Landung xorf+f3v+a11) ------------------
# 501/497/495 loest 500/496/494 ab. Der Zuwachs ist +1 in JEDER Klasse und stammt
# vollstaendig aus f3v: test_flaeche3_deckung_ceb_aus_tier_an (UNBEDINGT registriert,
# keine AVX-Klausel -- CMakeLists-Gegenprobe grep if/AVX vor der add_test-Zeile leer).
# LIVE-BELEG am Verbund-Baum build-welle2 (Klasse avx512f): ctest "100% tests passed,
# 0 tests failed out of 501"; Wache nach Nachzug GRUEN "ANKER ... 501". Die avx2-/
# basis-Sprossen folgen der +1-Regel unbedingter Tests (wie 500er-Nachzug c3478a9c).
# 500/496/494 loest 499/495/493 ab. Der Zuwachs ist +1 in JEDER Klasse und stammt vollstaendig aus
# dem VL-3-Strang; die VIER des HY-A-Strangs stecken bereits in der 499 des Blocks darunter und
# werden hier NICHT erneut addiert.
#     test_vl3_debug_flag_sperre          (rohes add_test, echte Planer-Binary als Prozess)
# GEMESSEN, NICHT GERECHNET: die Zahl ist am zusammengesetzten Stand (development nach den vier
# --no-ff-Merges b334a657/be724cb5/c8d7295e/6a8cdc65) EINMAL LIVE erhoben, nicht aus Strang-Deltas
# addiert. ZWEI-SEITIGE GEGENPROBE am Objekt: (a) LC_ALL=C comm der gemessenen 500er-Namensliste
# gegen die 499er-Namensliste des HY-A-Strangs ergibt GENAU EINEN Zugang, test_vl3_debug_flag_sperre,
# Gegenrichtung LEER -- kein verstecktes -1/+2; (b) der Name steht in ALLEN DREI Klassen-Namensmengen
# (avx512f/avx2/basis je 1x) und in KEINER comm-Verlustliste, ist also weder ISA- noch
# Pruefling-gegatet -> +1 auf allen drei Sprossen, Sprossenabstaende -4 und -2 unveraendert.
# ZAHLEN-FALLE, im VL-3-Bericht ausdruecklich benannt und hier beachtet: die im VL-3-Strang
# gemessenen 495 sind die PRUEFLING-LOSE Form und NICHT der gleichnamige Anker 'avx512f 495' der
# Pruefling-Form. Wer beide gegeneinander haelt, schliesst faelschlich "der Anker stimmt schon".
# MESSHOST prod1, GCC 15.3.0, Ninja, 2026-08-17. Bau-Baum build-covguard KALT angelegt (rm -rf) und
# in der CI-FORM MIT Pruefling konfiguriert (CC=gcc-15 CXX=g++-15 configure.sh --with-generator=Ninja
# --enable-build-tests --build-dir=build-covguard
# -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"), J-1-Muster (erst die fuenf Werkzeuge
# comdare_anatomy_codegen_cli, comdare_adhoc_emitter_cli, comdare_axis_registry_gen,
# comdare_system_axis_registry_gen, comdare_measurement_axis_registry_gen, dann RE-KONFIGURIERT).
# Registrierungs-Protokoll dieses Baums: 6 von 6 bedingten Bloecken AKTIV. Schalter LITERAL aus der
# CMakeCache.txt DESSELBEN Baums: COMDARE_HOST_RUNS_AVX2:INTERNAL=1, COMDARE_HOST_RUNS_AVX512F:
# INTERNAL=1, COMDARE_CE_PRUEFLINGE:STRING=<repo>/tests/pruefling_fixture -- die Klasse ist gemessen,
# nicht angenommen. Beide Klassen per cmake erzwungen, danach 'cmake -U' auf BEIDE Variablen:
# Wiederherstellung BYTE-GLEICH (cmp ohne Ausgabe, Total wieder 500). MESS-FALLE beachtet: Zaehlbasis
# war die Schlusszeile "Total Tests: N", jede Namensliste hat die Gegenprobe Listenlaenge == Total
# bestanden (500/500, 496/496, 494/494). LC_ALL=C comm in BEIDE Richtungen: avx512f->avx2 verliert
# exakt die dokumentierten VIER, avx2->basis die ZWEI, beide Gegenrichtungen LEER.
#     avx512f (gemessen, prod1, ctest "Total Tests")                       -> 500
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                  -> 496
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0       -> 494
#
# -- NACHZUG 2026-08-17 (HY-A: Dock-Schicht + K2-Extraktion, VIER NEUE REGISTRIERUNGEN;
#    die WERTE 499/495/493 sind durch den A2.1a-NACHZUG oben abgeloest -- Verfahren und
#    Konto dieses Abschnitts bleiben unveraendert gueltig) --
# 499/495/493 loest 495/491/489 ab. Der Zuwachs ist +4 in JEDER Klasse und stammt vollstaendig
# aus diesem Strang -- keine Klassen-Umverteilung, keine Delta-Addition auf einen Alt-Anker:
#     test_hy_a1_dock_contract            (comdare_add_test)
#     test_hy_a3_hybrid_config_parser     (comdare_add_test)
#     test_k2_stufen_neutraler_loader     (comdare_add_test)
#     test_hy_a1_contract_token_negativ   (add_test -- VIERTE Negativ-Compile-Fixture nach dem
#                                          C1-Muster, PASS_REGULAR_EXPRESSION statt Ausfuehrung)
# ZWEI-SEITIGE GEGENPROBE am Objekt: (a) alle vier Namen stehen in der 499er- UND in der
# 493er-Namensliste, sind also klassen-unabhaengig -- daher +4 auf allen drei Sprossen;
# (b) der Branch-Diff gegen origin/development enthaelt GENAU 4 hinzugefuegte Registrierungs-
# Aufrufe. Zahl und Namen decken sich; die 4 ist gezaehlt, nicht aus 499-495 erschlossen.
# GEMESSEN am 2026-08-17, Messhost prod1, GCC 15.3.0, Ninja, Branch bau/hy-a-hybrid-struktur
# (Basis 04ac26fa). Bau-Baum build-covguard KALT angelegt (rm -rf) und in der CI-FORM MIT
# Pruefling gebaut (CC=gcc-15 CXX=g++-15 configure.sh --with-generator=Ninja
# --enable-build-tests --build-dir=build-covguard
# -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"), J-1-Muster (erst
# comdare_anatomy_codegen_cli + comdare_adhoc_emitter_cli, dann RE-KONFIGURIERT), 'make
# inventar', beide Klassen per cmake erzwungen, danach 'cmake -U' auf BEIDE Variablen:
# Wiederherstellung BYTE-GLEICH (cmp ohne Ausgabe, Total wieder 499). Die MESS-FALLE des
# Abschnitts darunter (rechtsbuendige ctest-Nummern) ist beachtet: Zaehlbasis war die
# Schlusszeile "Total Tests: N", jede Namensliste hat die Gegenprobe Listenlaenge == Total
# bestanden (499/499, 495/495, 493/493). LC_ALL=C comm in BEIDE Richtungen: avx512f->avx2
# verliert exakt die dokumentierten VIER (test_ap5_simd_extension_coherence,
# test_buildvariant_dll_real, test_simd_add_u64_carry_avx512, test_simd_field_sum_dispatch_
# avx512), avx2->basis die ZWEI (test_simd_add_u64_carry_avx2, test_simd_field_sum_dispatch_
# avx2), beide Gegenrichtungen LEER.
#     avx512f (gemessen, prod1, ctest "Total Tests")                       -> 499
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                  -> 495
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0       -> 493
# NEBENBEFUND F-12 (in DIESER Runde behoben, tests/unit/CMakeLists.txt): wer dieses Rezept
# buchstabengetreu faehrt, baut die Werkzeuge VOR 'make inventar' und rekonfiguriert -- damit
# sind die Bloecke comdare_perms_pa / comdare_perms_full_join schon aktiv, wenn 'all' laeuft,
# und der Bau fiel an einem prt-art-Header, den die Fixture bewusst nicht traegt. CI sah das
# nie, weil 'make inventar' seine erste 'all'-Runde VOR dem Reconfigure faehrt (Bloecke noch
# SKIPPED) und danach nur noch 'comdare_tests' baut. Die Bloecke pruefen den Header jetzt und
# ueberspringen sich mit genanntem Grund. Auf die Zahlen wirkt das NICHT: sie registrieren
# keinen ctest-Test (D2-AUSNAHME, ebd. dokumentiert), und die Nachmessung nach dem Fix ergab
# dieselbe Namensmenge byte-gleich.
#
# -- WIE DIE ZAHLEN ENTSTANDEN SIND (NACHMESSUNG 2026-08-15, GOLDEN-FENSTER: B2-MERGE IN #16) --
# GEMESSEN am 2026-08-15 in diesem Repo, Stand = golden-Fenster-Arbeitsbaum (Branch
# bau/golden-homes, Worktree wt-ce-golden-homes, WAEHREND der Aufloesung des ABSICHTLICHEN
# Floor-Textkonflikts des B2-Merges e966ee81 -- Werte im SELBEN Change wie der Merge-Commit),
# Messhost prod1, GCC 15.3.0, Ninja. Bau-Baum build-covguard KALT angelegt (rm -rf, existierte
# danach nicht) und UNVERAENDERT nach dem Rezept gebaut einschliesslich Pruefling-Schalter
# (CC=gcc-15 CXX=g++-15 configure.sh --with-generator=Ninja --enable-build-tests
# --build-dir=build-covguard -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture");
# J-1-Muster: erst die beiden Registrierungs-Werkzeuge (comdare_anatomy_codegen_cli,
# comdare_adhoc_emitter_cli), dann RE-KONFIGURIERT.
# -- KORREKTUR 2026-08-17 (CI-LIVE-BEFUND PIPELINE 15936 + NACHMESSUNG AM GELANDETEN STAND;
#    die WERTE 495/491/489 sind durch den NACHZUG 2026-08-17/HY-A oben abgeloest -- Verfahren,
#    Mess-Falle und Pruefling-Befund dieses Abschnitts bleiben unveraendert gueltig) --
# 495/491/489 ersetzt die 16.08.-Fassung 494/490/488. Der tragende Satz der 16.08.-Nachmessung
# ("Der CI-Floor-Baum baut OHNE Pruefling") ist FALSCH: der coverage-guard-Job konfiguriert
# build-covguard ausdruecklich MIT Pruefling (.gitlab-ci.yml: _pf="$CI_PROJECT_DIR/tests/
# pruefling_fixture", -DCOMDARE_CE_PRUEFLINGE="$_pf"; T-4-Doku ebd.: test:unit faehrt die
# Gegenrichtung OHNE), wie es auch BEIDE 15.08.-Messrezepte dieser Datei ausdruecklich tun
# ("einschliesslich Pruefling-Schalter"). Die 494 vom 16.08. war die richtige Zahl der FALSCHEN
# Bau-Form (nach 'cmake -U COMDARE_CE_PRUEFLINGE' fuer die test:unit-Diagnose erhoben).
# LIVE-BELEGE: CI-Job 380164 (test:coverage-guard, prod2, Klasse avx2, Pruefling-Block
# pruefling_slots_v1 GELAUFEN) zaehlt Inventur 491 gegen Anker 490 -> ROT "FLOOR-NACHZUG FEHLT".
# CI-Job 380159 (test:unit, prod1, avx512f, OHNE Pruefling): 494 = 495 - 1 (SOLL-DELTA
# pruefling_slots_v1, dritte Achse der Wache). NACHMESSUNG 2026-08-17 am Stand 90ee6809 nach dem
# Rezept des Datei-Kopfs (build-covguard KALT, CI-Form MIT Pruefling, make inventar,
# cmake-Erzwingen beider Klassen, 'cmake -U' auf beide Variablen, Wiederherstellung byte-gleich,
# LC_ALL=C comm in BEIDE Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER,
# avx2->basis die ZWEI, Gegenrichtungen leer):
#     avx512f (gemessen, prod1, ctest "Total Tests")                       -> 495
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                  -> 491
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0       -> 489
# MESS-FALLE (dokumentiert, damit sie nicht wiederkehrt): ctest -N richtet die Test-Nummern
# rechtsbuendig aus -- bei Nummern < 100 stehen MEHRERE Spaces zwischen 'Test' und '#'
# ('Test   #1:'). Ein Namens-Extrakt mit dem Muster '^ *Test #[0-9]*: ' verliert damit still
# die Tests #1..#99 (hier: 396 statt 495). Zaehlbasis ist die ctest-Schlusszeile
# "Total Tests: N"; jede gefilterte Namensliste braucht die Gegenprobe Listenlaenge == Total.
# Die LANDE-AUFLAGE unten ("nach Doppel-Landung sind 495/491/489 zu ERWARTEN") ist damit durch
# Messung EINGELOEST; uebernommen ist die MESSUNG, keine Prognose und keine Delta-Addition.
#
# -- HISTORIE (NACHMESSUNG 2026-08-16, GOLDEN-FENSTER-LANDUNG, J-1-Rezept live gefahren;
#    FEHLKALIBRIERT, siehe KORREKTUR 2026-08-17 direkt darueber) --
# 494/490/488 ersetzt die vorherige Fassung 495/491/489: die 495 war im PRUEFLING-
# konfigurierten Entwickler-Worktree erhoben (COMDARE_CE_PRUEFLINGE=fixture registriert
# test_pruefling_fixture_ladung zusaetzlich; comm-Differenz = exakt dieser eine Name).
# Der CI-Floor-Baum baut OHNE Pruefling -- zur Testzahl gehoert der BAU-ZUSTAND (V6.5).
#     avx512f (gemessen, prod1)                                          -> 494
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 490
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 488
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 494 Tests,
# Namensmenge byte-gleich zur Erstmessung (cmp leer). Namensdiffs mit LC_ALL=C comm in
# BEIDE Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER, avx2->basis
# die ZWEI, Gegenrichtungen leer -- die Sprossenabstaende -4 und -2 stehen.
#
# WOHER +2 JE SPROSSE KOMMEN (Konto, je Name; gegen den 493/489/487-Stand des #16-Strangs):
# GENAU ZWEI neue Registrierungen aus dem B2-Strang (bau/b2-gate-trennung, KON74-01/KON75-01):
# test_b2_mess_gate_trennung (Bau 11972b3d) + test_b2_gate_zustand_g2an_g3aus (Abschluss-Fix
# e966ee81). Beide weder ISA- noch Pruefling-gegatet (stehen in ALLEN drei Klassen-Namensmengen,
# je 1x, in KEINER comm-Verlustliste). Die B2-seitige Prognose im HARMONISIERUNGS-Block unten
# ('nach Doppel-Landung sind 495/491/489 zu ERWARTEN') ist damit durch Messung EINGELOEST --
# uebernommen wurde die MESSUNG, nicht die Prognose und keine Delta-Addition.
#
# -- HISTORIE (NACHMESSUNG 2026-08-15, S-18/#16 HOMES + RIDER #95) --
# GEMESSEN am 2026-08-15 in diesem Repo, Stand = #16-Arbeitsbaum (Branch bau/golden-homes,
# Worktree wt-ce-golden-homes, Basis 0817c7bf), Messhost prod1, GCC 15.3.0, Ninja. Bau-Baum
# build-covguard KALT angelegt (rm -rf, existierte danach nicht) und UNVERAENDERT nach dem
# Rezept des 2026-08-14-Abschnitts gebaut einschliesslich Pruefling-Schalter
# (CC=gcc-15 CXX=g++-15 configure.sh --with-generator=Ninja --enable-build-tests
# --build-dir=build-covguard -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture");
# J-1-Muster: erst die beiden Registrierungs-Werkzeuge (comdare_anatomy_codegen_cli,
# comdare_adhoc_emitter_cli), dann RE-KONFIGURIERT. Registrierungs-Protokoll dieses Baums:
# alle bedingten Bloecke AKTIV (inkl. des neuen s18_fixture_schema_subset_check_ce).
#     avx512f (gemessen, prod1)                                          -> 493
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 489
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 487
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 493 Tests,
# Namensmenge Byte-gleich zur Erstmessung (cmp leer). Namensdiffs mit LC_ALL=C comm in
# BEIDE Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER, avx2->basis
# die ZWEI, Gegenrichtungen leer -- die Sprossenabstaende -4 und -2 stehen.
#
# WOHER +1 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff des #16-Strangs in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen): GENAU EINE neue
# Registrierung, fixture_schema_subset_check_ce (Rider #95 im #16-Auftrag, rohes
# add_test/cmake -P Script-Test, Vermerk-Block s18_fixture_schema_subset_check_ce).
# Der Test ist weder ISA- noch Pruefling-gegatet (Registrierung auf oberster Ebene;
# steht in ALLEN drei Klassen-Namensmengen, je 1x, und in KEINER comm-Verlustliste).
# Nachzug ALLER DREI Zeilen im SELBEN Change wie die Registrierung (#39-Pflicht;
# die Registrierung kam im selben Push-Zug dieses Branches).
#
# -- HISTORIE (NACHMESSUNG 2026-08-14, LANDUNG S-7+WACHEN+P6) --
# GEMESSEN am 2026-08-14 in diesem Repo, Stand a6804dfa (development im Lande-Baum
# wt-super-landung nach den DREI seriellen --no-ff-Merges der Drei-Stufen-Landung
# Stufe 1: d215b127 = S-7/P2, 0304ebf1 = Wachen-Floor-Inventar/#39, a6804dfa =
# P6-Lizenz), Messhost prod1, GCC 15.3.0, Ninja. Bau-Baum build-covguard KALT
# angelegt (existierte beim Start nicht), Configure UNVERAENDERT nach dem Rezept
# des HISTORIE-Abschnitts 'MESSUNG 2026-08-10' unten einschliesslich des
# Pruefling-Schalters (CC=gcc-15 CXX=g++-15 configure.sh --with-generator=Ninja
# --enable-build-tests --build-dir=build-covguard
# -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"). ABWEICHUNG, benannt:
# statt 'make inventar' (Vollbau) wurden NUR die beiden Registrierungs-Werkzeuge
# gebaut (cmake --build build-covguard --target comdare_anatomy_codegen_cli
# comdare_adhoc_emitter_cli) und dann RE-KONFIGURIERT (J-1-Muster; das Protokoll
# selbst verlangt 'bauen, dann reconfigure'). Beleg statt Schrittzahl:
# Registrierungs-Protokoll dieses Baums 5 von 5 bedingten Bloecken AKTIV
# (Erst-Configure ohne Werkzeuge: 4 UEBERSPRUNGEN, 488 Tests -- der falsche Baum,
# genau wie der J-1-Absatz es beschreibt). Der Vollbau mit ctest-LAUF folgt im
# KON55-Kombibau der Landungs-Stufe 2 und zaehlt mit ctest -N nach. Zaehlung je
# Klasse: ctest --test-dir build-covguard -N mit sed/sort -u/wc -l wie unten.
#     avx512f (gemessen, prod1)                                          -> 492
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 488
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 486
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 492 Tests,
# Namensmenge Byte-gleich zur Erstmessung (cmp leer). Namensdiffs mit LC_ALL=C comm
# in BEIDE Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER,
# avx2->basis die ZWEI, Gegenrichtungen leer -- die Sprossenabstaende -4 und -2
# stehen.
#
# WOHER +1 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diffs der drei
# Merge-Elternteile in tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts
# entfallen): GENAU EINE neue Registrierung, test_lizenz_konsistenz (P6
# Lizenz-Umsetzung, comdare_add_test, LABELS wachen;lizenz;compliance). Der
# Wachen-Strang (#39) registriert NICHTS (4 Dateien, tests/unit/CMakeLists.txt
# unberuehrt); die +2 des S-7-Strangs stecken bereits in der 491 des HISTORIE-
# Abschnitts darunter. test_lizenz_konsistenz ist weder ISA- noch Pruefling-
# gegatet (Registrierung auf oberster Ebene; der Test steht in ALLEN drei
# Klassen-Namensmengen, je 1x gezaehlt, und in KEINER comm-Verlustliste).
# Eigener Commit, wie der Datei-Kopf es verlangt (S-3-Praezedenz 965cd6da).
#
# -- HISTORIE: NACHMESSUNG 2026-08-13 (S-7-ABNAHME) -- WIE 491/487/485 ENTSTANDEN ----
# GEMESSEN am 2026-08-13 in diesem Repo, Stand = S-7-Arbeitsbaum auf Basis 5f3f26a5 (Branch
# bau/s7-algo-hardware-stempel, Worktree wt-ce-mess-ordnung), Messhost prod1, GCC 15.3.0,
# Ninja. Bau-Baum build-covguard KALT angelegt (existierte beim Start nicht) und mit dem
# Rezept des HISTORIE-Abschnitts 'MESSUNG 2026-08-10' unten gebaut, UNVERAENDERT
# einschliesslich des Pruefling-Schalters
# (configure.sh --with-generator=Ninja --enable-build-tests
#  -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"; make inventar;
#  ctest -N | sed 's/^ *Test *#[0-9][0-9]*: *//p' | LC_ALL=C sort -u | wc -l);
# Registrierungs-Protokoll dieses Baums: 5 von 5 bedingten Bloecken AKTIV.
#     avx512f (gemessen, prod1)                                          -> 491
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 487
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 485
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 491 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Namensdiffs mit LC_ALL=C comm in BEIDE
# Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER, avx2->basis die ZWEI,
# Gegenrichtungen leer -- die Sprossenabstaende -4 und -2 stehen.
#
# WOHER +2 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff 5f3f26a5..S-7 in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen): GENAU ZWEI neue
# Registrierungen, test_stempel_vollbestand_registry + test_stempel_zulassung_bruecke
# (S-7 Achsen-Algo-Hardware-Stempel, Paket P2, comdare_add_test am Dateiende). Der
# Zugangs-Diff traegt 0 Treffer auf COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE (Diff-Probe
# nachgemessen 2026-08-13 am Bereich 5f3f26a5..ec3c71fa, git diff -U0, added-Zeilen:
# 0 Treffer in 40 (tests/unit/CMakeLists.txt) wie in allen 449 (tests/unit/ gesamt);
# die 3 Treffer des Gesamt-Diffs, Nenner 770 added-Zeilen, liegen saemtlich in
# Kommentarzeilen DIESER Datei -- 2x Sprossen-Rezept, 1x der Probe-Satz selbst -- und
# registrieren nichts. Der hier zuvor genannte Nenner '151 added-Zeilen' war am Objekt
# in KEINEM Scope reproduzierbar; korrigiert in der S-7-Fix-Stufe, eigener Commit);
# beide Tests stehen in ALLEN drei Klassen-Namensmengen
# (je 1x gezaehlt). Eigener Commit, wie der Datei-Kopf es verlangt.
#
# -- HISTORIE: NACHMESSUNG 2026-08-13 (WELLEN-LANDUNG P1+P11) -- WIE 489/485/483 ENTSTANDEN
# GEMESSEN am 2026-08-13 in diesem Repo, Stand b91b22fc (development im Haupt-Baum nach den
# beiden Wellen-Merges: P1 4c37116a = S-3, P11 b91b22fc = S-14a-Riegel), Messhost prod1,
# GCC 15.3.0, Ninja. Bau-Baum build-covguard KALT angelegt (existierte beim Start nicht) und
# mit dem Rezept des HISTORIE-Abschnitts 'MESSUNG 2026-08-10' unten gebaut, UNVERAENDERT
# einschliesslich des Pruefling-Schalters (dasselbe Rezept wie im obersten Abschnitt);
# Registrierungs-Protokoll dieses Baums: 5 von 5 bedingten Bloecken AKTIV.
#     avx512f (gemessen, prod1)                                          -> 489
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 485
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 483
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 489 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Namensdiffs mit LC_ALL=C comm in BEIDE
# Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER, avx2->basis die ZWEI,
# Gegenrichtungen leer -- die Sprossenabstaende -4 und -2 stehen.
#
# WOHER +1 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff 4a89aed5..b91b22fc in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen): GENAU EINE neue
# Registrierung, test_s14_axis_version_lock_tripwire (S-14a Riegel, Paket P11, rohes
# add_test). Der Zugangs-Diff traegt 0 Treffer auf COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE
# (Diff-Probe gemessen); der Test steht in ALLEN drei Klassen-Namensmengen (je 1x gezaehlt).
# Eigener Commit, wie der Datei-Kopf es verlangt.
#
# -- HISTORIE: NACHMESSUNG 2026-08-13 (S-3-Abnahme) -- WIE 488/484/482 ENTSTANDEN ------
# GEMESSEN am 2026-08-13 in diesem Repo, Stand 4a89aed5 (S-3 Ordnungs-Relation, Branch
# bau/s3-ordnungsrelation, Worktree wt-ce-mess-ordnung), Messhost prod1, GCC 15.3.0, Ninja.
# Bau-Baum build-covguard KALT angelegt (existierte beim Start nicht) und mit dem Rezept des
# HISTORIE-Abschnitts unten gebaut, UNVERAENDERT einschliesslich des Pruefling-Schalters
# (configure.sh --with-generator=Ninja --enable-build-tests
#  -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"; make inventar;
#  ctest -N | sed 's/^ *Test *#[0-9][0-9]*: *//p' | LC_ALL=C sort -u | wc -l):
#     avx512f (gemessen, prod1)                                          -> 488
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 484
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 482
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 488 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Namensdiffs mit LC_ALL=C comm in BEIDE
# Richtungen: avx512f->avx2 verliert GENAU die vier im HISTORIE-Abschnitt genannten,
# avx2->basis GENAU die zwei dort genannten, beide Gegenrichtungen LEER -- die
# Sprossenabstaende -4 und -2 sind unveraendert.
#
# WOHER +9 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff 6c010cdc..4a89aed5 in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen):
#   +2  S-3 (dieser Stand): test_s3_ordnung_relation, test_s3_ordnung_freigabe
#   +7  fruehere Pakete seit der Sammellandung, deren Sprossen-Nachzug HIER fehlte:
#       test_anker_marke_statt_ledgerzeile, test_d37b_modus_riegel, test_pa1_tote_ausnahme,
#       test_vor_push_gate, test_vs_taxonomie_klassen_grep (comdare_add_test) sowie
#       test_bvset_teilmenge, test_s1_stempel_basis_vertrag (rohes add_test). Das ist
#       DERSELBE Versaeumnis-Typ, den der ca6d8af1-Absatz unten beschreibt: wer eine
#       Registrierung anlegt, muss diese Datei im selben Zug anfassen.
# KEINER der neun Zugaenge ist ISA- oder Pruefling-gegatet (Diff-Probe: 0 Treffer auf
# COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE; alle neun registrieren auf oberster Ebene;
# die gemessenen Klassen-Differenzen oben bestaetigen es). Der Pruefling-Beitrag bleibt
# +1 (test_pruefling_fixture_ladung); ein Baum OHNE den Schalter zaehlt entsprechend 487
# (KON55-Kombibau der S-3-Abnahme: 4x gemessen, gcc-15/clang-22 x Debug/Release, alle 487
# mit Byte-gleicher Namensmenge).
#
# -- HISTORIE: MESSUNG 2026-08-10 -- WIE 479/475/473 ENTSTANDEN (nachgemessen) -
# GEMESSEN am 2026-08-10 in diesem Repo, Stand 6c010cdc (die Sammellandung, auf der
# Pipeline 15536 fuhr), Worktree wt-ce-fk, Messhost prod1, GCC 15.3.0, Ninja. Der
# Bau-Baum build-covguard wurde dafuer KALT angelegt -- er existierte beim Start
# nicht -- und mit dem Rezept des Jobs test:coverage-guard gebaut, einschliesslich
# des Pruefling-Schalters, der diesen Baum von dem des Jobs test:unit unterscheidet:
#
#     CC=gcc-15 CXX=g++-15 sh ./configure.sh --with-generator=Ninja \
#         --enable-build-tests --build-dir=build-covguard \
#         -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"
#     make inventar BUILD_JOBS=4
#     ctest --test-dir build-covguard -N | sed -n 's/^ *Test *#[0-9][0-9]*: *//p' \
#         | LC_ALL=C sort -u | wc -l    ->    479
#
# Dass der Baum wirklich der des Jobs ist, haengt nicht an der Absicht: der Bau
# meldete 2172 Schritte, genau wie Job 371636. Ohne den Pruefling-Schalter sind es
# 2168 Schritte und 478 Tests -- der Block 'pruefling_slots_v1' steht dann als
# UEBERSPRUNGEN im Registrierungs-Protokoll. Wer hier ohne den Schalter misst, misst
# den falschen Baum; genau das ist beim Nachmessen am 2026-08-10 zuerst passiert.
#
# Dieser Messhost ist die REICHSTE Klasse: CMakeCache.txt meldet
# COMDARE_HOST_RUNS_AVX2=1 UND COMDARE_HOST_RUNS_AVX512F=1. 479 ist damit die
# Untergrenze fuer 'avx512f' und fuer sonst keine.
#
# FREMDER NENNER (T-3), damit die Zahl nicht nur aus der eigenen Messung stammt: der
# Job test:unit derselben Pipeline (371647, prod1, Stand 6c010cdc) hat seine Inventur
# als Artefakt veroeffentlicht -- build/Testing/ctest_unit_inventar.txt, 478 Namen.
# Sie ist mit der hiesigen Messung OHNE Pruefling-Schalter BYTE-GLEICH (diff leer).
# Zwei Baeume, zwei Bau-Wege, zwei Maschinenlaeufe, dieselbe Namensmenge.
#
# DIE BEIDEN ANDEREN SPROSSEN SIND NICHT GERECHNET, SONDERN VON CMAKE ERZEUGT
# (Gegenorakel, V-7: die Zahl kommt aus einer anderen Quelle als der Rechnung).
# Verfahren: denselben Baum mit erzwungener Klasse neu KONFIGURIEREN (kein Neubau)
# und die Namensmengen vergleichen --
#     cmake -S . -B build-covguard -DCOMDARE_HOST_RUNS_AVX512F=0                  -> 475
#     cmake -S . -B build-covguard -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0 -> 473
# und danach 'cmake -U COMDARE_HOST_RUNS_AVX2 -U COMDARE_HOST_RUNS_AVX512F', damit
# die Probe wieder echt laeuft. Die Wiederherstellung ist nachgewiesen: 479 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Die Differenzen sind NAMENTLICH belegt,
# in beide Richtungen (die Gegenrichtung war jeweils LEER -- es kommt nichts hinzu).
# ALLE Vergleiche mit LC_ALL=C, auch die von 'comm': ein comm ohne C-Kollation haelt
# eine C-sortierte Datei fuer unsortiert und darf dann alles melden -- am 2026-08-10
# genau so passiert und deshalb wiederholt worden.
#
#   avx512f -> avx2  verliert genau diese VIER:
#       test_ap5_simd_extension_coherence      (Block an AVX2 UND AVX512F, tests/unit/CMakeLists.txt:4236)
#       test_buildvariant_dll_real             (Block an AVX2 UND AVX512F, tests/unit/CMakeLists.txt:4236)
#       test_simd_add_u64_carry_avx512         (do_run an AVX512F, tests/unit/CMakeLists.txt:5446)
#       test_simd_field_sum_dispatch_avx512    (do_run an AVX512F, tests/unit/CMakeLists.txt:5414)
#
#   avx2 -> basis    verliert zusaetzlich diese ZWEI:
#       test_simd_add_u64_carry_avx2           (do_run an AVX2, tests/unit/CMakeLists.txt:5443)
#       test_simd_field_sum_dispatch_avx2      (do_run an AVX2, tests/unit/CMakeLists.txt:5401)
#
# Mehr ISA-gattierte Registrierungen gibt es nicht: 'COMDARE_HOST_RUNS' kommt in
# tests/unit/CMakeLists.txt an genau ZEHN Stellen vor --
#   VIER Proben-Definitionen  4200, 4201, 5363, 5364  (dieselben zwei
#       check_cxx_source_runs-Aufrufe stehen ZWEIMAL in der Datei; der zweite ist
#       fuer den unteren Abschnitt da und ueberschreibt nichts: das Modul steigt bei
#       'if(NOT DEFINED "${_var}")' aus, Modules/Internal/CheckSourceRuns.cmake:7)
#   EINE Block-Bedingung      4236  (if(AVX2 AND AVX512F) -> die zwei add_test in
#       4237 und 4251)
#   VIER do_run-Argumente     5401, 5414, 5443, 5446
#   EIN Kommentar             6232  (bei test_mt_l4_registrierungs_wache_isa; registriert nichts)
# Bis zur Sammellandung waren es NEUN; der zehnte Fundort ist der Kommentar bei 6232,
# der mit dem mt-l4-Paket hinzukam. Die REGISTRIERENDEN Stellen sind unveraendert --
# deshalb sind die Sprossenabstaende -4 und -2 dieselben geblieben.
# Sonst REGISTRIERT niemand im Repo hinter diesen Variablen; 'grep -rl' ueber alle
# CMakeLists.txt und *.cmake ausserhalb von ext/ liefert genau diese eine Datei. Der
# Name kommt zwar noch woanders vor -- scripts/ci_test_coverage_guard.sh (liest ihn,
# 4 Fundstellen), scripts/ci_test_sichtbarkeit_wache.sh:59,328 und
# tests/unit/test_ap5_simd_extension_coherence.cpp:10 (beide nur im Kommentar) --,
# aber keine davon legt einen ctest-Eintrag an. Die frueher hier stehende Formel
# "sonst nirgends im Repo" war deshalb zu weit gegriffen.
#
# -- WARUM DIE ZAHL GEFALLEN IST, UND WARUM DAS KEIN SCHWUND IST --------------
# Die Sammellandung 6c010cdc hat die Inventur von 496 auf 479 gesenkt. Die Wache hat
# das in Pipeline 15536 (Job 371636, prod1) als NENNER-BEFUND gemeldet -- literal
# "UNTERSCHRITTEN um 17 Test(e)", Exit 4 -- und damit genau getan, wofuer sie da ist.
# Die 17 sind vollstaendig aufgeklaert; jeder Posten ist gemessen, keiner gerechnet:
#
#   496   Stand 83a6d443, prod1, Klasse avx512f, Job-Rezept OHNE Pruefling-Schalter.
#         Den gab es zu diesem Stand im Job noch nicht:
#         'git show 83a6d443:.gitlab-ci.yml | grep -c COMDARE_CE_PRUEFLINGE' -> 0.
#   -28   ca6d8af1 "D2-G1: die zwei gtest-Binaries unter libs/ nach dem Repo-Muster
#         registrieren". gtest_discover_tests legte je gtest-FALL einen ctest-Eintrag
#         an, add_test legt je BINARY einen an. Am Objekt gezaehlt (2026-08-10, die
#         gebauten Binaries mit '--gtest_list_tests'): test_commands 23 Faelle,
#         test_engine_adapters 7, zusammen 30 -- jetzt 2 Eintraege.
#         KEIN Testfall ist verschwunden: gtest_main faehrt beim Start der Binary
#         weiterhin alle 30. In 15536 sind beide Eintraege gruen gelaufen
#         (Job 371647, Test #1 und #2, "Passed").
#   +11   neue Test-Ziele der Landung: test_d2g1_ctest_registrierung,
#         test_d3_6_realm_selektor_produktionsablage, test_hyg_kumulativer_bereich,
#         test_mt_l3_wide_schema_orakel, test_mt_l4_registrierungs_wache_isa,
#         test_t6_biss_achsen_roundtrip_wache, test_t6_biss_registrierungs_wache,
#         test_t6_biss_sichtbarkeits_wache, test_t6_wachen_inventar,
#         test_w0b_durchstich_kette und test_pruefling_fixture_ladung. Das letzte
#         zaehlt nur, weil DIESELBE Landung dem Job den Schalter
#         -DCOMDARE_CE_PRUEFLINGE mitgibt (8a8f6877).
#   = 479 Stand 6c010cdc, prod1, Klasse avx512f, Job-Rezept MIT Schalter.
#
# WAS DER COVGUARD-JOB NICHT WAR: zu klein gebaut. Sein Registrierungs-Protokoll in
# 15536 meldet 5 von 5 bedingten Bloecken GELAUFEN, 0 uebersprungen -- alle vier
# 2-Pass-Tests sind drin. Der Nenner war vollstaendig; alt war nur die Untergrenze.
#
# WAS DARAUS FOLGT UND WAS NICHT: die alten drei Sprossen waren NICHT falsch, sie
# waren UEBERHOLT. 492 ist am 2026-08-10 auf prod2 sogar unabhaengig bestaetigt
# worden (Pipeline 15517, Job 371160: "LIVE-INVENTUR (ctest -N): 492", Klasse avx2).
# Sie sind hier gesenkt worden, WEIL der Grund gemessen ist -- nicht, damit die Wache
# gruen wird. Waere der Grund unklar geblieben, waere die Senkung unzulaessig; der
# Kopf dieser Datei sagt es selbst, und er gilt auch fuer den, der ihn gerade liest.
#
# -- WIDERRUF: WAS DIE VORIGE FASSUNG DIESES ABSATZES BEHAUPTETE ---------------
# Sie nannte fuenf Zeilenanker (4134, 5249, 5262, 5291, 5294) und die Aufteilung
# "zwei Proben-Definitionen, eine Block-Bedingung, sechs do_run-Argumente". BEIDES
# WAR FALSCH, und der Beleg dafuer ist so einfach wie der fuer die Korrektur:
#   * Die fuenf Anker loesen am heutigen Stand auf Kommentare und set()-Zeilen auf.
#     Sie loesen sauber auf in 78fcc627 -- dem Stand VOR dem Merge b7d82d73. Die
#     Namens-Analyse ist also am Vor-Merge-Stand gefahren und danach ohne erneuten
#     Blick uebernommen worden.
#   * Damit waren auch die ZAHLEN nicht gemessen, sondern fortgeschrieben: 492 und
#     490 sind die alten 488 und 486 plus die 4, die der Merge zur avx512f-Sprosse
#     hinzufuegte. Genau das schliesst der Absatz drei Zeilen weiter oben fuer sich
#     aus ("NICHT GERECHNET, SONDERN VON CMAKE ERZEUGT").
#   * Die drei Zahlen waren trotzdem RICHTIG -- fuer IHREN Stand. Sie standen nicht
#     mehr deshalb da, sondern weil sie am 2026-08-10 am Stand 83a6d443 neu erhoben
#     wurden, mit den zwei Namensmengen und beiden Gegenrichtungen. Der Merge hat
#     keine ISA-gattierte Registrierung hinzugefuegt; die Abstaende -4 und -2 sind
#     dieselben geblieben.
# DIESER ABSATZ IST HISTORIE: er beschreibt 496/492/490 am Stand 83a6d443. Danach galten
# seit der Sammellandung 479/475/473 (s. HISTORIE-Abschnitt darueber), ab der S-3-Abnahme
# 2026-08-13 die Werte 488/484/482, ab der Wellen-Landung P1+P11 (gleicher Tag, Stand
# b91b22fc) die Werte 489/485/483; gueltig sind seit der S-7-Abnahme (gleicher Tag, Basis
# 5f3f26a5 + 2 Stempel-Tests) die Werte 491/487/485, s. den obersten Abschnitt.
#
# NEBENBEI, damit niemand hineinlaeuft: das Erzwingen oben ist ein Verfahren fuer
# 'ctest -N', NICHT fuer die Wache. Auf einem so erzwungenen Baum bricht
# scripts/ci_test_coverage_guard.sh seit 2026-08-10 mit Exit 2 ab -- eine
# erzwungene Host-Klasse ist keine gemessene, und sie als solche zu nehmen war der
# Defekt, den diese Nachbesserung geschlossen hat. Erst 'cmake -U ...' macht den
# Baum wieder messbar.
#
# EINE VIERTE KLASSE GIBT ES NICHT: 'AVX-512F ohne AVX2' existiert auf keiner
# x86-Maschine. Stuende es im Cache, waere nicht die Klasse ungewoehnlich, sondern
# der Cache unglaubwuerdig -- die Wache bricht dort ab.
#
# -- HISTORIE: DIE SPROSSEN 496 / 492 / 490 (DEPRECATED 2026-08-10) -----------
# Sie galten vom 2026-08-10 (Stand 83a6d443) bis zur Sammellandung 6c010cdc desselben
# Tages. Sie sind NICHT geloescht, weil sie kein Irrtum waren, sondern ein
# ueberholter Messstand -- und weil man an ihnen sieht, wie schnell eine Untergrenze
# ihren Gegenstand verliert: zwischen Messung und Ueberholung lagen Stunden, nicht
# Wochen. Was sie ueberholt hat, steht oben unter "WARUM DIE ZAHL GEFALLEN IST".
# 492 ist unabhaengig belegt (15517/371160, prod2, Klasse avx2) und war damit die
# einzige der drei, die je eine echte Maschine ihrer Klasse gesehen hat.
#
# WAS DARAN ZU LERNEN IST -- und was der Grund fuer den Abschnitt hier ist: ca6d8af1
# hat die Zahl im eigenen Commit-Bogen ausdruecklich genannt ("ctest -N, beide Zahlen
# mit Host: prod1 vorher 496, nachher 469") und die Untergrenze trotzdem nicht
# nachgezogen. Der Paket-Stand hatte auch nie eine eigene Pipeline
# ('/pipelines?sha=ca6d8af1' -> keine); die Wache lief also erstmals in der
# Sammellandung ueber diese Aenderung. Wer eine Registrierung umbaut, aendert den
# Gegenstand DIESER Datei -- und muss sie im selben Zug anfassen.
#
# -- HISTORIE: DIE ALTE ZAHL 488 UND WARUM SIE ZU HOCH WAR ---------------------
# BIS 2026-08-10 stand hier GENAU EINE Zahl, 488, mit dem Anspruch, sie gelte
# "auf JEDER Hardware-Klasse". Sie ist NICHT geloescht, sondern hier vermerkt,
# weil ihr Irrtum die Bauform erklaert:
#
#   * Sie entstand als 492 - 4 am Stand aebc4f2c. Der Abzug 4 war die richtige
#     Zahl fuer den Schritt avx512f -> avx2, wurde aber als Abzug fuer JEDE
#     Klasse verstanden. Fuer eine Maschine ganz ohne AVX2 sind es SECHS.
#     Auf einer 'basis'-Maschine haette der damalige Baum 486 Tests gehabt und
#     die Wache haette 486 < 488 gemeldet -- ROT an einem gesunden Baum.
#   * Ihre Namensliste war ausserdem falsch besetzt: sie nannte
#     test_simd_field_sum_dispatch_avx2 (das faellt erst bei 'basis') und liess
#     test_simd_add_u64_carry_avx512 aus (das faellt schon bei 'avx2'). Die
#     Anzahl stimmte zufaellig, die Menge nicht.
#   * WAS SIE NICHT WAR: die Ursache der roten ce-Pipeline 15515 vom 2026-08-10.
#     Der dortige prod2-Lauf hatte 488 Tests in seiner eigenen unfilterten
#     Inventur ('D1c: Inventur veroeffentlicht -- 488 Tests') und damit GENAU die
#     Untergrenze, nicht weniger. Die 486 aus der ctest-Summenzeile jenes Jobs ist
#     die mit '-LE pmc' GEFILTERTE Laufzahl (488 - 2 pmc-Tests) und wird mit der
#     Untergrenze nie verglichen. Rot war dort ein Umgebungs-Leck im Selbsttest.
#
# -- WAS DIESE ZAHLEN NICHT LEISTEN (ehrlich benannt) -------------------------
# Innerhalb EINER Klasse ist die Untergrenze jetzt exakt und nicht mehr um vier
# Tests locker -- der alte Schlupf ("zwischen 488 und 492 kann auf diesem Host
# einiges verschwinden") ist damit zu. Was sie weiterhin nicht sehen: einen
# Schwund, der zeitgleich mit einem ehrlichen Zuwachs kommt und ihn genau
# ausgleicht. Dagegen stehen das Registrierungs-Protokoll (Befund (c)/(d),
# Exit 4) und die Fremd-Inventur (Befund (e), COMDARE_FREMD_INVENTUR); die
# Untergrenze ist die GROBE Achse gegen Schwund im Grossen und ersetzt die
# beiden feinen nicht.
#
# -- NACHZUG B2 GATE-TRENNUNG (2026-08-15, Scheibe B2 auf Basis 0817c7bf) -----
# KONTO, je Name (Registrierungs-Diff tests/unit/CMakeLists.txt dieser Scheibe,
# Gegenrichtung leer -- nichts entfallen): GENAU EINE neue Registrierung,
# test_b2_mess_gate_trennung (Koeder der G2/G3-Gate-Trennung, add_executable +
# add_test + COMDARE_TEST_TARGETS, LABELS b2;messachse;stempel;gates). Weder
# ISA- noch Pruefling-gegatet (Registrierung auf oberster Ebene, keine
# COMDARE_HOST_RUNS_*-Bedingung) -> +1 in ALLEN DREI Klassen; die
# Sprossenabstaende -4 und -2 bleiben. LIVE nachgemessen nach dem Rezept des
# Datei-Kopfs (cmake-Erzwingen beider Klassen, ctest -N je Klasse, 'cmake -U'
# auf beide Variablen, LC_ALL=C comm in BEIDE Richtungen); Zahlen und
# comm-Verlustlisten woertlich im Bericht der B2-Scheibe.
#
# -- NACHZUG B2-ABSCHLUSS (2026-08-15, Dual-Review-Fixstufe derselben Scheibe) --
# KONTO, je Name (Registrierungs-Diff tests/unit/CMakeLists.txt der Fix-Stufe,
# Gegenrichtung leer -- nichts entfallen): GENAU EINE neue Registrierung,
# test_b2_gate_zustand_g2an_g3aus (Gate-Zustands-Probe G2-an/G3-aus als
# instanziiertes Kompilat, add_executable + add_test + COMDARE_TEST_TARGETS,
# LABELS b2;messachse;gates;standalone). Weder ISA- noch Pruefling-gegatet
# (Registrierung auf oberster Ebene) -> +1 in ALLEN DREI Klassen gegenueber dem
# NACHZUG-B2-Block darueber; Sprossenabstaende -4 und -2 bleiben. Die
# try_compile-Negativprobe derselben Fix-Stufe (b2_probe_g3_schichtung_tu.cpp)
# registriert NICHTS (Configure-Zeit, kein ctest-Eintrag). LIVE nachgemessen
# 2026-08-15 nach dem Rezept des Datei-Kopfs (build-covguard KALT, J-1-Muster:
# beide Registrierungs-Werkzeuge, dann RE-KONFIGURIERT; cmake-Erzwingen beider
# Klassen, ctest -N je Klasse, 'cmake -U' auf beide Variablen, Wiederherstellung
# byte-gleich cmp leer, LC_ALL=C comm in BEIDE Richtungen: avx512f->avx2
# verliert exakt die dokumentierten VIER, avx2->basis die ZWEI, Gegenrichtungen
# leer):
#     avx512f (gemessen, prod1)                                          -> 494
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 490
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 488
#
# LANDE-AUFLAGE (Dual-Review-Fund Floor-Kollision, 2026-08-15) -- FUER DEN, DER
# DIESEN BRANCH ODER bau/golden-homes ALS ZWEITEN MERGT: bau/golden-homes
# (Stand 0eea2a0a) schreibt fuer seinen EIGENEN neuen Test
# (fixture_schema_subset_check_ce) die Wertzeilen 493/489/487 aus derselben
# Basis 492/488/486. Diese Zeilen hier (494/490/488) kollidieren mit ihnen im
# 3-Wege-Merge ABSICHTLICH als Textkonflikt -- das ist die Harmonisierungs-
# Doktrin in Dateiform: beim Merge der ZWEITEN Scheibe den Floor EINMAL LIVE
# neu messen (Rezept im Datei-Kopf; nach Doppel-Landung sind 495/491/489 zu
# ERWARTEN, gueltig ist ausschliesslich die Messung) und NIEMALS eine der
# beiden Seiten uebernehmen oder Deltas addieren. Die Landung dieser Scheibe
# selbst ist ein DEKLARIERTES GOLDEN-EREIGNIS und faehrt AUSSCHLIESSLICH im
# golden-Fenster (mess_achsen_naht.hpp, Abschnitt BYTE-BILANZ B2).
#
# FORMAT: '#'-Kommentare und Leerzeilen sind erlaubt. Wertzeilen haben die Form
# '<klasse> <ganzzahl>'. Verlangt sind ALLE DREI Klassen, jede GENAU EINMAL.
#
# NACHZUG 18.08.2026 (Q2/V-06/KON101-02, Bump-Bruch #15): 502/498/496 -> 503/499/497.
# ANLASS: EINE neue ctest-Registrierung, test_q2_identitaets_riegel (der Identitaets-Riegel ueber
# den echten dlopen-Ladeweg). Sie ist UNBEDINGT -- kein ISA-Gate, kein opt-in-Label --, also
# bewegt sie alle drei Sprossen um dieselbe 1.
# WIE GEMESSEN, NICHT GERECHNET: die avx512f-Sprosse ist die Klasse dieser Maschine (prod1) und
# wurde LIVE abgelesen -- gcc-release, voller Bau ueber --target comdare_tests, ctest-Schlusszeile
# literal "100% tests passed, 0 tests failed out of 503". Die beiden anderen Sprossen sind auf
# dieser Klasse nicht messbar (andere Host-Klasse); fuer sie gilt die im Kopf festgehaltene
# +1-Regel unbedingter Tests, wie beim 500er-Nachzug c3478a9c. Kein Delta-Addieren ueber mehrere
# Aenderungen hinweg: dies ist EINE Registrierung in EINEM Zug.
# Eine nackte Zahl ohne Klasse (die alte Bauform) ist ein Abbruch, keine Wahl.
#
# NACHZUG 19.08.2026 (A2.5-Fixstrecke-2 G4, Bump-Bruch #15): 503/499/497 -> 506/502/500.
# KONTO, je Name (alle drei UNBEDINGT registriert -- comdare_add_test auf oberster Ebene, kein
# ISA-Gate, kein opt-in-Label -- also je +1 auf allen drei Sprossen):
#   test_hy_a2_stempel_kette            (G3/A-03, Commit 06bcbba3)  -- NACHHOLUNG: die
#     Registrierung kam OHNE den hier pflichtigen Floor-Nachzug im selben Change (#39-Verstoss,
#     in G4 als Verzug festgestellt und hier benannt ausgeglichen, nicht still addiert).
#   test_axis_kind_kategorien_zuordnung (V-01R,  Commit e34666fa)  -- NACHHOLUNG, derselbe Fall.
#   test_hy_a1_attach_nur_array_include (G4/L21, DIESER Change)    -- die F-9-Link-Probe.
# WIE GEMESSEN, NICHT GERECHNET: avx512f ist die Klasse dieser Maschine (prod1) und wurde LIVE
# abgelesen -- gcc-release, voller Bau (all + comdare_tests), ctest-Schlusszeile literal
# "100% tests passed, 0 tests failed out of 506". Fuer avx2/basis gilt die im Kopf festgehaltene
# +1-Regel unbedingter Tests je Registrierung (drei Registrierungen -> +3), wie beim 500er- und
# 503er-Nachzug. Die Drei-in-einem-Zug-Form ist dem Verzug geschuldet und AUSDRUECKLICH als
# solcher dokumentiert: zwei der drei Zeilen gehoeren zu bereits gelandeten Commits dieses
# Branches, ihr Nachzug war faellig und fehlte.

# NACHZUG 20.08.2026 (F2-5 Posten 1, Band A): 507/503/501 -> 508/504/502. KONTO, EIN Name in
# EINEM Zug (Registrierung und Floor-Zeile im SELBEN Commit, #39-Doktrin):
#   test_lg_skip_callback_null (LG-SkipCallback==0: SKIP-Zweitlauf ruft den Mess-Callback 0-mal;
#     DIESER Change) -- UNBEDINGT registriert (add_test auf oberster Ebene, kein ISA-Gate, kein
#     opt-in-Label) => +1 auf allen drei Sprossen (die im Kopf festgehaltene +1-Regel).
# WIE GEMESSEN, NICHT GERECHNET: avx512f ist die Klasse dieser Maschine (prod1) und wurde LIVE
# abgelesen -- gcc-release, inkrementeller Bau, 'ctest -N'-Schlusszeile literal
# "Total Tests: 508". Fuer avx2/basis gilt die +1-Regel wie beim 500er-, 503er- und 506er-Nachzug.
#
# NACHZUG 20.08.2026 (F2-5 Posten 2, Band A): 508/504/502 -> 509/505/503. KONTO, EIN Name in
# EINEM Zug (Registrierung und Floor-Zeile im SELBEN Commit, #39-Doktrin):
#   test_lg_xlsx_alt_nie_vernichten (LG-XlsxAlt "nie vernichten": Versions-Wechsel schreibt additiv,
#     Alt-Datei vollbyte-identisch; DIESER Change) -- UNBEDINGT registriert (comdare_add_test auf
#     oberster Ebene, kein ISA-Gate, kein opt-in-Label) => +1 auf allen drei Sprossen.
# WIE GEMESSEN, NICHT GERECHNET: avx512f ist die Klasse dieser Maschine (prod1) und wurde LIVE
# abgelesen -- gcc-release, inkrementeller Bau, 'ctest -N'-Schlusszeile literal
# "Total Tests: 509". Fuer avx2/basis gilt dieselbe +1-Regel unbedingter Tests.

# =============================================================================
# NACHZUG 20.08.2026 (L1-LANDUNG #15-Bruch in development, Merge-Stand): 510/506/504
# loest 509/505/503 ab. Der Zuwachs ist +1 in JEDER Klasse und stammt aus der
# g2-Landung ba33b9ac (1 neuer Test der dev-Seite), am MERGE-Stand LIVE gemessen
# (A2.1b: geteilte Zaehlwerke EINMAL live, nie je Strang addiert):
# WIE GEMESSEN, NICHT GERECHNET: avx512f ist die Klasse dieser Maschine (prod1) und wurde LIVE
# erhoben: frischer build-l1 (Frisch-Configure MIT -DCOMDARE_CE_PRUEFLINGE, J-1 SIEBEN Werkzeuge,
# RECONFIGURE, all, comdare_tests, RECONFIGURE2), dann "Total Tests: 510". Fuer avx2/basis gilt
# dieselbe +1-Regel unbedingter Tests.
# NACHZUG 20.08.2026 (N-1/F2-8, #15-Nachlandeschritt): 511/507/505 loest 510/506/504 ab.
# KONTO, EIN Name in EINEM Zug (Registrierung und Floor-Zeile im SELBEN Commit, #39-Doktrin):
#   test_s6c_kategorien_ordnung_zwei_welten (S-6c/S-21 Kategorien-Ordnungs-Wache: ZWEI-WELTEN-
#     Auflage -- Aussen MESS,SYSTEM,ORGAN / Lager-Messdaten MESS->SYSTEM->ORGAN / Lager-Binaries
#     SYSTEM->ORGAN->MESS (D-12), NIE-EINE-FOLGE-Beweis, R-A..R-F benennbar; DIESER Change) --
#     UNBEDINGT registriert (comdare_add_test auf oberster Ebene, kein ISA-Gate, kein opt-in-
#     Label) => +1 auf allen drei Sprossen (die im Kopf festgehaltene +1-Regel).
# WIE GEMESSEN, NICHT GERECHNET: avx512f ist die Klasse dieser Maschine (prod1) und wurde LIVE
# abgelesen -- gcc-release, voller Bau (all + comdare_tests + RECONFIGURE2, Frisch-Configure MIT
# -DCOMDARE_CE_PRUEFLINGE), 'ctest -N'-Schlusszeile literal "Total Tests: 511". Fuer avx2/basis
# gilt dieselbe +1-Regel unbedingter Tests wie beim 508er-, 509er- und 510er-Nachzug.
# NACHZUG 21.08.2026 (W1-LUECKEN-LANDUNG B01-B16, Merge ee8abb86): 523/519/517 loest
# 511/507/505 ab. KONTO, ZWOELF Namen in EINEM Push-Change (#39-Doktrin: Registrierung im
# W1L-Baustrang bau/w1-luecken=3149c8a2, dieser Nachzug im SELBEN Push-Bereich; der volle
# Lande-Gates-Lauf meldete das Fehlen als GATE-[5/6]-ROT, DIESER Change schliesst es):
#   test_genus_ct_komposition (B02) . test_simd_organ_achsen_deckung (B03) .
#   test_vl3_debug_stdout_bytegleich (B04) . test_lg_host_binder (B05) .
#   test_lg_e2e_xlsx_facade (B06) . test_pk_klemm_json (B07) .
#   test_xl_l4_na_konkurrenz (B08) . test_lg_load_wache_genus_kontrast (B09a) .
#   test_lg_idempotenz_beide_wdh (B09b) . test_registry_gen_out_pflicht (B12) .
#   test_ci_coverage_guard_strikt (B13) . test_configure_enable_gnu (B15)
# ALLE ZWOELF UNBEDINGT registriert (7x comdare_add_test + 5x add_test auf oberster Ebene;
# Zugangs-Diff 66de5c09..ee8abb86 traegt 0 Treffer auf COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE)
# => +12 auf allen drei Sprossen; die Sprossenabstaende -4 und -2 bleiben unveraendert.
# WIE GEMESSEN, NICHT GERECHNET, ALLE DREI SPROSSEN LIVE an DIESEM Baum: frischer build-l1
# (gcc-release-Form, Frisch-Configure MIT -DCOMDARE_CE_PRUEFLINGE, J-1 SIEBEN Werkzeuge,
# RECONFIGURE, all, comdare_tests, RECONFIGURE2), 'ctest -N'-Schlusszeile literal
# "Total Tests: 523"; avx2/basis per cmake erzwungen (-DCOMDARE_HOST_RUNS_AVX512F=0 -> 519;
# zusaetzlich -DCOMDARE_HOST_RUNS_AVX2=0 -> 517); jede Namensliste mit Gegenprobe
# Listenlaenge == Total (523/523, 519/519, 517/517).
# ZWEI-SEITIGE GEGENPROBE am Objekt, LC_ALL=C comm in BEIDE Richtungen:
#     avx512f -> avx2  verliert exakt die dokumentierten VIER
#                      (test_ap5_simd_extension_coherence, test_buildvariant_dll_real,
#                       test_simd_add_u64_carry_avx512, test_simd_field_sum_dispatch_avx512)
#     avx2    -> basis verliert exakt die ZWEI
#                      (test_simd_add_u64_carry_avx2, test_simd_field_sum_dispatch_avx2)
#     beide Gegenrichtungen LEER -- kein verstecktes -N/+M; KEIN 12er-Name in einer
#     Verlustliste (je genau 12 in allen drei Namensmengen).
# Danach 'cmake -U' auf BEIDE Variablen: Wiederherstellung BYTE-GLEICH (Total wieder 523,
# sortierte Namensliste cmp ohne Ausgabe, beide Cache-Schalter wieder INTERNAL=1).
# PRUEFLING-GEGENPROBE: Namensmenge MIT Pruefling minus J-0b-Baum (build/gcc-release, 522)
# == exakt test_pruefling_fixture_ladung, Gegenrichtung LEER.
# MESSHOST prod1, GCC 15.3.0, Ninja, 2026-08-21. Messlogs:
# /home/comdare/backups-workflow/20260821-w1l-landung/floor-messung.log (+ floor-logs/).
avx512f 523
avx2 519
basis 517
