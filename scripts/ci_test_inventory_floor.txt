# UNTERGRENZE DER CTEST-INVENTUR -- EINE ZAHL JE HOST-KLASSE (D2-G5, 2026-08-10)
# =============================================================================
# WAS DIESE ZAHLEN SIND: je Hardware-Klasse die kleinste Testzahl, die ein RICHTIG
# gebauter Baum dieses Repos auf DIESER Klasse noch haben darf. Faellt 'ctest -N'
# darunter, ist etwas verschwunden -- scripts/ci_test_coverage_guard.sh meldet
# dann einen NENNER-BEFUND (Exit 4).
#
# WELCHE ZEILE GILT, ENTSCHEIDET NICHT DIE MEINUNG, SONDERN DER BAU-BAUM: die
# Wache liest COMDARE_HOST_RUNS_AVX2 und COMDARE_HOST_RUNS_AVX512F aus der
# CMakeCache.txt DESSELBEN Baums, den sie gerade zaehlt. Laesst sich die Klasse
# nicht bestimmen, ist das ABBRUCH (Exit 2) -- nicht die hoechste und nicht die
# niedrigste Annahme.
#
# WARUM SIE IN EINER DATEI STEHEN UND NICHT IM SKRIPT: die Wache bezieht sonst
# JEDE ihrer Zahlen aus dem Baum, den sie gerade prueft. Schrumpft der Baum,
# schrumpfen Zaehler und Nenner gemeinsam und das Verhaeltnis bleibt 'vollstaendig'
# (V-7 FREMDER NENNER). Diese Zahlen sind die einzigen im ganzen Lauf, die der
# gemessene Baum nicht beeinflussen kann: sie stammen aus einem frueher bewusst
# abgenommenen Zustand und aendern sich nur durch einen Menschen in einem eigenen,
# im Diff sichtbaren Commit.
#
# DIE WACHE SCHREIBT DIESE DATEI NIEMALS SELBST. Eine Untergrenze, die sich beim
# Unterschreiten automatisch nachfuehrt, ist keine Untergrenze, sondern ein
# Protokoll des Verfalls. Waechst die Testzahl, sagt die Wache das ausdruecklich
# an und die Zahlen hier gehoeren dann von Hand nachgezogen -- als eigener Commit,
# damit die Aenderung im Diff auffaellt und jemand sie absichtlich verantwortet.
# NACHZIEHEN HEISST ALLE DREI: die Sprossenabstaende (-4 und -2) sind Teil der
# Aussage, nicht Beiwerk.
#
# -- WIE DIE ZAHLEN ENTSTANDEN SIND (NACHMESSUNG 2026-08-13, S-7-ABNAHME) ------------
# GEMESSEN am 2026-08-13 in diesem Repo, Stand = S-7-Arbeitsbaum auf Basis 5f3f26a5 (Branch
# bau/s7-algo-hardware-stempel, Worktree wt-ce-mess-ordnung), Messhost prod1, GCC 15.3.0,
# Ninja. Bau-Baum build-covguard KALT angelegt (existierte beim Start nicht) und mit dem
# Rezept des HISTORIE-Abschnitts 'MESSUNG 2026-08-10' unten gebaut, UNVERAENDERT
# einschliesslich des Pruefling-Schalters
# (configure.sh --with-generator=Ninja --enable-build-tests
#  -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"; make inventar;
#  ctest -N | sed 's/^ *Test *#[0-9][0-9]*: *//p' | LC_ALL=C sort -u | wc -l);
# Registrierungs-Protokoll dieses Baums: 5 von 5 bedingten Bloecken AKTIV.
#     avx512f (gemessen, prod1)                                          -> 491
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 487
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 485
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 491 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Namensdiffs mit LC_ALL=C comm in BEIDE
# Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER, avx2->basis die ZWEI,
# Gegenrichtungen leer -- die Sprossenabstaende -4 und -2 stehen.
#
# WOHER +2 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff 5f3f26a5..S-7 in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen): GENAU ZWEI neue
# Registrierungen, test_stempel_vollbestand_registry + test_stempel_zulassung_bruecke
# (S-7 Achsen-Algo-Hardware-Stempel, Paket P2, comdare_add_test am Dateiende). Der
# Zugangs-Diff traegt 0 Treffer auf COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE (Diff-Probe
# nachgemessen 2026-08-13 am Bereich 5f3f26a5..ec3c71fa, git diff -U0, added-Zeilen:
# 0 Treffer in 40 (tests/unit/CMakeLists.txt) wie in allen 449 (tests/unit/ gesamt);
# die 3 Treffer des Gesamt-Diffs, Nenner 770 added-Zeilen, liegen saemtlich in
# Kommentarzeilen DIESER Datei -- 2x Sprossen-Rezept, 1x der Probe-Satz selbst -- und
# registrieren nichts. Der hier zuvor genannte Nenner '151 added-Zeilen' war am Objekt
# in KEINEM Scope reproduzierbar; korrigiert in der S-7-Fix-Stufe, eigener Commit);
# beide Tests stehen in ALLEN drei Klassen-Namensmengen
# (je 1x gezaehlt). Eigener Commit, wie der Datei-Kopf es verlangt.
#
# -- HISTORIE: NACHMESSUNG 2026-08-13 (WELLEN-LANDUNG P1+P11) -- WIE 489/485/483 ENTSTANDEN
# GEMESSEN am 2026-08-13 in diesem Repo, Stand b91b22fc (development im Haupt-Baum nach den
# beiden Wellen-Merges: P1 4c37116a = S-3, P11 b91b22fc = S-14a-Riegel), Messhost prod1,
# GCC 15.3.0, Ninja. Bau-Baum build-covguard KALT angelegt (existierte beim Start nicht) und
# mit dem Rezept des HISTORIE-Abschnitts 'MESSUNG 2026-08-10' unten gebaut, UNVERAENDERT
# einschliesslich des Pruefling-Schalters (dasselbe Rezept wie im obersten Abschnitt);
# Registrierungs-Protokoll dieses Baums: 5 von 5 bedingten Bloecken AKTIV.
#     avx512f (gemessen, prod1)                                          -> 489
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 485
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 483
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 489 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Namensdiffs mit LC_ALL=C comm in BEIDE
# Richtungen: avx512f->avx2 verliert exakt die dokumentierten VIER, avx2->basis die ZWEI,
# Gegenrichtungen leer -- die Sprossenabstaende -4 und -2 stehen.
#
# WOHER +1 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff 4a89aed5..b91b22fc in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen): GENAU EINE neue
# Registrierung, test_s14_axis_version_lock_tripwire (S-14a Riegel, Paket P11, rohes
# add_test). Der Zugangs-Diff traegt 0 Treffer auf COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE
# (Diff-Probe gemessen); der Test steht in ALLEN drei Klassen-Namensmengen (je 1x gezaehlt).
# Eigener Commit, wie der Datei-Kopf es verlangt.
#
# -- HISTORIE: NACHMESSUNG 2026-08-13 (S-3-Abnahme) -- WIE 488/484/482 ENTSTANDEN ------
# GEMESSEN am 2026-08-13 in diesem Repo, Stand 4a89aed5 (S-3 Ordnungs-Relation, Branch
# bau/s3-ordnungsrelation, Worktree wt-ce-mess-ordnung), Messhost prod1, GCC 15.3.0, Ninja.
# Bau-Baum build-covguard KALT angelegt (existierte beim Start nicht) und mit dem Rezept des
# HISTORIE-Abschnitts unten gebaut, UNVERAENDERT einschliesslich des Pruefling-Schalters
# (configure.sh --with-generator=Ninja --enable-build-tests
#  -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"; make inventar;
#  ctest -N | sed 's/^ *Test *#[0-9][0-9]*: *//p' | LC_ALL=C sort -u | wc -l):
#     avx512f (gemessen, prod1)                                          -> 488
#     cmake -DCOMDARE_HOST_RUNS_AVX512F=0                                -> 484
#     cmake -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0     -> 482
# danach 'cmake -U' auf BEIDE Variablen; Wiederherstellung nachgewiesen: 488 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Namensdiffs mit LC_ALL=C comm in BEIDE
# Richtungen: avx512f->avx2 verliert GENAU die vier im HISTORIE-Abschnitt genannten,
# avx2->basis GENAU die zwei dort genannten, beide Gegenrichtungen LEER -- die
# Sprossenabstaende -4 und -2 sind unveraendert.
#
# WOHER +9 JE SPROSSE KOMMT (Konto, je Name; Registrierungs-Diff 6c010cdc..4a89aed5 in
# tests/unit/CMakeLists.txt, Gegenrichtung leer -- nichts entfallen):
#   +2  S-3 (dieser Stand): test_s3_ordnung_relation, test_s3_ordnung_freigabe
#   +7  fruehere Pakete seit der Sammellandung, deren Sprossen-Nachzug HIER fehlte:
#       test_anker_marke_statt_ledgerzeile, test_d37b_modus_riegel, test_pa1_tote_ausnahme,
#       test_vor_push_gate, test_vs_taxonomie_klassen_grep (comdare_add_test) sowie
#       test_bvset_teilmenge, test_s1_stempel_basis_vertrag (rohes add_test). Das ist
#       DERSELBE Versaeumnis-Typ, den der ca6d8af1-Absatz unten beschreibt: wer eine
#       Registrierung anlegt, muss diese Datei im selben Zug anfassen.
# KEINER der neun Zugaenge ist ISA- oder Pruefling-gegatet (Diff-Probe: 0 Treffer auf
# COMDARE_HOST_RUNS/COMDARE_CE_PRUEFLINGE; alle neun registrieren auf oberster Ebene;
# die gemessenen Klassen-Differenzen oben bestaetigen es). Der Pruefling-Beitrag bleibt
# +1 (test_pruefling_fixture_ladung); ein Baum OHNE den Schalter zaehlt entsprechend 487
# (KON55-Kombibau der S-3-Abnahme: 4x gemessen, gcc-15/clang-22 x Debug/Release, alle 487
# mit Byte-gleicher Namensmenge).
#
# -- HISTORIE: MESSUNG 2026-08-10 -- WIE 479/475/473 ENTSTANDEN (nachgemessen) -
# GEMESSEN am 2026-08-10 in diesem Repo, Stand 6c010cdc (die Sammellandung, auf der
# Pipeline 15536 fuhr), Worktree wt-ce-fk, Messhost prod1, GCC 15.3.0, Ninja. Der
# Bau-Baum build-covguard wurde dafuer KALT angelegt -- er existierte beim Start
# nicht -- und mit dem Rezept des Jobs test:coverage-guard gebaut, einschliesslich
# des Pruefling-Schalters, der diesen Baum von dem des Jobs test:unit unterscheidet:
#
#     CC=gcc-15 CXX=g++-15 sh ./configure.sh --with-generator=Ninja \
#         --enable-build-tests --build-dir=build-covguard \
#         -DCOMDARE_CE_PRUEFLINGE="$PWD/tests/pruefling_fixture"
#     make inventar BUILD_JOBS=4
#     ctest --test-dir build-covguard -N | sed -n 's/^ *Test *#[0-9][0-9]*: *//p' \
#         | LC_ALL=C sort -u | wc -l    ->    479
#
# Dass der Baum wirklich der des Jobs ist, haengt nicht an der Absicht: der Bau
# meldete 2172 Schritte, genau wie Job 371636. Ohne den Pruefling-Schalter sind es
# 2168 Schritte und 478 Tests -- der Block 'pruefling_slots_v1' steht dann als
# UEBERSPRUNGEN im Registrierungs-Protokoll. Wer hier ohne den Schalter misst, misst
# den falschen Baum; genau das ist beim Nachmessen am 2026-08-10 zuerst passiert.
#
# Dieser Messhost ist die REICHSTE Klasse: CMakeCache.txt meldet
# COMDARE_HOST_RUNS_AVX2=1 UND COMDARE_HOST_RUNS_AVX512F=1. 479 ist damit die
# Untergrenze fuer 'avx512f' und fuer sonst keine.
#
# FREMDER NENNER (T-3), damit die Zahl nicht nur aus der eigenen Messung stammt: der
# Job test:unit derselben Pipeline (371647, prod1, Stand 6c010cdc) hat seine Inventur
# als Artefakt veroeffentlicht -- build/Testing/ctest_unit_inventar.txt, 478 Namen.
# Sie ist mit der hiesigen Messung OHNE Pruefling-Schalter BYTE-GLEICH (diff leer).
# Zwei Baeume, zwei Bau-Wege, zwei Maschinenlaeufe, dieselbe Namensmenge.
#
# DIE BEIDEN ANDEREN SPROSSEN SIND NICHT GERECHNET, SONDERN VON CMAKE ERZEUGT
# (Gegenorakel, V-7: die Zahl kommt aus einer anderen Quelle als der Rechnung).
# Verfahren: denselben Baum mit erzwungener Klasse neu KONFIGURIEREN (kein Neubau)
# und die Namensmengen vergleichen --
#     cmake -S . -B build-covguard -DCOMDARE_HOST_RUNS_AVX512F=0                  -> 475
#     cmake -S . -B build-covguard -DCOMDARE_HOST_RUNS_AVX2=0 -DCOMDARE_HOST_RUNS_AVX512F=0 -> 473
# und danach 'cmake -U COMDARE_HOST_RUNS_AVX2 -U COMDARE_HOST_RUNS_AVX512F', damit
# die Probe wieder echt laeuft. Die Wiederherstellung ist nachgewiesen: 479 Tests,
# Namensmenge Byte-gleich zur Erstmessung. Die Differenzen sind NAMENTLICH belegt,
# in beide Richtungen (die Gegenrichtung war jeweils LEER -- es kommt nichts hinzu).
# ALLE Vergleiche mit LC_ALL=C, auch die von 'comm': ein comm ohne C-Kollation haelt
# eine C-sortierte Datei fuer unsortiert und darf dann alles melden -- am 2026-08-10
# genau so passiert und deshalb wiederholt worden.
#
#   avx512f -> avx2  verliert genau diese VIER:
#       test_ap5_simd_extension_coherence      (Block an AVX2 UND AVX512F, tests/unit/CMakeLists.txt:4236)
#       test_buildvariant_dll_real             (Block an AVX2 UND AVX512F, tests/unit/CMakeLists.txt:4236)
#       test_simd_add_u64_carry_avx512         (do_run an AVX512F, tests/unit/CMakeLists.txt:5446)
#       test_simd_field_sum_dispatch_avx512    (do_run an AVX512F, tests/unit/CMakeLists.txt:5414)
#
#   avx2 -> basis    verliert zusaetzlich diese ZWEI:
#       test_simd_add_u64_carry_avx2           (do_run an AVX2, tests/unit/CMakeLists.txt:5443)
#       test_simd_field_sum_dispatch_avx2      (do_run an AVX2, tests/unit/CMakeLists.txt:5401)
#
# Mehr ISA-gattierte Registrierungen gibt es nicht: 'COMDARE_HOST_RUNS' kommt in
# tests/unit/CMakeLists.txt an genau ZEHN Stellen vor --
#   VIER Proben-Definitionen  4200, 4201, 5363, 5364  (dieselben zwei
#       check_cxx_source_runs-Aufrufe stehen ZWEIMAL in der Datei; der zweite ist
#       fuer den unteren Abschnitt da und ueberschreibt nichts: das Modul steigt bei
#       'if(NOT DEFINED "${_var}")' aus, Modules/Internal/CheckSourceRuns.cmake:7)
#   EINE Block-Bedingung      4236  (if(AVX2 AND AVX512F) -> die zwei add_test in
#       4237 und 4251)
#   VIER do_run-Argumente     5401, 5414, 5443, 5446
#   EIN Kommentar             6232  (bei test_mt_l4_registrierungs_wache_isa; registriert nichts)
# Bis zur Sammellandung waren es NEUN; der zehnte Fundort ist der Kommentar bei 6232,
# der mit dem mt-l4-Paket hinzukam. Die REGISTRIERENDEN Stellen sind unveraendert --
# deshalb sind die Sprossenabstaende -4 und -2 dieselben geblieben.
# Sonst REGISTRIERT niemand im Repo hinter diesen Variablen; 'grep -rl' ueber alle
# CMakeLists.txt und *.cmake ausserhalb von ext/ liefert genau diese eine Datei. Der
# Name kommt zwar noch woanders vor -- scripts/ci_test_coverage_guard.sh (liest ihn,
# 4 Fundstellen), scripts/ci_test_sichtbarkeit_wache.sh:59,328 und
# tests/unit/test_ap5_simd_extension_coherence.cpp:10 (beide nur im Kommentar) --,
# aber keine davon legt einen ctest-Eintrag an. Die frueher hier stehende Formel
# "sonst nirgends im Repo" war deshalb zu weit gegriffen.
#
# -- WARUM DIE ZAHL GEFALLEN IST, UND WARUM DAS KEIN SCHWUND IST --------------
# Die Sammellandung 6c010cdc hat die Inventur von 496 auf 479 gesenkt. Die Wache hat
# das in Pipeline 15536 (Job 371636, prod1) als NENNER-BEFUND gemeldet -- literal
# "UNTERSCHRITTEN um 17 Test(e)", Exit 4 -- und damit genau getan, wofuer sie da ist.
# Die 17 sind vollstaendig aufgeklaert; jeder Posten ist gemessen, keiner gerechnet:
#
#   496   Stand 83a6d443, prod1, Klasse avx512f, Job-Rezept OHNE Pruefling-Schalter.
#         Den gab es zu diesem Stand im Job noch nicht:
#         'git show 83a6d443:.gitlab-ci.yml | grep -c COMDARE_CE_PRUEFLINGE' -> 0.
#   -28   ca6d8af1 "D2-G1: die zwei gtest-Binaries unter libs/ nach dem Repo-Muster
#         registrieren". gtest_discover_tests legte je gtest-FALL einen ctest-Eintrag
#         an, add_test legt je BINARY einen an. Am Objekt gezaehlt (2026-08-10, die
#         gebauten Binaries mit '--gtest_list_tests'): test_commands 23 Faelle,
#         test_engine_adapters 7, zusammen 30 -- jetzt 2 Eintraege.
#         KEIN Testfall ist verschwunden: gtest_main faehrt beim Start der Binary
#         weiterhin alle 30. In 15536 sind beide Eintraege gruen gelaufen
#         (Job 371647, Test #1 und #2, "Passed").
#   +11   neue Test-Ziele der Landung: test_d2g1_ctest_registrierung,
#         test_d3_6_realm_selektor_produktionsablage, test_hyg_kumulativer_bereich,
#         test_mt_l3_wide_schema_orakel, test_mt_l4_registrierungs_wache_isa,
#         test_t6_biss_achsen_roundtrip_wache, test_t6_biss_registrierungs_wache,
#         test_t6_biss_sichtbarkeits_wache, test_t6_wachen_inventar,
#         test_w0b_durchstich_kette und test_pruefling_fixture_ladung. Das letzte
#         zaehlt nur, weil DIESELBE Landung dem Job den Schalter
#         -DCOMDARE_CE_PRUEFLINGE mitgibt (8a8f6877).
#   = 479 Stand 6c010cdc, prod1, Klasse avx512f, Job-Rezept MIT Schalter.
#
# WAS DER COVGUARD-JOB NICHT WAR: zu klein gebaut. Sein Registrierungs-Protokoll in
# 15536 meldet 5 von 5 bedingten Bloecken GELAUFEN, 0 uebersprungen -- alle vier
# 2-Pass-Tests sind drin. Der Nenner war vollstaendig; alt war nur die Untergrenze.
#
# WAS DARAUS FOLGT UND WAS NICHT: die alten drei Sprossen waren NICHT falsch, sie
# waren UEBERHOLT. 492 ist am 2026-08-10 auf prod2 sogar unabhaengig bestaetigt
# worden (Pipeline 15517, Job 371160: "LIVE-INVENTUR (ctest -N): 492", Klasse avx2).
# Sie sind hier gesenkt worden, WEIL der Grund gemessen ist -- nicht, damit die Wache
# gruen wird. Waere der Grund unklar geblieben, waere die Senkung unzulaessig; der
# Kopf dieser Datei sagt es selbst, und er gilt auch fuer den, der ihn gerade liest.
#
# -- WIDERRUF: WAS DIE VORIGE FASSUNG DIESES ABSATZES BEHAUPTETE ---------------
# Sie nannte fuenf Zeilenanker (4134, 5249, 5262, 5291, 5294) und die Aufteilung
# "zwei Proben-Definitionen, eine Block-Bedingung, sechs do_run-Argumente". BEIDES
# WAR FALSCH, und der Beleg dafuer ist so einfach wie der fuer die Korrektur:
#   * Die fuenf Anker loesen am heutigen Stand auf Kommentare und set()-Zeilen auf.
#     Sie loesen sauber auf in 78fcc627 -- dem Stand VOR dem Merge b7d82d73. Die
#     Namens-Analyse ist also am Vor-Merge-Stand gefahren und danach ohne erneuten
#     Blick uebernommen worden.
#   * Damit waren auch die ZAHLEN nicht gemessen, sondern fortgeschrieben: 492 und
#     490 sind die alten 488 und 486 plus die 4, die der Merge zur avx512f-Sprosse
#     hinzufuegte. Genau das schliesst der Absatz drei Zeilen weiter oben fuer sich
#     aus ("NICHT GERECHNET, SONDERN VON CMAKE ERZEUGT").
#   * Die drei Zahlen waren trotzdem RICHTIG -- fuer IHREN Stand. Sie standen nicht
#     mehr deshalb da, sondern weil sie am 2026-08-10 am Stand 83a6d443 neu erhoben
#     wurden, mit den zwei Namensmengen und beiden Gegenrichtungen. Der Merge hat
#     keine ISA-gattierte Registrierung hinzugefuegt; die Abstaende -4 und -2 sind
#     dieselben geblieben.
# DIESER ABSATZ IST HISTORIE: er beschreibt 496/492/490 am Stand 83a6d443. Danach galten
# seit der Sammellandung 479/475/473 (s. HISTORIE-Abschnitt darueber), ab der S-3-Abnahme
# 2026-08-13 die Werte 488/484/482, ab der Wellen-Landung P1+P11 (gleicher Tag, Stand
# b91b22fc) die Werte 489/485/483; gueltig sind seit der S-7-Abnahme (gleicher Tag, Basis
# 5f3f26a5 + 2 Stempel-Tests) die Werte 491/487/485, s. den obersten Abschnitt.
#
# NEBENBEI, damit niemand hineinlaeuft: das Erzwingen oben ist ein Verfahren fuer
# 'ctest -N', NICHT fuer die Wache. Auf einem so erzwungenen Baum bricht
# scripts/ci_test_coverage_guard.sh seit 2026-08-10 mit Exit 2 ab -- eine
# erzwungene Host-Klasse ist keine gemessene, und sie als solche zu nehmen war der
# Defekt, den diese Nachbesserung geschlossen hat. Erst 'cmake -U ...' macht den
# Baum wieder messbar.
#
# EINE VIERTE KLASSE GIBT ES NICHT: 'AVX-512F ohne AVX2' existiert auf keiner
# x86-Maschine. Stuende es im Cache, waere nicht die Klasse ungewoehnlich, sondern
# der Cache unglaubwuerdig -- die Wache bricht dort ab.
#
# -- HISTORIE: DIE SPROSSEN 496 / 492 / 490 (DEPRECATED 2026-08-10) -----------
# Sie galten vom 2026-08-10 (Stand 83a6d443) bis zur Sammellandung 6c010cdc desselben
# Tages. Sie sind NICHT geloescht, weil sie kein Irrtum waren, sondern ein
# ueberholter Messstand -- und weil man an ihnen sieht, wie schnell eine Untergrenze
# ihren Gegenstand verliert: zwischen Messung und Ueberholung lagen Stunden, nicht
# Wochen. Was sie ueberholt hat, steht oben unter "WARUM DIE ZAHL GEFALLEN IST".
# 492 ist unabhaengig belegt (15517/371160, prod2, Klasse avx2) und war damit die
# einzige der drei, die je eine echte Maschine ihrer Klasse gesehen hat.
#
# WAS DARAN ZU LERNEN IST -- und was der Grund fuer den Abschnitt hier ist: ca6d8af1
# hat die Zahl im eigenen Commit-Bogen ausdruecklich genannt ("ctest -N, beide Zahlen
# mit Host: prod1 vorher 496, nachher 469") und die Untergrenze trotzdem nicht
# nachgezogen. Der Paket-Stand hatte auch nie eine eigene Pipeline
# ('/pipelines?sha=ca6d8af1' -> keine); die Wache lief also erstmals in der
# Sammellandung ueber diese Aenderung. Wer eine Registrierung umbaut, aendert den
# Gegenstand DIESER Datei -- und muss sie im selben Zug anfassen.
#
# -- HISTORIE: DIE ALTE ZAHL 488 UND WARUM SIE ZU HOCH WAR ---------------------
# BIS 2026-08-10 stand hier GENAU EINE Zahl, 488, mit dem Anspruch, sie gelte
# "auf JEDER Hardware-Klasse". Sie ist NICHT geloescht, sondern hier vermerkt,
# weil ihr Irrtum die Bauform erklaert:
#
#   * Sie entstand als 492 - 4 am Stand aebc4f2c. Der Abzug 4 war die richtige
#     Zahl fuer den Schritt avx512f -> avx2, wurde aber als Abzug fuer JEDE
#     Klasse verstanden. Fuer eine Maschine ganz ohne AVX2 sind es SECHS.
#     Auf einer 'basis'-Maschine haette der damalige Baum 486 Tests gehabt und
#     die Wache haette 486 < 488 gemeldet -- ROT an einem gesunden Baum.
#   * Ihre Namensliste war ausserdem falsch besetzt: sie nannte
#     test_simd_field_sum_dispatch_avx2 (das faellt erst bei 'basis') und liess
#     test_simd_add_u64_carry_avx512 aus (das faellt schon bei 'avx2'). Die
#     Anzahl stimmte zufaellig, die Menge nicht.
#   * WAS SIE NICHT WAR: die Ursache der roten ce-Pipeline 15515 vom 2026-08-10.
#     Der dortige prod2-Lauf hatte 488 Tests in seiner eigenen unfilterten
#     Inventur ('D1c: Inventur veroeffentlicht -- 488 Tests') und damit GENAU die
#     Untergrenze, nicht weniger. Die 486 aus der ctest-Summenzeile jenes Jobs ist
#     die mit '-LE pmc' GEFILTERTE Laufzahl (488 - 2 pmc-Tests) und wird mit der
#     Untergrenze nie verglichen. Rot war dort ein Umgebungs-Leck im Selbsttest.
#
# -- WAS DIESE ZAHLEN NICHT LEISTEN (ehrlich benannt) -------------------------
# Innerhalb EINER Klasse ist die Untergrenze jetzt exakt und nicht mehr um vier
# Tests locker -- der alte Schlupf ("zwischen 488 und 492 kann auf diesem Host
# einiges verschwinden") ist damit zu. Was sie weiterhin nicht sehen: einen
# Schwund, der zeitgleich mit einem ehrlichen Zuwachs kommt und ihn genau
# ausgleicht. Dagegen stehen das Registrierungs-Protokoll (Befund (c)/(d),
# Exit 4) und die Fremd-Inventur (Befund (e), COMDARE_FREMD_INVENTUR); die
# Untergrenze ist die GROBE Achse gegen Schwund im Grossen und ersetzt die
# beiden feinen nicht.
#
# FORMAT: '#'-Kommentare und Leerzeilen sind erlaubt. Wertzeilen haben die Form
# '<klasse> <ganzzahl>'. Verlangt sind ALLE DREI Klassen, jede GENAU EINMAL.
# Eine nackte Zahl ohne Klasse (die alte Bauform) ist ein Abbruch, keine Wahl.
avx512f 491
avx2 487
basis 485
