# =============================================================================
#  ALLOWLIST der TEST-REGISTRIERUNGS-WACHE (MT-L4, 2026-08-09)
#  gelesen von scripts/ci_test_registrierungs_wache.sh
# =============================================================================
#
# HIER STEHT NUR, WAS BEGRUENDET AUSSERHALB DES BAUWEGS LIEGEN DARF.
# Nicht jede Test-Quelldatei muss in jedem Baum uebersetzt werden -- ein Test gegen
# einen optionalen Fremdbaum ist ohne diesen Baum legitim abwesend. Was NICHT legitim
# ist: dass diese Abwesenheit unbemerkt bleibt.
#
# FORMAT je Zeile, drei Felder, '|'-getrennt:
#
#   <test-quelldatei> | <art>:<gegenstand-der-fehlen-muss> | <begruendung>
#
# Feld 2 ist der Kern und der Grund, warum diese Liste kein Freibrief ist: es nennt
# einen GEGENSTAND, dessen ABWESENHEIT die Ausnahme traegt. Die Wache prueft ihn bei
# jedem Lauf nach. Ist der Gegenstand wieder da, ist die Ausnahme ERLOSCHEN und die
# Zeile wird ROT -- ohne dass jemand diese Liste anfassen muss.
# Eine Allowlist ohne diese Gegenprobe waere eine Freistellung mit unbegrenzter
# Laufzeit; genau daran sind die vier unregistrierten Dateien 14 bzw. 68 Tage lang
# nicht aufgefallen.
#
# ZWEI ARTEN VON GEGENSTAND, und das PRAEFIX benennt die Art (2026-08-10):
#
#   datei:<pfad>                Die Ausnahme traegt, solange der Pfad NICHT existiert.
#                               Geprueft mit '[ -e ]' im Arbeitsbaum.
#   isa:<merkmal>[+<merkmal>]   Die Ausnahme traegt, solange MINDESTENS EIN Merkmal auf
#                               dem Bau-Host fehlt. Bekannt sind genau zwei: avx2 und
#                               avx512f. Geprueft an COMDARE_HOST_RUNS_<GROSS> im
#                               CMakeCache.txt DES GEMESSENEN BAUMS -- also an dem, was
#                               der Bau vorgefunden hat, nicht an /proc/cpuinfo der
#                               Maschine, auf der die Wache gerade laeuft.
#
# WARUM ES DIE ZWEITE ART GIBT (Pipeline 15517, ce/development ROT): Feld 2 kannte bis
# dahin nur einen Pfad. Der erste echte Fang der Wache haengt aber an keiner Datei,
# sondern an einem CMake-Gatter ueber der HOST-ISA -- es gibt schlicht keine Datei,
# deren Abwesenheit man dafuer nennen koennte. Die Wache hat richtig gemeldet; das
# Format konnte die Begruendung nicht ausdruecken.
#
# WAS NICHT MEHR GEHT: Feld 2 leer lassen, oder einen Pfad ohne 'datei:' schreiben.
# Beides ist ab jetzt UNPRUEFBAR und damit ROT. Eine Begruendung, die die Wache nicht
# nachpruefen kann, ist keine Begruendung, sondern genau der Freibrief, gegen den diese
# Liste gebaut ist. Ein unbekanntes Praefix und ein unbekanntes ISA-Merkmal sind aus
# demselben Grund ROT und werden nicht geraten.
#
# 'isa:' IST EINE UND-VERKNUEPFUNG, weil das CMake-Gatter eine ist: der Test wird genau
# dann registriert, wenn ALLE genannten Merkmale da sind. Legitim abwesend ist er also
# genau dann, wenn MINDESTENS EINES fehlt. Sind alle da und die Datei fehlt trotzdem im
# Bauweg, ist die Ausnahme ERLOSCHEN -- das ist der Fall, den ein Host mit AVX-512
# erzeugt, wenn jemand die Registrierung entfernt.
#
# -----------------------------------------------------------------------------
# prt-art-Legacy-Baum: vier Tests haengen an if(COMDARE_PRT_ART_LEGACY_AVAILABLE)
# in tests/unit/CMakeLists.txt:116 bzw. :187. Diese Variable wird bei :96-99 aus
# EINEM Gegenstand gesetzt -- der Existenz von prt_art/include/prt_art/prt_art.hpp.
# Im Standalone-Bau von ce fehlt dieser Baum (gemessen 09.08.2026: das Verzeichnis
# prt_art/ existiert nicht), mit COMDARE_CE_PRUEFLINGE ist er da. Genau dieselbe
# Datei prueft die Wache nach: kommt der Legacy-Baum zurueck und die Tests bleiben
# trotzdem ungebaut, ist das ein Befund und kein Normalzustand.
# Dieselben vier stehen aus dem SPIEGELBILDLICHEN Grund in
# scripts/ci_test_sichtbarkeit_allowlist.txt: dort fehlt ihr ctest-Eintrag,
# hier ihre Uebersetzung. Zwei Stufen, ein Grund.
# -----------------------------------------------------------------------------
tests/unit/test_concepts_compile.cpp | datei:prt_art/include/prt_art/prt_art.hpp | Legacy-Concept-Test
tests/unit/test_value_handle.cpp | datei:prt_art/include/prt_art/prt_art.hpp | Legacy-Concept-Test
tests/unit/test_three_layer_audit.cpp | datei:prt_art/include/prt_art/prt_art.hpp | Legacy-Concept-Test
tests/unit/test_six_page_structures.cpp | datei:prt_art/include/prt_art/prt_art.hpp | Legacy-Seitentypen

# -----------------------------------------------------------------------------
# HOST-ISA-GATTER: EINE Test-QUELLDATEI haengt an der ISA des Bau-Hosts.
# tests/unit/CMakeLists.txt:4174 legt add_executable(test_ap5_simd_extension_coherence)
# INNERHALB von if(COMDARE_HOST_RUNS_AVX2 AND COMDARE_HOST_RUNS_AVX512F) (:4165) an.
# Auf einem Host ohne AVX-512 entsteht das Ziel nie und die Quelldatei wird nie
# uebersetzt. Das ist ein HARDWARE-GATTER und kein Defekt -- dieselbe Klasse steht aus
# dem spiegelbildlichen Grund in scripts/ci_test_sichtbarkeit_allowlist.txt:81 (dort
# fehlt der ctest-Eintrag, hier die Uebersetzung) und traegt die avx2-Sprosse in
# scripts/ci_test_inventory_floor.txt.
#
# ANLASS: ce-Pipeline 15517, Job test:coverage-guard -- "1 von 459 ohne Begruendung".
# Beide bare-metal-Runner tragen das Tag 'baremetal'; prod1 (AMD Zen 5) hat AVX-512,
# prod2 (Intel) nicht. Welcher den Job nimmt, entscheidet GitLab.
#
# T-6 SCHWESTERSUCHE, am Objekt gemessen (2026-08-10, dieser Worktree, build/):
# Dieselbe Frage fuer ALLE sechs ISA-gattierten Registrierungen. Verfahren: denselben
# Baum mit erzwungener Klasse neu KONFIGURIEREN und die Mengen der uebersetzten
# Test-Quelldateien vergleichen (459 SOLL aus 'git ls-files'):
#   avx512f (ehrlicher Cache) .......... 455 im Bauweg
#   -DCOMDARE_HOST_RUNS_AVX512F=0 ...... 454 im Bauweg -- verliert GENAU diese eine Datei
#   zusaetzlich -DCOMDARE_HOST_RUNS_AVX2=0  454 im Bauweg -- verliert KEINE weitere
#   Gegenrichtung beide Male LEER; nach 'cmake -U ...' wieder 455, Menge byte-gleich.
# Die anderen fuenf Registrierungen gattern nur ihr add_test(), nicht ihr
# add_executable() -- ihre Quelldateien werden auf jeder Klasse uebersetzt und koennen
# dieser Wache deshalb nie fehlen. Fuer die Stufe 'Quelldatei -> Bauweg' hat diese
# Klasse also GENAU EIN Mitglied, nicht sechs.
# -----------------------------------------------------------------------------
tests/unit/test_ap5_simd_extension_coherence.cpp | isa:avx2+avx512f | GO-3-A1-Kohaerenzbeweis hinter dem Host-ISA-Gatter
