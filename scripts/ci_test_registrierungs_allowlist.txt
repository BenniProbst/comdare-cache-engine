# =============================================================================
#  ALLOWLIST der TEST-REGISTRIERUNGS-WACHE (MT-L4, 2026-08-09)
#  gelesen von scripts/ci_test_registrierungs_wache.sh
# =============================================================================
#
# HIER STEHT NUR, WAS BEGRUENDET AUSSERHALB DES BAUWEGS LIEGEN DARF.
# Nicht jede Test-Quelldatei muss in jedem Baum uebersetzt werden -- ein Test gegen
# einen optionalen Fremdbaum ist ohne diesen Baum legitim abwesend. Was NICHT legitim
# ist: dass diese Abwesenheit unbemerkt bleibt.
#
# FORMAT je Zeile, drei Felder, '|'-getrennt:
#
#   <test-quelldatei> | <art>:<gegenstand-der-fehlen-muss> | <begruendung>
#
# Feld 2 ist der Kern und der Grund, warum diese Liste kein Freibrief ist: es nennt
# einen GEGENSTAND, dessen ABWESENHEIT die Ausnahme traegt. Die Wache prueft ihn bei
# jedem Lauf nach. Ist der Gegenstand wieder da, ist die Ausnahme ERLOSCHEN und die
# Zeile wird ROT -- ohne dass jemand diese Liste anfassen muss.
# Eine Allowlist ohne diese Gegenprobe waere eine Freistellung mit unbegrenzter
# Laufzeit; genau daran sind die vier unregistrierten Dateien 14 bzw. 68 Tage lang
# nicht aufgefallen.
#
# DREI ARTEN VON GEGENSTAND, und das PRAEFIX benennt die Art (2026-08-10):
#
#   datei:<pfad>                Die Ausnahme traegt, solange der Pfad NICHT existiert und
#                               nicht belegt ist. Geprueft mit '[ -e ] || [ -L ]' im
#                               Arbeitsbaum (auch ein Symlink ohne Ziel belegt den Pfad;
#                               Fix-r6 LA6-06, Wortlaut Fix-r7 LC5W-08) -- UND der Pfad muss
#                               ueberhaupt entstehen koennen (s. TOTE AUSNAHME unten).
#   isa:<merkmal>[+<merkmal>]   Die Ausnahme traegt, solange MINDESTENS EIN Merkmal auf
#                               dem Bau-Host fehlt. Bekannt sind genau zwei: avx2 und
#                               avx512f. Geprueft an COMDARE_HOST_RUNS_<GROSS> im
#                               CMakeCache.txt DES GEMESSENEN BAUMS -- also an dem, was
#                               der Bau vorgefunden hat, nicht an /proc/cpuinfo der
#                               Maschine, auf der die Wache gerade laeuft.
#   frist:<JJJJ-MM-TT>          Die Ausnahme traegt bis zu diesem Tag. Danach ROT. Fuer
#                               den Fall, dass es KEINEN erreichbaren Gegenstand gibt --
#                               ein geparkter Posten mit Ablaufdatum statt einer Adresse,
#                               die niemand je belegen kann. Sie ist die SCHWAECHSTE Form:
#                               wer sie benutzt, muss in Feld 3 sagen, wem der Posten
#                               gehoert und woran er haengt.
#
# FORMREGELN JE ZEILE (2026-09-18, Fix-r2 des OV-2-Zuges, Lens C LCW-08/LCW-09; Fix-r3 LC3W-05/LC3W-07):
# Feld 3 darf nicht leer sein -- eine Ausnahme ohne Begruendungstext ist keine und ist UNPRUEFBAR
# (rot); je Test-Quelldatei steht genau EINE Zeile -- die Wache laese nur die erste, jede weitere
# schliefe; ein mehrfach genannter Pfad ist UNPRUEFBAR (rot). Eine Zeile nur aus Leerraum gilt als
# Leerzeile; ein Kommentar beginnt in Spalte 1 (eingerueckt ist er eine Datenzeile ohne Gegenstand,
# rot). Die letzte Zeile darf ohne Zeilenumbruch enden -- sie wird trotzdem gelesen. Ein 'isa:'-
# Ausdruck hat kein leeres Teilmerkmal: '+avx2', 'avx2+' und 'avx2++avx512f' sind UNPRUEFBAR (rot),
# nicht 'avx2'. DIE FORM GILT FUER JEDE DATENZEILE, auch fuer eine Datei, die IM Bauweg steht: eine
# solche Zeile ist stumm (sie wird nie ausgewertet -- die 'isa:'-Zeile unten ist es auf einem AVX-512-
# Host), aber ein Formfehler darin (leeres Feld 3, unbekannte Art, leeres oder unbekanntes Merkmal,
# kein Datum) ist UNPRUEFBAR (rot) und steht im Nenner als eigene Klasse ("mit Formfehler fuer Dateien
# im Bauweg"); bis Fix-r3 lag er in Reserve, bis die Datei einmal fehlte.
#
# WARUM ES DIE ZWEITE ART GIBT (Pipeline 15517, ce/development ROT): Feld 2 kannte bis
# dahin nur einen Pfad. Der erste echte Fang der Wache haengt aber an keiner Datei,
# sondern an einem CMake-Gatter ueber der HOST-ISA -- es gibt schlicht keine Datei,
# deren Abwesenheit man dafuer nennen koennte. Die Wache hat richtig gemeldet; das
# Format konnte die Begruendung nicht ausdruecken.
#
# WAS NICHT MEHR GEHT: Feld 2 leer lassen, oder einen Pfad ohne 'datei:' schreiben.
# Beides ist ab jetzt UNPRUEFBAR und damit ROT. Eine Begruendung, die die Wache nicht
# nachpruefen kann, ist keine Begruendung, sondern genau der Freibrief, gegen den diese
# Liste gebaut ist. Ein unbekanntes Praefix und ein unbekanntes ISA-Merkmal sind aus
# demselben Grund ROT und werden nicht geraten.
#
# 'isa:' IST EINE UND-VERKNUEPFUNG, weil das CMake-Gatter eine ist: der Test wird genau
# dann registriert, wenn ALLE genannten Merkmale da sind. Legitim abwesend ist er also
# genau dann, wenn MINDESTENS EINES fehlt. Sind alle da und die Datei fehlt trotzdem im
# Bauweg, ist die Ausnahme ERLOSCHEN -- das ist der Fall, den ein Host mit AVX-512
# erzeugt, wenn jemand die Registrierung entfernt.
#
# TOTE AUSNAHME -- die ZWEITE RICHTUNG von ERLOSCHEN (PA-1, 2026-08-10):
# Bis heute fragte die Wache nur "existiert der Gegenstand WIEDER?". Damit faengt sie den
# Fall, dass er zurueckkehrt -- aber nicht den, dass er NIE zurueckkehren kann. Der zweite
# Fall lag genau hier vor, in allen vier prt-art-Zeilen (Einzelheiten im Block unten).
# Eine 'datei:'-Zeile ist ab jetzt auch dann ROT, wenn KEINE Quelle dieses Repos den ZWEIG
# kennt, in dem der Pfad liegt: weder der Git-Index (getrackte Datei oder Submodul-Gitlink)
# noch eine .gitignore-Regel, und das vom Elternverzeichnis bis hinauf zur Wurzel. Dann hat
# der Gegenstand in diesem Repo keinen Erzeuger, die Zeile koennte per Konstruktion nie
# erloeschen -- und eine Ausnahme, die nie erlischt, ist die Freistellung mit unbegrenzter
# Laufzeit, gegen die diese Liste gebaut wurde.
# ERREICHBAR ist dagegen: ein Pfad unter einem ignorierten Verzeichnis (Bauprodukt), ein
# Pfad unter einem Submodul-Gitlink (auch beliebig tief -- der Inhalt eines nicht
# ausgecheckten Submoduls steht nie im Index), und eine neue Datei in einem vorhandenen
# Verzeichnis. Alle drei sind mit Koeder belegt (tests/unit/test_pa1_tote_ausnahme.cpp).
# WAS DAS NICHT BEWEIST, ausdruecklich: nicht, dass der Gegenstand nie existieren KANN.
# Jemand kann ihn morgen 'git add'en, ein Pruefling-Repo kann eingehaengt werden, ein
# CI-Job kann ihn hineinkopieren. Gemessen ist nur, dass ihn HEUTE keine Quelle dieses
# Repos nennt. Ein Pfad AUSSERHALB des Repos (absolut, oder mit '..'-Segmenten, die ueber
# die Repo-Wurzel HINAUSFUEHREN -- Tiefenzaehler, Fix-r7 LA7-02) wird gar nicht beurteilt
# und nie als tot gemeldet; die Wache weist ihre Zahl im Nenner aus. Ein '..', das
# INNERHALB des Repos aufloest ('ext/x/../y'), ist dagegen eine nicht kanonische
# Schreibweise und UNPRUEFBAR (rot), s. u.
#
# WAS EIN VORFAHR SEIN MUSS, DAMIT ER TRAEGT (2026-09-19, Fix-r5 des OV-2-Zuges; Lens A r5 LA5-01/02/03/06,
# Lens B r4 LB4-01/03; Wache, Kopf Folgen (11) und (12)): 'vorhandenes Verzeichnis' heisst ein Verzeichnis
# MIT getracktem Inhalt. Eine getrackte DATEI als Vorfahr ('ext/README.md/x.hpp') traegt NICHT -- unter einer
# Datei kann nie ein Kind entstehen, die Zeile ist TOT (rot; bis Fix-r5 galt sie als erreichbar). Ein SYMLINK
# als Vorfahr und ein Pfad, der im Index im Merge-Konflikt mit ungleichen Typen je Stufe steht (Datei gegen
# Gitlink), sind UNPRUEFBAR (rot): die Wache loest keinen Link auf und raet keinen Merge-Ausgang; ein Gitlink
# zaehlt dagegen auf jeder Index-Stufe. DIE FORM DES PFADS: ein 'datei:'-Pfad ist kanonisch und repo-relativ
# geschrieben -- kein './', kein '//', kein '/' am Ende, kein Segment '.' -- und enthaelt kein Zeichen, das
# git in seiner Ausgabe quotiert (Tabulator, Steuerzeichen, Anfuehrungszeichen, Backslash); sonst ist die
# Zeile UNPRUEFBAR (rot), auch als stumme Zeile einer Datei im Bauweg. Leerzeichen und Nicht-ASCII-Zeichen
# im Pfad sind erlaubt (die Wache liest den Index mit core.quotePath=false) -- diese Datei selbst bleibt
# nach Hausregel ASCII. Alle Klassen sind mit Koeder belegt (Faelle (27d-f), (28), (29)).
# [Ergaenzt 2026-09-19, Fix-r6 des OV-2-Zuges; Lens A r6 LA6-01/02/03/06, Lens C r4 LC3W-15..18; Wache, Kopf
# Folge (13)]: UNPRUEFBAR (rot) ist auch ein Vorfahr, der im Index im Merge-Konflikt als EINTRAG GEGEN EIN
# VERZEICHNIS darunter steht (D/F: Gitlink oder Datei auf einer Stufe, Eintraege darunter auf einer anderen) --
# die Meldung nennt die Stufen und Typen, nicht mehr fest 'Datei gegen Gitlink' --, und ein Vorfahr, der NUR im
# Arbeitsbaum ein Symlink ist (nicht im Index). Die Formregel fuer den 'datei:'-Pfad gilt nur INNERHALB des Repos:
# ein absoluter Pfad und ein Pfad, der per '..' ueber die Wurzel hinausfuehrt, werden wie bisher gar nicht beurteilt
# (Nenner-Zeile 'AUSSERHALB', s. o.); ein '..', das innerhalb des Repos aufloest, ist seit Fix-r7 UNPRUEFBAR (LA7-02,
# Faelle (5b)-(5e); '(5d)' bis 89cf7103, berichtigt mit Fix-r8, Lens A r8 LA8-06). Ein Gegenstand, der als Symlink
# ohne Ziel vorliegt, ist ERLOSCHEN (der Pfad ist belegt).
# Nicht-ASCII-Pfade zaehlen seit Fix-r6 auch im SOLL und am VERMERK.md-Anker (die Wache liest beide roh; bis
# 9223cbd5 fielen sie dort STILL heraus). Faelle (6b), (27e2/e3/f2), (27g)-(27g3), (27h), (28f), (31).]
#
# -----------------------------------------------------------------------------
# prt-art-Legacy: VIER GEPARKTE POSTEN. Frueher stand hier
# 'datei:prt_art/include/prt_art/prt_art.hpp' -- eine Adresse, die NIEMAND je belegen
# kann. Sie war damit die Freistellung mit unbegrenzter Laufzeit, vor der der Kopf
# dieser Datei warnt, und zwar in genau der Liste, die das verhindern sollte.
#
# WAS AM OBJEKT GEMESSEN WURDE (2026-08-10, dieser Worktree):
#   prt_art.hpp .................. 0 Treffer unter /home/comdare (find -name)
#   Gegenprobe cache_engine.hpp .. 45 Treffer -- das Werkzeug sucht, der Nullbefund gilt
#   in ce je getrackt ............ JA, aber nur vom 2026-05-12 (5c210581) bis zum
#                                  2026-05-14: 'git mv' schob alle 44 Pfade nach
#                                  libs/deprecated/prt_art_legacy/ (b9abc720, R100 --
#                                  KEINE Datei wurde dabei geloescht), und dieser Baum
#                                  fiel am 2026-06-01 komplett (6b3ed0d9).
#   im Nachbarrepo comdare-prt-art  NEIN -- dort war der Pfad NIE getrackt
#                                  (git log --all --diff-filter=A: leer).
#
# UND ES GIBT AUCH KEINEN ERSATZ-GEGENSTAND. Die vier Dateien inkludieren
# prt_art/prt_art.hpp, prt_art/concepts/*.hpp, prt_art/page_structures/*.hpp und
# prt_art/interpreters/*.hpp. Im Nachbarrepo existiert KEINER dieser Header -- die
# Verzeichnisse concepts/, page_structures/ und interpreters/ gibt es dort gar nicht
# (gemessen: 38 .hpp unter prt_art/include/prt_art/, alle in anderen Unterordnern).
# Es gibt also keine Datei auf dieser Maschine, deren Auftauchen "der Legacy-Baum ist
# zurueck" bedeuten wuerde. Die Praemisse "optionaler Fremdbaum, der wiederkommen kann"
# ist am Objekt widerlegt; ein cleverer zweiter Pfad waere nur die naechste Fiktion.
#
# DESHALB 'frist:' UND NICHT 'datei:'. Der Posten ist ehrlich unbeweisbar, also traegt
# er ein Ablaufdatum statt einer Adresse. Er gehoert dem Owner (Ledger PA-3: neu
# verdrahten / archivieren / loeschen) und ist seit 8a8f6877 auch im CMake sichtbar
# geparkt -- E-1 hat das unerfuellbare if(COMDARE_PRT_ART_LEGACY_AVAILABLE) dort
# entfernt und durch die Pruefling-Zusicherung ersetzt. Die vier .cpp bleiben liegen und
# sind damit ausserhalb jedes Bauwegs; genau das steht hier.
# Laeuft die Frist ab, ohne dass entschieden wurde, wird diese Wache ROT -- gewollt.
# Das ist der Unterschied zu vorher: der Posten kann nicht mehr lautlos alt werden.
#
# T-6 SCHWESTERPFLICHT, mit einer BERICHTIGUNG: frueher stand hier, dieselben vier
# stuenden "aus dem spiegelbildlichen Grund" auch in
# scripts/ci_test_sichtbarkeit_allowlist.txt. Das gilt seit dem 2026-08-10 NICHT MEHR,
# und zwar zu Recht -- am Objekt nachgezaehlt: dort stehen heute 2 wirksame Zeilen, KEINE
# davon ist eine dieser vier. Die beiden Stufen haben verschiedene SOLL-Quellen:
#   Stufe 1 (hier)   SOLL = git ls-files  -> die vier .cpp liegen weiter im Baum,
#                                            werden nie uebersetzt, brauchen also je
#                                            eine Zeile.
#   Stufe 2 (dort)   SOLL = die Registrierungs-AUFRUFE im CMake-Quelltext -> die sind
#                                            seit 8a8f6877 entfernt, die vier tauchen
#                                            dort gar nicht mehr auf. Eine Zeile waere
#                                            eine Ausnahme OHNE Anlass und faenge nur
#                                            das naechste echte Verschwinden desselben
#                                            Namens lautlos weg.
# Die Listen laufen hier also nicht auseinander, sie antworten auf verschiedene Fragen.
# Der Google Test test_pa1_tote_ausnahme, Fall (9), haelt genau das fest.
#
# W-B-NACHTRAG (2026-08-15, Vorlage-B2-GO "Aufraeumen (a)"): die vier Dateien sind per
# 'git mv' nach tests/deprecated/prt_art_legacy_waisen/ archiviert -- NICHT geloescht;
# Begruendung, Messungen und die drei aufgegebenen Deckungs-Luecken stehen dort in
# VERMERK.md.
# [UEBERHOLT ab dem naechsten Satz "Der SOLL dieser Wache ..." bis zum Ende dieses Nachtrags,
#  Frist-Saetze eingeschlossen -- OV-2-NACHTRAG unten (2026-09-17, Marker nachgezogen 2026-09-18,
#  vor den ueberholten Satz gestellt 2026-09-18 Fix-r2 F4, Lens B LB2-04 / Lens A LA3-09).]
# Der SOLL dieser Wache ist 'git ls-files' mit DATEINAMEN-Anker, die vier
# bleiben also im SOLL; ihre 'frist:'-Zeilen tragen deshalb weiter, nur mit dem neuen
# Pfad in Feld 1 (tests/unit/test_pa1_tote_ausnahme.cpp kGeparkt ist nachgezogen).
# Die Frist 2026-09-15 bleibt unveraendert: sie gehoert dem Owner und deckt jetzt die
# ENDGUELTIGE Form (Archiv bestaetigen und Wachen-SOLL-Frage klaeren ODER Loeschung mit
# eigenem Owner-GO) -- der Posten soll weiterhin nicht lautlos alt werden koennen.
#
# OV-2-NACHTRAG (2026-09-17, Owner-Order 206 verbatim: "OV-2 Archiv-Variante gilt und frist Zeilen
# entfernen"): die im W-B-Nachtrag offen gelassene Wachen-SOLL-Frage ist damit GEKLAERT, und die vier
# 'frist:2026-09-18'-Zeilen sind ENTFERNT -- sie standen bis zu dieser Fassung unmittelbar unter
# diesem Absatz. UEBERHOLT ist deshalb der W-B-Nachtrag ab "Der SOLL dieser Wache ist 'git ls-files'
# mit DATEINAMEN-Anker, die vier bleiben also im SOLL; ihre 'frist:'-Zeilen tragen deshalb weiter"
# bis zu seinem Ende, die Frist-Saetze eingeschlossen (die Frist hiess 2026-09-15 und seit 57757fe3,
# Owner 15.09. Frage 1, 2026-09-18; es gibt sie nicht mehr). Alles DAVOR am W-B-Nachtrag gilt fort
# (Archiv statt Loeschung, VERMERK.md, Messungen, die drei aufgegebenen Deckungs-Luecken).
# [Berichtigt 2026-09-18, Lens A LA-03: die vorige Fassung dieses Absatzes zitierte den ueberholten
# Satz mit dem Wortlaut aus tests/unit/test_pa1_tote_ausnahme.cpp:122 statt aus dieser Datei und
# liess die Frist-Saetze des W-B-Nachtrags ohne Marker stehen.]
# [Berichtigt 2026-09-19, Lens B r4 LB4-I2 (Fix-r5): die Zeilennummer ':122' im vorigen Klammersatz gilt
# fuer die Fassung fe692d47 und wandert mit jedem Kopf-NACHTRAG des Tests (am Stand c62cfc7e ist :122 eine
# Trennzeile). Gemeint ist ohne Zeilennummer: der W-B-Absatz ueber der Konstante kGeparkt in
# tests/unit/test_pa1_tote_ausnahme.cpp ("Der SOLL ist am DATEINAMEN verankert, die vier bleiben also im
# SOLL und in der Allowlist"), dort seit 2026-09-17 mit UEBERHOLT-Marker.]
#
# WAS AN IHRE STELLE TRITT -- ein ORT, keine Ausnahme: scripts/ci_test_registrierungs_wache.sh nimmt
# eine getrackte Test-Quelldatei unter tests/deprecated/<ordner>/ aus ihrem SOLL, wenn und nur wenn
# tests/deprecated/<ordner>/VERMERK.md im Git-Index liegt UND die Form eines Ankers hat (regulaeres
# Blob 100644/100755 auf Index-Stufe 0, dessen Objekt in der Objektdatenbank liegt und ein Blob IST,
# lesbar, mit mindestens einem Nicht-Leerraum-Zeichen; seit 2026-09-18, Lens-Funde r1/r2, Lens C
# LCW-06, Fix-r3 LC3W-06), und weist die Archiv-Menge bei jedem Lauf mit Zahl,
# Ordner-Zahl und Dateinamen aus (Nenner und Endzeile). Die vier Dateien brauchen deshalb hier keine
# Zeile mehr: eine Ausnahme OHNE ANLASS faenge nur das naechste echte Verschwinden desselben Namens
# lautlos weg -- dieselbe Begruendung wie bei der T-6-Schwesterliste oben.
#
# WAS DAMIT NICHT ENTSCHIEDEN IST: die LOESCHUNG der vier Dateien. Sie braucht weiter ein eigenes
# Owner-GO. Die Begruendung des Archivs steht in tests/deprecated/prt_art_legacy_waisen/VERMERK.md;
# faellt diese Datei aus dem INDEX (git rm, git mv -- massgeblich ist der Index, ein blosses rm im
# Arbeitsbaum aendert ihn nicht; Lens A LA-05), fallen die vier .cpp in den SOLL zurueck und die
# Wache wird ROT. Seit 2026-09-18 (Lens-Funde r1/r2) gilt zudem: ein VERMERK.md ohne Nicht-Leerraum-
# Inhalt, als Symlink oder Gitlink, oder im Merge-Konflikt (Index-Stufe 1-3) ankert NICHT
# (UNPRUEFBARER ANKER, rot); eine Allowlist-Zeile fuer einen Pfad unter tests/deprecated/ -- die vier
# ARCHIV-Dateien eingeschlossen, mit oder ohne Anker -- ist UNPRUEFBAR (rot): diese Liste darf sie nie
# tragen, der Archiv-Ort kennt nur den Anker (Lens C LCW-05). Ebenso rot: zwei Zeilen fuer denselben
# Pfad, ein leeres Feld 3, eine Zeile, deren Feld 1 keine Datei des SOLL-Bestands nennt, ein Formfehler
# in der stummen Zeile einer Datei im Bauweg, und ein VERMERK.md, dessen Index-Eintrag auf ein fehlendes
# Objekt oder auf kein Blob (Tree, Commit) zeigt (Fix-r3, Lens C LC3W-05/LC3W-06). Werkzeug-Ausfaelle
# der Wache (grep, git, wc, sort, uniq, date, mktemp) sind Exit 2, nie ein Befund (LC3W-01..04).
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# HOST-ISA-GATTER: EINE Test-QUELLDATEI haengt an der ISA des Bau-Hosts.
# tests/unit/CMakeLists.txt:4174 legt add_executable(test_ap5_simd_extension_coherence)
# INNERHALB von if(COMDARE_HOST_RUNS_AVX2 AND COMDARE_HOST_RUNS_AVX512F) (:4165) an.
# Auf einem Host ohne AVX-512 entsteht das Ziel nie und die Quelldatei wird nie
# uebersetzt. Das ist ein HARDWARE-GATTER und kein Defekt -- dieselbe Klasse steht aus
# dem spiegelbildlichen Grund in scripts/ci_test_sichtbarkeit_allowlist.txt:81 (dort
# fehlt der ctest-Eintrag, hier die Uebersetzung) und traegt die avx2-Sprosse in
# scripts/ci_test_inventory_floor.txt.
#
# ANLASS: ce-Pipeline 15517, Job test:coverage-guard -- "1 von 459 ohne Begruendung".
# Beide bare-metal-Runner tragen das Tag 'baremetal'; prod1 (AMD Zen 5) hat AVX-512,
# prod2 (Intel) nicht. Welcher den Job nimmt, entscheidet GitLab.
#
# T-6 SCHWESTERSUCHE, am Objekt gemessen (2026-08-10, dieser Worktree, build/):
# Dieselbe Frage fuer ALLE sechs ISA-gattierten Registrierungen. Verfahren: denselben
# Baum mit erzwungener Klasse neu KONFIGURIEREN und die Mengen der uebersetzten
# Test-Quelldateien vergleichen (459 SOLL aus 'git ls-files'):
#   avx512f (ehrlicher Cache) .......... 455 im Bauweg
#   -DCOMDARE_HOST_RUNS_AVX512F=0 ...... 454 im Bauweg -- verliert GENAU diese eine Datei
#   zusaetzlich -DCOMDARE_HOST_RUNS_AVX2=0  454 im Bauweg -- verliert KEINE weitere
#   Gegenrichtung beide Male LEER; nach 'cmake -U ...' wieder 455, Menge byte-gleich.
# Die anderen fuenf Registrierungen gattern nur ihr add_test(), nicht ihr
# add_executable() -- ihre Quelldateien werden auf jeder Klasse uebersetzt und koennen
# dieser Wache deshalb nie fehlen. Fuer die Stufe 'Quelldatei -> Bauweg' hat diese
# Klasse also GENAU EIN Mitglied, nicht sechs.
# -----------------------------------------------------------------------------
tests/unit/test_ap5_simd_extension_coherence.cpp | isa:avx2+avx512f | GO-3-A1-Kohaerenzbeweis hinter dem Host-ISA-Gatter
