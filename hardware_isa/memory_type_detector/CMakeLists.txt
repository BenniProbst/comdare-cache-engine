# hardware_isa/memory_type_detector — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_memory_type_detector INTERFACE)
target_include_directories(comdare_memory_type_detector INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
