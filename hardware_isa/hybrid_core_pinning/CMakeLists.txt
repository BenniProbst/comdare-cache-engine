# hardware_isa/hybrid_core_pinning — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_hybrid_core_pinning INTERFACE)
target_include_directories(comdare_hybrid_core_pinning INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
