# cache_engine/subsystems/platform_profiler — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_platform_profiler INTERFACE)
target_include_directories(comdare_platform_profiler INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
