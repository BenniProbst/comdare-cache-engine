# cache_engine/subsystems/prefetch_controller — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_prefetch_controller INTERFACE)
target_include_directories(comdare_prefetch_controller INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
