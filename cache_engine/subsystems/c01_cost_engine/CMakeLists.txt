# cache_engine/subsystems/cost_model — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_cost_model INTERFACE)
target_include_directories(comdare_cost_model INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
