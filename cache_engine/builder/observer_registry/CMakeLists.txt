# cache_engine/builder/observer_registry — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_observer_registry INTERFACE)
target_include_directories(comdare_observer_registry INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
