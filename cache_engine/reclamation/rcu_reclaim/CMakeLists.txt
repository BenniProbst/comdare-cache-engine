# cache_engine/reclamation/rcu_reclaim — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_rcu_reclaim INTERFACE)
target_include_directories(comdare_rcu_reclaim INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
