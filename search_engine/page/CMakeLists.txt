# search_engine/page — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_page_concept INTERFACE)
target_include_directories(comdare_page_concept INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
