# search_engine/traversal — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_traversal_concept INTERFACE)
target_include_directories(comdare_traversal_concept INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
