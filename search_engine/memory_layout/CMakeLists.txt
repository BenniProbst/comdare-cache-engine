# search_engine/memory_layout — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_memory_layout_concept INTERFACE)
target_include_directories(comdare_memory_layout_concept INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
