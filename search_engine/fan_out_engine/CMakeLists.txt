# search_engine/fan_out_engine — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_fan_out_engine INTERFACE)
target_include_directories(comdare_fan_out_engine INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
