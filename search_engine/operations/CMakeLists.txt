# search_engine/operations — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_operations INTERFACE)
target_include_directories(comdare_operations INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
