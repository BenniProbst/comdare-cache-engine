# measurement/perf_wrapper — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_perf_wrapper INTERFACE)
target_include_directories(comdare_perf_wrapper INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
