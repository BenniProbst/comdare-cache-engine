# measurement/platform_probe — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_platform_probe INTERFACE)
target_include_directories(comdare_platform_probe INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
