# measurement/hdr_histogram_wrapper — Sub-Komponente
# Skelett (Phase 4.B) — keine Implementation

add_library(comdare_hdr_histogram_wrapper INTERFACE)
target_include_directories(comdare_hdr_histogram_wrapper INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})
