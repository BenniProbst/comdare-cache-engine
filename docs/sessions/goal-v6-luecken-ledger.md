# Goal-V6 Lücken-Ledger — SOLL (Doc 28) ⇄ IST (Code, literal) — Single-Source der offenen Punkte

> **Goal-V6 Phase B (Konsolidierung).** Lebendiges Ledger: gleicht die SOLL-Kartographie (Doc 28) gegen den
> literal verifizierten IST-Code ab. Jede Lücke: SOLL-Quelle (Doc:§) · IST-Code-Stand (Datei:Zeile) · literales
> Verifikations-Kriterium · Aufwand · Status. IST-Stände stammen aus 3 frischen adversarial-präzisen
> Planungs-Agenten dieser Session (a0c96bb #74 · a94f574 Gattungen · a8de276 Skalierung), je gegen den realen
> Code gegengeprüft. **Kein Eintrag wird „erledigt" ohne literale Tool-Ausgabe** ([[feedback_no_success_marks_without_literal_output]]).
> Reihenfolge der Abarbeitung + Sprints: siehe Planungssession `20260602-goal-v6-planungssession-gesamtplan.md`.

## §0 Solide IST (literal belegt, NICHT offen) — Referenz-Anker

| Gate | Bedingung | Beleg | Status |
|------|-----------|-------|--------|
| Gate-1 | `binary_count() == ∏ mp_size(Enabled_i)` | `test_br1_full22_count`: == 137.594.142.720.000 (22 Achsen, arithmetisch, OOM-sicher) | ✅ solide |
| Gate-2 | 22 Achsen als Baum-Ebene, volles Enabled-Inventar | `test_br1_full22_count`: 22 distinkte Blöcke, `block_id()==axis()` | ✅ solide |
| Gate-5 | inverse Signatur-Projektion (KF-15) über reale Kompositionen | `test_br_kf15_real`: 8 reale Blätter → gepinnte Signatur | ✅ solide |
| Gate-6 | belegt Doc 26/27 + Session-Doku | Doc 26/27/28 + Audit-Session-Doku | ✅ solide |
| BR-4 (SearchAlgorithm) | Blatt → reale DLL → Loader → `dynamic_cast<IObservableTier*>` + observe | `br4_load`: organ_count==17, observe 256/256/256 (1 Pilot-DLL) | ✅ solide (Pilot) |

**Gattungs-Bindung IST:** 2 von 5 (`test_genus_binding`/`test_container_genus`: `GenusBound<SearchAlgorithm>==true`, `GenusBound<Adapter>==true`, Set/Sequence/View==false). `GenusBindingTraits<Adapter>::slot_count==1`.

---

## §1 Lücken-Ledger (offene Punkte)

### L-73 — `provision_all` results-Vektor O(K) statt O(∏)  · Task #73 · Aufwand: S (klein)
- **SOLL:** Doc 28 §6 (#73 Tech-Debt) + Doc 26 §2 (OOM-Disziplin „nie alle ∏ vorhalten").
- **IST (literal):** `builder/build_orchestrator/build_orchestrator.hpp:153` `std::vector<BuildResult> results(view.size());` → bei `view.size()==1.4e14` sofortiger OOM. Worker indizieren `results[i]` über globalen View-Index (`:170/:188/:223`); `next.fetch_add` läuft bis `view.size()`.
- **Verifikations-Kriterium:** Unit-Test mit synthetisch großer `view.size()` (z.B. 1e12 Stub-Levels) + `BuildSelection` mit 5 Indizes → `results.size()==5`, kein OOM, Stub-CompileFn nur 5× aufgerufen; bestehender `test_kf16_build_orchestrator` bleibt grün (Rückwärts-Overload).
- **Abhängigkeit:** L-SEL (BuildSelection). **Status:** OFFEN.

### L-SEL — StaticBinaryView (∏-lazy) → endliche reale Build-Liste  · (Teil #73-Umfeld) · Aufwand: M
- **SOLL:** Doc 28 §5 („reale Cluster-Build-Menge ≪ ∏; BUILD-PROFIL/Enabled-Flags/Coverage wählt"), Doc 22 §4 (1-wise 25 statt 2125), messarchitektur_v5_drei_profile §1.
- **IST (literal):** ∏-Reduktion existiert HEUTE zweigleisig getrennt: (a) configure-time `Enabled*`-Flags (kleines ∏); (b) `anatomy/combinatorial_coverage.hpp` 1-wise — aber NUR im `apps/adhoc_emitter/main.cpp:106-178`-Pilot, NICHT am `StaticBinaryView`. Es fehlt EINE explizite Übersetzung „Lazy-View(∏) → endliche Index-Selektion".
- **Verifikations-Kriterium:** `select_one_wise(48-Pilot-View).size() == max(AxisLevel.values.size())`; jede Achse-Variante ≥1× im Index-Satz (Identitäts-Test gegen `combinatorial_coverage`). NIE `for_each_binary`/`pinned_signature_index()` auf vollem ∏ (beide O(∏)).
- **Neue Datei:** `builder/experiment_tree/coverage_selection.hpp` (`BuildSelection` POD: `vector<size_t> indices` + Provenienz; `select_full/one_wise/by_pinned_signature/explicit`). **Status:** OFFEN.

### L-77 — ceb_generator reale Anatomie ODER Anspruch präzisieren  · Task #77 · Aufwand: S (Doku) + M (Brücke)
- **SOLL:** Doc 28 §3 BR-4-Kriterium (generiert→gebaut→Loader→`dynamic_cast<IObservableTier*>`+`tier_observe` über REALE Komposition), Doc 28 §6 (#77), Doc 26 §7 KF-8.
- **IST (literal):** `builder/experiment_tree/ceb_generator.hpp:52-82` emittiert STUB: `#define COMDARE_PERM_<AXIS>_IS_<VALUE>` (von niemandem konsumiert) + leerer `extern "C" perm_<id>_run` (gibt `0.0`). KEIN Umbrella-Include, KEIN ADHOC-Makro. Der REALE BR-4-Pfad ist `builder/codegen/adhoc_emitter.hpp` (`render_adhoc_module_source` → `COMDARE_DEFINE_ANATOMY_MODULE_ADHOC(<17 FQ>)`). **Diskrepanz:** Task #70 `[completed]` betitelt „ceb_generator emittiert ADHOC" — trifft nur auf adhoc_emitter zu.
- **Verifikations-Kriterium:** (4.B) ceb_generator-Header-Doku + Doc 26 §7 KF-8 + Task #70-Titel korrigiert: ceb_generator = Pfad-Manifest/Diagnose-Shell; reale Anatomie = adhoc_emitter. (4.A) `generate_all_real<Engine>` delegiert an `emit_adhoc_modules<Engine>`.
- **Risiko:** NICHT zur vollen String→Typ-Tabelle ausbauen (Direktive „kein Runtime-Switch"). — eingehalten (Brücke ist typ-getrieben via Engine).
- **✅ ERLEDIGT (D3, test_d3_ceb_generator literal grün, min_free 9.34 GB):** (1) `generate_perm_source` = Diagnose-Shell — hat `COMDARE_PERM_`-#defines + `perm_<id>_run`-Stub (0.0), KEIN ADHOC-Makro. (2) `generate_all_real<FakeEngine>` emittiert 1 Modul mit `COMDARE_DEFINE_ANATOMY_MODULE_ADHOC` + `all_axes_umbrella.hpp` + `anatomy_manifest.txt`. Beide Pfade output-disjunkt. Header-Doku + Doc 26 §7 KF-8 + Task #70-Titel präzisiert.

### L-75 — Container-Gattung vervollständigen: q2-Slot + DLL-Pfad  · Task #75 · Aufwand: M (3–5 Tage)
- **SOLL:** Doc 28 §2 (Adapter: 2 Slots q1+q2 + axis_inner), Doku 14 §26.4/§28/§32.2, Doc 24 §8.8 (neue Gattung = neues Sub-Interface + neuer V1-POD, NIE IAnatomyBase mutieren).
- **IST (literal):** `anatomy/container_anatomy.hpp` `ContainerComposition` = 1 Slot (Q1 buffer); `GenusBindingTraits<Adapter>::slot_count==1`; `ContainerDock` nur in-process (kein DLL-Pfad analog BR-4). q2-Inventar vorhanden: `topics/queuing/axis_q2_queuing/` 5 FlushPolicies (Eager/Watermark/Lazy/Timed/AdaptiveLsm).
- **Verifikations-Kriterium:** (1) `GenusBindingTraits<Adapter>::slot_count==2`; `ContainerAnatomy<ContainerComposition<FifoBuffer,WatermarkFlush>>::organ_count()==2`. (2) in-process: `observer.full_flush_count>0` bei Watermark. (3) **DLL-Round-Trip:** emittiert→gebaut→`AnatomyModuleLoader::load()==status_ok`→`anatomy()->genus()==Adapter`→`dynamic_cast<IContainerTier*>≠null`→put/get über DLL-Grenze→`tier_observe_container` liefert `put_count==N`.
- **Neue Dateien:** `anatomy/container_tier.hpp` (IContainerTier + ContainerObserverSnapshotV1), `anatomy/container_abi_adapter.hpp`, `COMDARE_DEFINE_CONTAINER_MODULE` in `include/cache_engine/abi/container_module_abi_v1.hpp`.
- **Loader bleibt UNVERÄNDERT** (gattungs-agnostisch, gibt `IAnatomyBase*`, Gattung runtime via `genus()`).
- **✅ ERLEDIGT (D4a in-process + D4b DLL, literal grün, min_free 9.07–9.50 GB):**
  - **D4a:** ContainerComposition<Q1,Q2> (Q2 Default ContainerNoFlushPolicy, entkoppelt), ContainerAnatomy organ_count()==2, put() treibt Flush; GenusBindingTraits<Adapter> slot_count==2. test_container_genus: WatermarkFlush 75%@cap8 → full_flush_count==3; test_container_dock + test_genus_binding rückwärts-kompatibel grün.
  - **D4b:** IContainerTier + ContainerObserverSnapshotV1 (ABI-POD, standard_layout) + ContainerAbiAdapter (erbt IAnatomyBase+IContainerTier) + COMDARE_DEFINE_CONTAINER_MODULE. test_d4b_container_adapter (in-process dynamic_cast). **test_d4b_container_dll (3-Phasen DLL-Round-Trip):** AnatomyModuleLoader::load==status_ok → genus()==Adapter, organ_count==2, `dynamic_cast<IContainerTier*>`≠null → put 20 + Q2-Watermark-Flush (full_flush=1) über die REALE .dll-Grenze. DERSELBE Loader wie SearchAlgorithm (keine Loader-Änderung, Doc 24 §8.8). **Status: ✅**

### L-74a — page_type/09b/12 als echte Build-Varianten DERSELBEN 17-Slot-Binary  · Task #74 · Aufwand: M (3–4 Tage)
- **SOLL:** Doc 27 §0.1 (Zeile 59: „SearchAlgorithm-Sub/Build-Varianten (3) … modifizieren DIESELBE Binary … NICHT eigene Gattung"), Doc 28 §1 Zeile 46-48 (Spalte „Build/Definition-SOLL"), Doc 15 §6 (Cross-Constraint 96→~25).
- **IST (literal):** Die 3 Achsen sind reine `static constexpr` Compile-Konstanten (`axis_01_page_type_concept.hpp:25-32`, `axis_09b_..._concept.hpp:18-28`, `axis_12_..._concept.hpp:19-28`). Der Emit-Pfad `adhoc_emitter.hpp:22-44` gibt NUR die 17 FQ-Typen → die Binary trägt 09b/12/page_type NICHT als Build-Parameter. BR-1 reflektiert sie bereits als Ebene (`registry_to_axis_levels.hpp:63-65,92-94`).
- **Entscheidung (Doc 27 §0.1 vorgegeben, NICHT neu):** KEINE `AdHocComposition<20>` (17 = Gattungs-Invariante bleibt). Die 3 als zusätzliche Codegen-Parameter (`using`-Aliase + `static_assert` Concept) in die Modul-`.cpp` einbacken; Build-Identität via 3 `extern "C"`-Inspection-Symbole (`comdare_build_page_kind/simd_width_bits/hw_cache_line`).
- **Verifikations-Kriterium:** `test_gate4_buildvariant_emit`: aus 1 Blatt emittiert → `COMDARE_DEFINE_ANATOMY_MODULE_ADHOC_BUILDVARIANT(<17+3 FQ>)` → DLL gebaut → `comdare_build_simd_width_bits()==256 (Avx2)` vs `==512 (Avx512)` (2 Varianten DERSELBEN 17-Komposition unterscheidbar). Cross-Constraint: `(4 ISA × 8 SE × 3 HW)=96 → ~25-30` literal via `mp_size`.
- **Neue Dateien:** `COMDARE_DEFINE_ANATOMY_MODULE_ADHOC_BUILDVARIANT` (anatomy_module_abi_v1.hpp), `build_variant_args<PT,SE,HW>()` (adhoc_emitter.hpp), `builder/experiment_tree/hw_cross_constraint.hpp`. **Status (2026-06-03):** GROSSTEILS DONE — `anatomy/build_variant_definition.hpp` (`BuildVariantDefinitionV1` POD + `build_variant_definition<PT,SE,HW>()`) + `include/cache_engine/abi/build_variant_inspection.hpp` (`COMDARE_DEFINE_BUILD_VARIANT_INSPECTION` extern-"C"-Symbol) existieren; in-process literal grün (`test_d7_build_variant` + `test_d7a_build_inspection`). **DLL-GRENZE DONE (NEU 2026-06-03):** `test_buildvariant_dll` zieht via `GetProcAddress` aus 2 Build-Varianten-DLLs (`genus_buildvariant_{avx512,avx2}.cpp`, committet + `cl /LD`) das Inspection-Symbol über die ECHTE .dll-Grenze → `simd_width_bits==512` (Avx512) vs `==256` (Avx2) DERSELBEN page/hw, `a512 != b256` literal unterscheidbar (`buildvariant_dll_roundtrip.ps1`, ALLE OK, run_exit=0). **Factory real-wrapper-kompatibel gemacht:** `detail::detect_avx512<SE>()` tolerant (`provides_avx512()` Stub ODER `provides_avx512f()` der ECHTEN axis_09b-Wrapper ODER Breite≥512) → regress-frei (test_d7/d7a grün). **ECHTE Library-Wrapper in DLL DONE (NEU 2026-06-03):** `genus_buildvariant_real_{avx512,avx2}.cpp` bauen die ECHTEN `DenseBytePageType`/`Avx512SimdExtension`/`Avx2SimdExtension`/`X86_64HardwareProfile`-KLASSEN in die Build-Varianten-DLL (CMake-MODULE `perm_buildvariant_real_*` mit generierten flags.hpp-Dirs + Boost::mp11). **Verifiziert (literal grün):** `test_buildvariant_dll` über die ECHTEN-Wrapper-DLLs: `simd_width_bits==512`/`simd_avx512==1` (via `provides_avx512f` der ECHTEN Avx512SimdExtension) vs `256`/`0` (Avx2), `page_kind==0` (DenseBytePageType), `hw_cache_line==64` (X86_64HardwareProfile), `a512 != b256` (real_exit=0). Damit ist die tolerante detect_avx512-Factory gegen die ECHTE axis_09b-API bewiesen. **ADHOC_BUILDVARIANT DONE (NEU 2026-06-03):** `COMDARE_DEFINE_ANATOMY_MODULE_ADHOC_BUILDVARIANT(PT,SE,HW,<17 Anatomie>)` (in anatomy_module_abi_v1.hpp; ruft COMDARE_DEFINE_ANATOMY_MODULE_ADHOC für die 17 + COMDARE_DEFINE_BUILD_VARIANT_INSPECTION für die 3) → `genus_adhoc_buildvariant.cpp` baut EINE DLL (`perm_adhoc_buildvariant`, CMake-MODULE mit Umbrella+Boost+generierten Dirs) die SOWOHL die 17-Slot-Anatomie ALS AUCH die 3-Build-Achsen-Inspection trägt. **Verifiziert (literal grün, `test_adhoc_buildvariant_dll`, adhoc_exit=0):** aus DERSELBEN .dll (1) über den AnatomyModuleLoader → `genus()==SearchAlgorithm` + `organ_count()==17`; (2) über `GetProcAddress("comdare_build_variant_inspect")` → `simd_width_bits==512`/`simd_avx512==1`/`page_kind==0`/`hw_cache_line==64`/`present_mask==7`. **Beweist Doc 27 §0.1: die 3 Build-Achsen modifizieren DIESELBE 17-Slot-Binary (KEINE AdHocComposition<20>, keine eigene Gattung).** **hw_cross_constraint (c) DONE (NEU 2026-06-03):** Befund — die Logik existierte BEREITS (R5.C.3/#704, `topic_hardware_config_set.hpp`: `isa_simd_compatible`/`isa_platform_compatible` + `FilteredIsa09xExt09bxPlatform12` via verkettete `mp_remove_if`); die „neue Datei hw_cross_constraint.hpp" war veraltet. **NEU literal verifiziert:** der deterministische 3-Wege-`96→N`-Beweis ergänzt in `test_v41_r5c3_isa_simd_cross_constraint.cpp` (Section 6, build-config-unabhängig über AllIsas×AllExtensions×AllPlatforms): **RAW(4 ISA × 8 SE × 3 HW)=96 → FILTERED=22** (physisch möglich), alle 22 ISA×SIMD- UND ISA×Plattform-konsistent, `<Amd64,Avx2,X86_64>` bleibt, `<Amd64,Avx512,Aarch64-Plattform>` raus. Test grün (3 PASSED, offizieller CMake-Build). **DAMIT IST L-74a VOLLSTÄNDIG — #74 GATE-4 (page_type/09b/12) KOMPLETT** (Build-Varianten in-process+DLL+ECHTE-Wrapper, ADHOC_BUILDVARIANT 17+3-in-1-DLL, per-Knoten-Definition L-74b, 22-Vollständigkeit, CMake-MODULE-Registrierung, Cross-Constraint 96→22).

### L-74b — Eigener „Observer" der 3 Build-Achsen = je-Knoten-getragene + ABI-gezogene Definition  · Task #74 · Aufwand: M (2 Tage)
- **SOLL:** Doc 27 §3 (Zeile 156-159: „Hardware-Achsen = reine Build-Time-Konstanten … evtl. KEIN Laufzeit-Observer → ‚Achsen-Definition statt Observer' je Knoten, aber EXPLIZIT dokumentiert"), Doc 28 §4 Punkt 4 („22 Observer, NICHT 17").
- **IST (literal):** `axis_observer_classification.hpp:42-66` klassifiziert die 3 als `DefinitionOnly` — nur statische Tabelle, KEIN je-Knoten-Träger, KEIN ABI-Pull.
- **Empfehlung (begründet):** „Definition-statt-Observer" IST korrekt (kein Laufzeit-Zustand: alle API `static constexpr`; Build-Konstante = Identität, kein Mess-Ergebnis; Doc 27 §3 sieht es vor). Task-Titel „echter Observer" präzisiert zu „echte je-Knoten-getragene + ABI-gezogene Definition". KEIN Pseudo-Observer (Anti-Fake).
- **Verifikations-Kriterium:** `test_gate4_definition_per_node`: (a) `build_variant_definition<DenseByte,Avx512,X86_64>().vector_width_bits==512`; (b) gemessener Knoten `build_def_real==true`, ungemessener `==false` (SPARSE-Kontrast); (c) 22-Vollständigkeit: 17 Observer + 3 Definition + 2 Container == 22, keine fällt weg.
- **Neue Dateien:** `anatomy/build_variant_definition.hpp` (BuildVariantDefinitionV1 POD), `builder/experiment_tree/build_variant_definition_reader.hpp`; `NodeValue` += `BuildVariantDefinitionV1 build_def{}; bool build_def_real=false`. **Status (2026-06-03): ✅ DONE** — `BuildVariantDefinitionV1` POD existierte schon (test_d7 grün); **NEU:** `NodeValue` trägt jetzt `build_def{}` + `build_def_real=false` (additiv, umbrella-unabhängig — BuildVariantDefinitionV1 ist ein flacher uint64-POD), Reader `build_variant_definition_reader.hpp` (`read_build_variant<PT,SE,HW>(nv)` → setzt build_def + markiert build_def_real, Gegenstück zu node_value_measurement.hpp/BR-3). **`test_d7b_definition_per_node` literal grün** (alle 3 Verifikations-Kriterien): (a) `build_variant_definition<DenseByte,Avx512,X86_64>().simd_width_bits==512`; (b) gemessener Knoten `build_def_real==true`/`simd_width==512`/`avx512==1` vs ungemessener `==false`+`build_def==Default{}` (SPARSE-Kontrast wie observer_real) + Avx512-Knoten≠Avx2-Knoten (Build-Varianten-Definition unterscheidbar); (c) **22-Vollständigkeit** via `count_observer_kind`: 17 SearchAlgorithmObserver + 3 DefinitionOnly + 2 ContainerObserver == 22. Regress-frei (test_d1_d2_selection + test_d14_result_ingest grün nach NodeValue-Änderung). CTest-registriert (LIGHT, 16 leicht).

### L-74c — R5.B-Voll-Operativität: die 13 heute passiven der 17 Komposition-Achsen real messbar  · Task #74 · Aufwand: L (4–6 Tage)
> **KERN ERLEDIGT (2026-06-03):** Alle **4 OperativeCapable-Achsen** (telemetry/memory_layout/serialization/node_type) + search_algo sind als ObservableAxis-Hüllen real in `observe_all()` verdrahtet + literal verifiziert: `test_v41_anatomy_observer` **alle 5 `Saeule2_ObserveAllReal.*`-Tests `[ OK ]`** (kein SKIP), `observable_count>=5`, je echte getriebene Werte. Wiederverwendbares `ObservableXxx<Strategy>`-Muster (Doc 29 §3c–§3f). **VERBLEIBT (ehrlich, kein Block):** (a) Auto-Kopplung — die observe_*-Treiber werden heute explizit gerufen (wie search_algo via search_algo_organ()), eine Auto-Kopplung an den Workload-insert im abi_adapter/AnatomyExecutionContext ist Zusatz; (b) AdHoc/Registry-Pfad — nur die 11 Reference-Compositions tragen die Hüllen, der Permutations-Pfad (telemetry/memory_layout/serialization/node_type-Registries) müsste die Hüllen liefern, damit auch generierte Permutationen die 4 Achsen observable haben; (c) Cross-ABI-POD-Append. **Befund 2026-06-03 (abi_adapter.hpp:269-297 tier_observe gelesen):** Der Cross-ABI-Mess-Treiber `tier_observe` sammelt heute search_algo + allocator in `ComdareTierObserverSnapshotV1`; die 4 OperativeCapable-Achsen fehlen dort noch (eigene POD-Felder + abi_adapter-Organe nötig). **ABER `tier_observe:294` `observable_axis_count = ObserverAggregate::observable_count()` meldet durch die Hüllen-Umstellung bereits 5+ (vorher 2)** — der Cross-ABI-POD reflektiert den Composition-Driver-Fortschritt also schon teilweise automatisch. Vollausbau (Cross-ABI): der abi_adapter (hält heute nur `search_organ_`/`container_`) müsste die 4 Achsen-Organe selbst halten + in `run_workload`/`tier_insert` treiben + POD append-only erweitern (Minor-Bump) + DLL-Round-Trip-Test. Mehrteiliger Eingriff → eigener Sprint mit frischem Kontext (nicht im überfüllten Kontext überreichen). **TEILSCHRITT ERLEDIGT (2026-06-03): Cross-ABI-Brücke ObserverAggregate→flacher V2-POD.** `observable_tier.hpp`: `ComdareTierObserverSnapshotV2` (V1-Block + 17 uint64-Felder für die 4 OperativeCapable-Achsen; design-konform NEUER Typ statt append-only; standard_layout+trivially_copyable static_assert). `tier_observer_v2_bridge.hpp`: generisches `fill_tier_observer_v2<Composition>()` (if-constexpr je Achse). **Literal grün** (`test_v41_anatomy_observer`, `[ OK ] V2Bridge_FlatPod.AllDrivenAxesFlowIntoV2Snapshot`): alle 5 getriebenen Achsen fließen in den flachen POD (search_insert=10/telemetry_total=2/layout_checksum=100/serialization_bytes=32/node_checksum=6/observable_axis_count≥5). **VERBLEIBT (Cross-ABI-Rest):** abi_adapter müsste die 4 Achsen-Organe halten + in run_workload/tier_insert treiben + ein `tier_observe_v2()`-Pfad (Interface-additiv) + DLL-Round-Trip. Die Brücke selbst ist damit gebaut+verifiziert; verbleibt das Treiben über die echte DLL-Grenze. **AUTO-KOPPLUNG telemetry ERLEDIGT (2026-06-03):** `abi_adapter.hpp` hält `mutable telemetry_organ_` + treibt `record_node_touch` in `tier_insert`/`tier_lookup` (if-constexpr-geschützt für AdHoc-nackte-Strategie) + `fill_observer_v2()` (non-virtual additiv) zieht V1-Achsen + telemetry in den V2-POD. **Literal grün** (`test_d_v42_abi_telemetry_coupling`, scratch MEASUREMENT_ON+STATISTICS): 20 insert + 20 lookup AUSSCHLIESSLICH über die Gattungs-ABI → `telemetry_total=40/leaf=40/node=0` (LeafOnlyCounter), `search_insert=20`, `observable_axes=8`. KEIN explizites Organ-Treiben — echte Cross-ABI-Auto-Kopplung. **VERBLEIBT:** (a) scan-Achsen (memory_layout/serialization/node_type) Auto-Kopplung über run_workload (Pfad A, Daten-Buffer) — telemetry war der per-Op-koppelbare; (b) ~~`tier_observe_v2()`-Interface-Method (vtable-additiv)~~ **ERLEDIGT (2026-06-03)** + DLL-Round-Trip noch offen; (c) AdHoc/Registry-Pfad (Registry liefert Hüllen). **tier_observe_v2 ERLEDIGT:** `observable_tier.hpp` `IObservableTier::tier_observe_v2(V2*)` als vtable-additive virtuelle Methode (Default-no-op am vtable-Ende → vorwärts-kompatibel, kein tier_observe-Bruch); `abi_adapter` override ruft `fill_observer_v2`. **Literal grün** (`test_d_v42_abi_telemetry_coupling` erweitert): der V2-POD wird über die ECHTE Gattungs-ABI `static_cast<IObservableTier*>(&ad)->tier_observe_v2()` gezogen, identisch (`v2_abi == v2`, telemetry_total=40). Regress test_d13 (AdHoc-Pfad) grün nach dem vtable-Eingriff. Damit ist der Host-Zugriffspfad (dynamic_cast→tier_observe_v2) für die 4 OperativeCapable-Achsen vorhanden; verbleibt nur der echte DLL-Round-Trip (load+observe über die .dll-Grenze) + scan-Achsen-Treiben. **DLL-ROUND-TRIP-BEFUND (2026-06-03, ehrlich):** Erster Versuch (Test über die reale `comdare_r5g_adhoc_perm`-DLL) crashte mit SEH 0xc0000005 — KEIN V2-Code-Fehler (In-Process grün), sondern vtable-ABI-Subtilität: `tier_observe_v2` ist eine NEUE virtuelle Methode am vtable-Ende; die per Codegen erzeugte DLL wurde nicht synchron neu gebaut (Touch erzwingt den Emitter-Rebuild nicht) → Host-vtable (neuer Slot) vs. DLL-vtable (alt) → Sprung über das vtable-Ende → Crash. **Regress-frei verifiziert:** die 3 bestehenden DLL-Tests (R5G + R8RestA, nur tier_observe-V1) bleiben grün — mein vtable-Eingriff bricht sie NICHT. **LEHRE + nächster Schritt:** vtable-additive Methode ist über die DLL-Grenze nur bei SYNCHRONEM DLL+Host-Rebuild sicher; ABI-robuster Weg = **separates Sub-Interface `IObservableTierV2`** (per dynamic_cast abgefragt, alte DLLs → nullptr statt Crash — exakt das Designprinzip, mit dem IObservableTier selbst NICHT an IAnatomyBase hängt). Der In-Process-V2-Pfad bleibt verifiziert; der DLL-Round-Trip wird über IObservableTierV2 + Codegen-DLL-Rebuild-Dependency nachgezogen. **IObservableTierV2 ERLEDIGT (2026-06-03):** `observable_tier.hpp` `IObservableTierV2` als EIGENSTÄNDIGES Sub-Interface (erbt nichts, `tier_observe_v2` pure virtual); `abi_adapter` erbt es zusätzlich (MEASUREMENT_ON) + override→fill_observer_v2. **In-Process literal grün** (`test_d_v42_abi_telemetry_coupling`): `dynamic_cast<IObservableTierV2*>` gelingt → tier_observe_v2 → telemetry_total=40. **DLL-Round-Trip literal grün (ABI-ROBUST bewiesen):** `test_v41_anatomy_adhoc_dll_load` — der neue V2-Sub-Interface-Test über die reale DLL **SKIPPED sauber statt zu crashen** (`dynamic_cast<IObservableTierV2*>`→nullptr bei nicht-synchron-gebauter DLL → Host degradiert auf V1, run_exit=0); die 3 bestehenden DLL-Tests bleiben grün (regress-frei). Der vtable-Append-Crash (0xc0000005) ist damit strukturell behoben. **VERBLEIBT für VOLLEN DLL-telemetry-Beweis (nicht-SKIP):** synchroner Codegen-DLL-Rebuild (cmake erkennt den Header-Change der codegen-erzeugten comdare_r5g_adhoc_perm-DLL nicht automatisch — Build-System-Dependency, kein ABI-Problem mehr) + Registry-Pfad (Hüllen in der DLL-Composition). Damit ist der Cross-ABI-V2-Pfad strukturell vollständig + robust; offen bleibt nur die Build-Dependency + scan-Achsen + Registry. **DLL-ROUND-TRIP VOLL GRÜN (2026-06-03):** Ursache des SKIP war ein Multi-Config-Output-Dir-Bug (die neu gebaute DLL landete in `r5g_adhoc_modules/Debug/`, `load_all` lud die alte DLL aus `r5g_adhoc_modules/`). Fix: `RUNTIME/LIBRARY_OUTPUT_DIRECTORY_DEBUG/_RELWITHDEBINFO` explizit in der CMakeLists → die codegen-DLL landet bei jeder Config direkt im Lade-Ort. **`test_v41_anatomy_adhoc_dll_load` 4/4 PASSED**, darunter `[ OK ] V2SubInterfaceOverRealDllBoundaryOrGracefulDegrade` (NICHT-SKIP): `dynamic_cast<IObservableTierV2*>` gelingt über die reale `.dll`-Grenze, `tier_observe_v2` zieht den V2-POD (search_insert≥1, observable_axis_count≥1, tier_fill_level>0 — über die echte DLL getrieben). **Der Cross-ABI-V2-Mess-Pfad ist damit end-to-end (In-Process + reale DLL) bewiesen + ABI-robust + reproduzierbar.** VERBLEIBT an L-74c: telemetry-über-DLL = 0 (AdHoc-DLL trägt nackte Registry-Strategie → Registry-Pfad-Umstellung nötig) + scan-Achsen-Auto-Kopplung (Pfad A). Der V2-POD-Transport + search_algo über die DLL ist voll verifiziert. **REGISTRY-PFAD telemetry ERLEDIGT (2026-06-03):** `topic_telemetry_config_set.hpp` `StaticAxisVariants_11 = mp_transform<make_observable_telemetry, EnabledTelemetries>` — der Permutations-Pfad (BR-1 registry_to_axis_levels) trägt jetzt die ObservableTelemetry-Hülle, sodass AUTO-emittierte AdHoc-Kompositionen telemetry-observable sind (nicht nur die Reference-Compositions). **Literal verifiziert:** `test_d_v42_telemetry_registry_observable` (alle 4 StaticAxisVariants ObservableAxis) + **`test_br3_observer` voll grün** (Permutations-Pfad BR-1/BR-2/BR-3 mit Hülle, `ALLE OK`, search_insert=256, 17-Achsen-Definition korrekt — KEIN Regress). Damit ist die telemetry-Achse durchgehend observable (Reference + Permutation + DLL-fähig). **ALLE 4 REGISTRIES ERLEDIGT (2026-06-03):** memory_layout/serialization/node_type analog umgestellt (`topic_*_config_set.hpp`: `StaticAxisVariants = mp_transform<make_observable_*, Enabled*>`; bei nodes NUR axis_04, path_compression bleibt nackt). **`test_br3_observer` voll grün nach allen 4 Umstellungen** (`ALLE OK`, search_insert=256, 17-Achsen-Definition korrekt — der Permutations-Pfad BR-1/BR-2/BR-3 baut mit node_type-Hülle als N + memory_layout-Hülle als L in ComposedStore, KEIN Regress). **Damit sind alle 4 OperativeCapable-Achsen durchgehend observable: Reference-Compositions + Permutations-Pfad** (auto-emittierte AdHoc-Kompositionen). VERBLEIBT an L-74c: (1) DLL über den Emitter neu-generieren (statische `auto_emitted_perm_module.cpp` trägt noch die alte nackte Composition) → dann observable_axis_count>baseline über die echte DLL; (2) scan-Achsen-Auto-Kopplung Pfad A (run_workload Z.215/218 static→Organ-observe_*). **SCAN-ACHSEN-AUTO-KOPPLUNG memory_layout+serialization ERLEDIGT (2026-06-03):** statt Pfad-A-run_workload den saubereren Pfad-B-Zustand-Scan gewählt — `ComposedStore.organ_observe_layout/serialization(org)` treibt ein Observer-Organ über das ECHTE Slot-Backing (`slots_.data()`), durchgereicht via `ObservableComposedSearch.store_observe_*`, der `abi_adapter` hält `ml_organ_`/`ser_organ_` (mutable) + treibt sie idempotent (reset vor Scan) in `fill_observer_v2`. **Literal verifiziert** (`test_d_v42_abi_telemetry_coupling`): nach 20 inserts `layout_records=20 == tier_fill_level`, `layout_checksum=190 = sum(0..19)` (REALE Tier-Daten, kein synthetischer Buffer), `serialization_records=20`, idempotent (`v2_abi==v2`). Regress test_d13 (AdHoc) grün. **node_type-AUTO-KOPPLUNG ERLEDIGT (2026-06-03):** `ComposedStore.organ_observe_node_type` extrahiert die niederwertigsten Key-Bytes (ART-Node256-Adressierung) aus dem Slot-Backing als stored + self-query, durchgereicht via `ObservableComposedSearch.store_observe_node_type`, im `abi_adapter` `node_organ_` (mutable) idempotent in `fill_observer_v2` getrieben. **Literal verifiziert** (`test_d_v42_abi_telemetry_coupling`): nach 20 inserts `node_find_count=1`, `node_keys_stored=20 == tier_fill_level`, `node_last_checksum=190 = sum(0..19)` self-query (REALE Tier-Keys). Regress test_d13 (AdHoc) grün. **DAMIT SIND ALLE 4 OperativeCapable-Achsen AUTO-GEKOPPELT** (telemetry via tier_insert/lookup; memory_layout/serialization/node_type via Slot-Backing-Zustand-Scan) und fließen über `tier_observe_v2`/`IObservableTierV2` in den flachen V2-POD. **DLL-EMITTER-QUELLE umgestellt (2026-06-03):** `auto_emitted_perm_module.cpp` trägt jetzt die 4 OperativeCapable-Achsen als ObservableXxx-Hüllen (= Emitter-Output nach der Registry-Umstellung) + die nötigen observable-Shim-Includes; **DLL-Build grün** (`comdare_r5g_adhoc_perm` kompiliert mit der Hüllen-Composition). Der V2-DLL-Test ist graceful (informative Ausgabe statt Hard-Fail). **OFFEN (reine Build-Infrastruktur, KEIN Code):** der DLL-telemetry>0-Beweis über die echte DLL scheitert an einem cmake-obj-Cache-Problem — die Codegen-DLL wird trotz Quell-Änderung nicht zuverlässig neu gebaut (Multi-Config-Subdir vs Lade-Ort + gecachtes obj; ein erzwungenes Löschen triggert den Sandbox-Pfadschutz). Braucht ein sauberes `cmake -B` Re-Configure (→ OUTPUT_DIRECTORY_DEBUG-Fix greift) + Clean-Rebuild. **Der In-Process-Pfad beweist alle 4 Achsen literal** (`test_d_v42_abi_telemetry_coupling`: telemetry=40, layout/serialization/node je 20 records, checksum=190). **L-74c-Kern + Cross-ABI + Registry-Pfad + ALLE Auto-Kopplungen sind damit verifiziert.** Die ursprüngliche 13-Achsen-Formulierung bleibt: die übrigen 8 (path_compression/prefetch/concurrency/value_handle/isa/index_organization/io/migration/filter) sind dauerhaft Deskriptor (kein Mess-Body), wie unten beschrieben.

- **SOLL:** Doc 24 §2.2/§5.5/§8.5, Doc 28 §1 Spalte „Observer-SOLL".
- **IST (literal):** `node_value_measurement.hpp:11-13` + `abi_adapter.hpp:268-298` (`tier_observe`): operativ getrieben sind nur **search_algo + allocator**; Pfad-A `run_workload` (`:215-218`) treibt zusätzlich memory_layout (`scan_field_sum`) + serialization (`serialize_scan`), aber diese fließen NICHT in `tier_observe`/`ObserverAggregate`. `observe_all()` (`search_algorithm_anatomy.hpp:62-72`) hält nur `axis_search_algo_`.
- **Befund:** R7.2/7.3/7.4 (#5/#6/#7 completed) = Organ-Body-Ausbau erledigt; was FEHLT = der **Composition-Driver** (Verdrahtung dieser Organe in tier_observe/observe_all).
- **Operativ machbar:** memory_layout (⚠️ PMC nötig, R5.D), serialization, telemetry (statistics-tragend), node_type (KF-6). Dauerhaft Deskriptor: path_compression/prefetch/concurrency/value_handle/isa/index_organization/io/migration/filter.
- **Verifikations-Kriterium:** `test_gate4_r5b_operative`: getriebenes Tier → `tier_observe` POD-Felder >0 für search+alloc UND ≥layout_scan_checksum>0 + telemetry_*>0 (Delta vor/nach Treiben, kein Stub); `observable_axis_count` == literal getriebene Zahl (≥4, exakt belegt); ungetriebene == 0. **R5.D PMC** ehrlich als „operativ-PMC-pending" markieren, NICHT als gemessen.
- **Ehrlicher Endzustand „22 Observer":** 17 SearchAlgorithmObserver (2 voll + 2-4 erweiterbar + Rest Deskriptor) + 3 Definition + 2 Container == 22. „alle 17 voll operativ" ist NICHT Ziel. **Status:** OFFEN.
- **V42-Sprint Baustein 3 (2026-06-03) — memory_layout-Hülle GRÜN (2. OperativeCapable-Achse):** `axis_05_memory_layout_observable.hpp` `ObservableMemoryLayout<Strategy>` — Abweichung von telemetry: `scan_field_sum()` ist static + wird static aufgerufen (`abi_adapter:215`, `composed_store:90`), daher Hülle mit **static Pass-Through** (Drop-in-Kompat) **+ Instanz `observe_scan()`** (Driver, trackt). `test_d_v42_memory_layout_observable` literal grün: `scan_count=1 records=4 field_bytes=16 cache_lines=4 checksum=100` (Delta>0) + Probe-Asserts. CTest-registriert (17 D-Tests). **NACHTRAG (gleicher Tag) — memory_layout VOLL VERDRAHTET:** Hülle um `topic_tag`-Durchreichung erweitert (→ erfüllt MemoryLayoutStrategy, einsetzbar als `L` in ComposedStore<N,L,A>); alle 11 Reference-Compositions `memory_layout`→Hülle; Anatomie hält `axis_memory_layout_` als 3. Organ + `memory_layout_organ()` + observe_all-Sammlung. **`test_v41_anatomy_observer` literal grün** (msvc-release/STATISTICS): `[ OK ] DrivenMemoryLayoutOrganFlowsIntoAggregate` (kein SKIP) — `observe_all().memory_layout` trägt nach `observe_scan()` `scan_count=1/records=4/checksum=100/cache_lines>0`, `observable_count>=3`. **Status L-74c: 3/4 OperativeCapable voll (search_algo+telemetry+memory_layout); nur serialization + node_type ausstehend.**
- **V42-Sprint Baustein 5 (2026-06-03) — serialization VOLL VERDRAHTET:** drittes Exemplar des Hüllen-Musters (analog memory_layout, EINFACHER: serialization nicht in ComposedStore). `axis_10_serialization_observable.hpp` `ObservableSerialization<Strategy>` (static `serialize_scan` Pass-Through für `abi_adapter:218` + Instanz `observe_serialize` Driver + topic_tag/supports_compression-Durchreichung). Alle 11 Reference-Compositions `serialization`→Hülle; Anatomie `axis_serialization_` als 4. Organ + `serialization_organ()` + observe_all. `test_d_v42_serialization_observable` (Baustein) + `test_v41_anatomy_observer` (Verdrahtung) literal grün: `[ OK ] DrivenSerializationOrganFlowsIntoAggregate` — `observe_all().serialization` = `serialize_count=1/records=4/bytes=32/checksum=100`, `observable_count>=4`. Alle **4 Säule-2-Tests grün**. **Status L-74c: 3 von 4 OperativeCapable-Achsen voll (telemetry+memory_layout+serialization) + search_algo; nur node_type ausstehend (komplexer — N in ComposedStore<N,L,A>).**
- **node_type-BEFUND (2026-06-03, für nächste Runde):** node_type passt NICHT 1:1 ins scan-Hüllen-Muster der anderen 3. Grund: node_type ist das `N` in `ComposedStore<N,L,A>` (`axis_04_node_type_composed_store.hpp`, `requires NodeTypeStrategy<N>`), eingebettet als Storage-Struktur — KEINE freistehende `scan_field_sum`-Methode wie L/serialization. Die vorhandene `organ_scan_field_sum()` (composed_store:89) misst faktisch **L (memory_layout)** über das Slot-Backing, nicht N. Eine node_type-Observable-Hülle müsste als `N` voll in ComposedStore einsetzbar sein (NodeTypeStrategy + Slot-Interface komplett durchreichen — tiefer als die L-Hülle, die nur `cache_line_size`/`topic_tag` brauchte). **Optionen für nächste Runde:** (a) ComposedStore selbst zu einer ObservableAxis machen (es trägt schon `allocator_statistics()` unter STATISTICS → analog ein `node_statistics()`/snapshot ergänzen + in der Anatomie als node_type-Organ beobachten); (b) NodeTypeStrategy-konforme Hülle um Node256NodeType. **Pflicht: zuerst NodeTypeStrategy + Node256NodeType-Wrapper proben** (Lehre: realer Wrapper). Erst danach Auto-Kopplung + AdHoc/Registry-Pfad für alle 4.
- **V42-Sprint Baustein 2 (2026-06-02) — telemetry VOLL VERDRAHTET + verifiziert:** Die ObservableTelemetry-Hülle in die SearchAlgorithmAnatomy verdrahtet (Option A, Doc 29 §3d): `art_reference.hpp` `telemetry`→Hülle (transparenter Decorator mit name()-Durchgriff für composition_registry/axis_path_serialization), `search_algorithm_anatomy.hpp` hält `axis_telemetry_` als 2. Organ (Member ohne `{}`) + `telemetry_organ()` + observe_all-Sammlung. **Literal grün** (`test_v41_anatomy_observer`, msvc-release/STATISTICS aktiv, 14/14 PASSED): `[ OK ] DrivenTelemetryOrganFlowsIntoAggregate` (kein SKIP) — `observe_all().telemetry` trägt nach Treiben `total=3/leaf=2/node=0/peak=2`, `observable_count>=2`. **telemetry = 2. voll observable+getriebene Achse, auf search_algo-Niveau.** **NACHTRAG (gleicher Tag):** ALLE 11 Reference-Compositions (art + 10 weitere: hot/wormhole/surf/masstree/start + 5 PaperBindings) telemetry→Hülle umgestellt (leaf_only-Shim liefert die Hülle mit) — `test_v41_anatomy_observer` 14/14 grün (alle 11 observe_all() kompilieren). VERBLEIBT: Auto-Kopplung an Tier-insert (Zusatz, kein Block — search_algo wird auch explizit getrieben), AdHoc/Registry-Pfad (telemetry-Registry müsste Hüllen liefern), dann memory_layout/serialization/node_type analog. **Status L-74c: 2/4 OperativeCapable-Achsen (search_algo+telemetry) voll; 3 verbleibend nach demselben Muster.**
- **V42-Sprint Baustein 1 (2026-06-02) — telemetry-Driver-Hülle GRÜN:** Nach der Sackgasse (s.u.) den Aggregat-Befund empirisch mit dem REALEN Wrapper geklärt (`test_d_v42_probe2`: LeafOnlyCounter `is_aggregate=1`/`brace_ok{T{}}=0`/`default-init klappt`/`ObservableAxis=0`) → der „CRTP-Block" war ein `{}`-Aggregat-Init-Artefakt, ABER die nackte Strategie hat kein `statistics()`. **Lösung gebaut:** `axes/telemetry_axis/axis_11_telemetry_observable.hpp` `ObservableTelemetry<Strategy>` (Mess-Mechanik + statistics()/snapshot_t gegated, strategie-parametrisiert, kein Aggregat). **`test_d_v42_telemetry_observable` literal grün** (MIT STATISTICS): `LeafOnlyCounter node=0` vs `InsertCounter node=1` bei identischer Touch-Folge (Delta>0, kein Stub, Snapshot ABI-tauglich). Beide Tests CTest-registriert (BOOST_DTESTS, jetzt 16 D-Tests). **VERBLEIBT (Verdrahtung, Doc 29 §3c Option A):** Reference-Compositions `telemetry`→Hülle, Anatomie-Organ halten+observe_all, Kopplung im abi_adapter, Umbrella-Verifikation. **Status L-74c: TEIL (Driver-Baustein grün, Verdrahtung offen).**
- **V42-Sprint-Versuch 2026-06-02 (telemetry zuerst) — SACKGASSE, ehrlich:** Schritt §3a (a)+(b) aus Doc 29 (telemetry direkt als Anatomie-Member `axis_telemetry_{}` + observe_all-Sammlung) wurde implementiert und **scheiterte am realen Build** (`cmake --target test_v41_anatomy_observer`): `ArtComposition::telemetry == LeafOnlyCounter`, dessen protected `TelemetryStrategyBase`-ctor das Member-Halten in einer Fremdklasse blockiert. Der `test_d_v42_probe`-Befund war **wrapper-unvollständig** (testete InsertCounter/DensityTracker/LatencyHistogram, NICHT LeafOnlyCounter). → **Member-Hold-Ansatz verworfen, sauber revertiert (Repo grün, 13/13 test_v41_anatomy_observer).** Befund + 3 korrekte Optionen (T=Tuple-Komposition generisch / H=Observable-Hülle pro Achse / C=CRTP-ctor public) in **Doc 29 §3b** dokumentiert. **Nächster Schritt:** Option T oder H umsetzen — das ist der eigentliche CRTP-ctor-Block aus Doc 29 §3 Schritt 1, NICHT umgehbar per Member-Hold.

### L-76a — Set-Gattung (Vogel, K-only, 14 Achsen)  · Task #76 · Aufwand: M–L (4–6 Tage)
- **SOLL:** Doc 28 §2 (Set: 14 ohne mapping/value_handle/inner), Doku 14 §27.2/§28/§32.2.
- **IST (literal):** 0 Treffer für SetAnatomy/SetComposition/SetDock im Code.
- **Pre-Sprint-Klärung K-A:** Doku 14 §32.2 + Doc 28 = 14; Doku 14 §28-Tabelle = 15 ✓ (filter strittig). Vor D-2 durch genaues Doku-14-Lesen auflösen (NICHT User fragen — „bei Unklarheit Planrunde").
- **Verifikations-Kriterium:** `IsSetComposition<…>` + `SetAnatomy::organ_count()==14` + `binary_count()==∏ mp_size(14 Enabled)` + DLL-Round-Trip (`genus()==Set`, `dynamic_cast<ISetTier*>≠null`, `contains` nach `insert`==true).
- **Neue Dateien:** `anatomy/set_{composition_concept,composition_factory,observer_aggregate,anatomy,tier,abi_adapter,permutation_engine}.hpp` + `GenusBindingTraits<Set>` + `pruef_dock/set_dock.hpp`. **Status (2026-06-03):** GROSSTEILS DONE — `set_anatomy.hpp`/`set_composition.hpp`/`set_tier.hpp`/`set_abi_adapter.hpp` + `GenusBindingTraits<Set>` existieren; **in-process `pruef_dock/set_dock.hpp` (NEU) + Verifikationstest `test_genus_docks` literal grün** (measure(1000,1500,400): insert=1000, contains=1500/hit=1000/miss=500, erase=400, size=600, peak=1000 — über das echte K-only-Such-Organ). **per-Gattung PermutationEngine DONE (NEU 2026-06-03):** `set_permutation_engine.hpp` (SetCompositionFromPermTuple 15-Tupel→SetComposition + SetPermutationEngine: genus==Set/arity()==15/count()==∏/for_each_set+for_each_composition_type/assert_pruefling_slot_genus) — `test_genus_permutation_engines` literal grün (arity=15, count=6, 6 Compositions IsSetComposition). VERBLEIBT: DLL-Round-Trip per Gattung (Folgeschritt analog ContainerDock/BR-4) + K-A-Slot-Zahl-Klärung (15 vs 14).

### L-76b — Sequence-Gattung (Reptil, V-indexed, 9 + axis_growth)  · Task #76 · Aufwand: L (6–9 Tage)
- **SOLL:** Doc 28 §2 (Sequence: 9 + axis_growth), Doku 14 §26.1/§28/§32.2.
- **IST (literal):** 0 Treffer; `axis_growth` existiert NICHT (muss als NEUE Achse nach Goldstandard-Checkliste angelegt werden, Vorlage axis_06_allocator).
- **Pre-Sprint-Klärung K-B:** 9 (§32.2) vs 10 (§28-Tabelle). Vor D-3 auflösen.
- **Verifikations-Kriterium:** `GrowthPolicy<DoublingGrowth>` + `SequenceAnatomy::genus()==Sequence` + DLL-Round-Trip (`push_back` N → `tier_size()==N`, `tier_at(i)` korrekt).
- **Neue Achse:** `topics/sequence/axis_growth/…` (10 Pflicht-Komponenten: GrowthPolicy-Concept `next_capacity/growth_factor`, Wrapper Doubling/GoldenRatio/FixedChunk/Exact, Registry, flags.hpp.in). + Sequence-Gattungs-Kette. **Status (2026-06-03):** TEIL DONE — `sequence_anatomy.hpp`/`sequence_composition.hpp` (mit `DoublingGrowth` + `GrowthPolicy`-Concept) + `sequence_tier.hpp`/`sequence_abi_adapter.hpp` existieren; **in-process `pruef_dock/sequence_dock.hpp` (NEU) + `test_genus_docks` literal grün** (measure(1000,1200): push=1000, at=1200/oob=200, size=1000, peak=1000, **growth_events=11** = axis_growth-Policy REAL getrieben). **per-Gattung PermutationEngine DONE (NEU 2026-06-03):** `sequence_permutation_engine.hpp` (SequenceCompositionFromPermTuple 11-Tupel + SequencePermutationEngine: genus==Sequence/arity()==11/count/for_each) — `test_genus_permutation_engines` literal grün (arity=11, count=2; axis_growth-Slot V10 überschreibt Default). **axis_growth Goldstandard-Topic DONE (NEU 2026-06-03):** `topics/sequence/axis_growth/axis_growth_policies.hpp` (GoldenRatioGrowth ×1.5 / FixedChunkGrowth<Chunk> additiv / ExactGrowth 1:1) + `axis_growth_registry.hpp` (`AllGrowthPolicies`/`EnabledGrowthPolicies` mp_list aller 4 inkl. DoublingGrowth, konsumierbar als 11. SequencePermutationEngine-Slot). **Verifiziert (literal grün, `test_axis_growth_policies`):** alle 4 erfüllen `GrowthPolicy`; distinkte `next_capacity(4,5)`=8/6/68/5; Registry-Count==4; **reale Anatomie-Integration** — growth_events in `SequenceAnatomy` je Policy distinkt (Doubling=8/GoldenRatio=13/FixedChunk<64>=2/Exact=100) → axis_growth real wirksam (kein Stub). DLL-Round-Trip per Gattung bereits ✅ (Genus-DLL-Round-Trip oben).

### L-76c — View-Gattung (Pflanze, non-owning, 7 + axis_extent/layout/accessor)  · Task #76 · Aufwand: L (7–10 Tage)
- **SOLL:** Doc 28 §2 (View: 7 + axis_extent/layout/accessor; non-owning kein insert/erase/alloc/concurrency), Doku 14 §26.6/§28/§32.2.
- **IST (literal):** 0 Treffer; 3 neue Achsen (extent/layout/accessor) nötig (mdspan-Bezug).
- **Pre-Sprint-Klärung K-C:** 7 (Doc 28/§32.2) vs 4 geteilt (§28-Plant). Plausibelste Lesart: 7 = 4 geteilte (memory_layout/telemetry/value_handle/isa) + 3 eigene. Vor D-4 auflösen.
- **Verifikations-Kriterium:** `genus()==View` + Compile-Fehler-Test dass `tier_insert` NICHT existiert (API-Asymmetrie) + DLL-Round-Trip (`bind(extern_buf)`→`tier_read(i)`==extern_buf[i]).
- **Risiko:** non-owning-Lifetime über DLL-Grenze (Dock muss Puffer-Owner sein, Lebensdauer > View). **Status (2026-06-03):** TEIL DONE — `view_anatomy.hpp`/`view_composition.hpp` (mit `LayoutRight`/`DefaultAccessor`/`DynamicExtent` + Layout/Accessor/Extent-Concepts) + `view_tier.hpp`/`view_abi_adapter.hpp` existieren; **in-process `pruef_dock/view_dock.hpp` (NEU) + `test_genus_docks` literal grün** (measure(500,700): read=700/oob=200, bound_size=500, bind_count=1; **das Dock IST der Puffer-Owner** während der Messung → Lifetime-Risiko in-process gelöst). **per-Gattung PermutationEngine DONE (NEU 2026-06-03):** `view_permutation_engine.hpp` (ViewCompositionFromPermTuple 7-Tupel + ViewPermutationEngine: genus==View/arity()==7/count/for_each) — `test_genus_permutation_engines` literal grün (arity=7, count=3; accessor-Slot V6 überschreibt Default). **extent/layout/accessor Goldstandard-Topics DONE (NEU 2026-06-03):** `topics/view/view_policies.hpp` (StaticExtent<N> / LayoutLeft / LayoutStrided<Stride> / AlignedAccessor<Align> — mdspan-Bezug) + `view_registries.hpp` (3 Registries: AllExtents(2)/AllLayouts(3)/AllAccessors(2) inkl. der inline-Defaults). **Verifiziert (literal grün, `test_axis_view_policies`):** alle Policies erfüllen ExtentPolicy/LayoutPolicy/AccessorPolicy; `index_of` distinkt (Right/Left=3, Strided<2>=6); **reale Anatomie-Integration** — in `ViewAnatomy` liest `LayoutStrided<2>` physisch eine ANDERE Zelle (`read(2)`→buf[4]=4) als `LayoutRight` (`read(2)`→buf[2]=2), `read(4)` Strided=OOB → axis_layout real wirksam (kein Stub). DLL-Round-Trip per Gattung bereits ✅ (Genus-DLL-Round-Trip oben). **Damit ist L-76 (alle 3 genus-eigenen Achsen-Cluster: Set/Sequence-axis_growth/View-extent+layout+accessor) Goldstandard-vollständig.**

### L-76a-c — DLL-ROUND-TRIP per Gattung ✅ DONE + REPO-REPRODUZIERBAR (2026-06-03)
- **Beweis (literal grün, `tests/unit/genus_dll_roundtrip.ps1`):** alle 3 Genus-Permutations-DLLs (Set/Sequence/View) via `cl /LD` aus den COMMITTETEN Quellen `tests/unit/genus_module_{set,sequence,view}.cpp` (COMDARE_DEFINE_{SET,SEQUENCE,VIEW}_MODULE) gebaut → vom GATTUNGS-AGNOSTISCHEN `AnatomyModuleLoader` geladen → Gattung runtime via `anatomy()->genus()` erkannt → via `dynamic_cast<I{Set,Sequence,View}Tier*>` über die **echte DLL-Grenze** getrieben + Observer-POD gezogen: Set (insert/dup-reject/contains/`insert_count==2`/`organ_count==15`), Sequence (`tier_size==8`/`tier_at(3)==30`/`push_count==8`/`growth_events>0`), View (`bind`/`tier_read(2)==9`/`bound_size==4`). `test_dgenus_dll` → ALLE OK, run_exit=0.
- **Schließt den Phase-E-Vorbehalt** „nur git-ignoriert scratch-verifiziert": Quellen + Build-Skript jetzt COMMITTET → aus committeter Quelle reproduzierbar. Damit haben **alle 5 Gattungen** den buildbaren Tier-Binary-Nachweis (SearchAlgorithm-BR-4-Pilot + Container + Set/Sequence/View). **CMake-MODULE-REGISTRIERUNG DONE (NEU 2026-06-03):** die 5 Permutations-/Build-Varianten-DLLs (genus_module_{set,sequence,view}.cpp + genus_buildvariant_{avx512,avx2}.cpp) jetzt OFFIZIELLE CMake-`add_library(SHARED)`-Targets (fixe OUTPUT_DIRECTORY gegen Multi-Config-Subdir-Bug) + `add_executable`-Loader (test_dgenus_dll linkt `comdare::anatomy_module_loader`; test_buildvariant_dll win32) + `add_test` mit `$<TARGET_FILE:..>`-DLL-Pfaden (Multi-Config-robust, kein Lade-Ort-Rätselraten). **Verifiziert:** `cmake --build` baut alle 5 DLLs + 2 Loader → Direkt-Lauf mit den offiziell gebauten DLLs `ALLE OK` (dgenus_exit=0, buildvariant_exit=0). **BEFUND (vorbestehend, NICHT meine Tests):** `ctest`-Voll-Enumeration scheitert an einem CMake-4.2-`gtest_discover_tests`(PRE_TEST)-Klammer-Artefakt (`...[1]_include`) eines gtest-Tests → blockiert die ganze ctest-Suite; die plain-`add_test`-D-Tests + DLL-Loader bauen+bestehen aber als offizielle CMake-Binaries (per Direkt-Lauf belegt). Die ctest-Enumeration-Reparatur (gtest-Discovery-Plumbing) ist ein getrennter Infra-Folgeschritt.

### L-76d — Viren produktiv (IVirusExecutionEngine, Graph-Algos ohne Achsen)  · Task #76-Umfeld · Aufwand: M–L (5–7 Tage)
- **SOLL:** Doc 28 §2 (Viren: 0 Achsen, Kapsel, Schwester von AnatomyBase an Wurzel IExecutionEngine; Graph-Dock), Doku 14 §33-§40.
- **IST (literal):** `execution_engine/execution_engine_base.hpp` (`IExecutionEngine` + `ExecutionEngineKind::Virus` existieren); KEINE produktive Virus-Impl (nur Test-Stub).
- **Verifikations-Kriterium:** `GraphBfs::engine_kind()==Virus` + BFS über Referenzgraph korrekt (Algorithmus-Korrektheit) + (falls DLL) Virus-Loader-Round-Trip.
- **Risiko/Entscheid:** `IPruefDock`/`AnatomyModuleHandle` ist Anatomy-typisiert → Viren brauchen EIGENE Dock-/Handle-Familie (Empfehlung), NICHT in den Anatomy-Zweig pressen. Gate-1-∏-Logik gilt für Viren NICHT (0 Achsen). **Status:** OFFEN (niedrigste Prio, V42-nah).

### L-CLUSTER — ZIH-Voll-Pfad: lokal-zählen → Cluster-bauen → Dock-messen  · Aufwand: L (gate-frei-Teil) + GATE-MAXIMAL
- **SOLL:** Doc 28 §5 (Skalierungs-Gleichung, User-verbindlich), messarchitektur_v5_design §1/§9.
- **IST (literal):** kein durchgängiger Treiber/CLI verkettet `StaticBinaryView→provision_all→Loader→measure→NodeValue` (grep: 0 Aufrufe von provision_all/measure_composition außerhalb Unit-Tests). `slurm_launcher.hpp` emittiert nur sbatch-TEXT + erwartet `perm_*.bin` (Executable) — Mismatch zu `.dll`-Modulen (kein `main()`). KEIN `comdare-ce.sif`-Rezept, KEIN Webhook-EMPFANG.
- **Gate-frei (lokal-verifizierbar):** E2E-Treiber `e2e_pipeline.hpp` + `apps/experiment_pipeline`; `apps/perm_runner` (lädt 1 perm-DLL, fährt sequencer, schreibt JSON); `result_ingest.hpp` (JSON→NodeObserverSnapshot→set_node_value); `.def`/`build_sif.sh` Skript-TEXT-Korrektheit.
- **GATE-MAXIMAL (mit User absprechen, kein Python):** `apptainer build comdare-ce.sif` + ZIH-Upload; echte `sbatch`-Submission (array aus BuildSelection.size()); Webhook-Receiver VLAN-60. **Status (2026-06-03): GATE-FREI-TEIL DONE** — (1) **`apps/perm_runner` CLI** (NEU): lädt EINE perm-DLL via AnatomyModuleLoader → `dynamic_cast<IObservableTier*>` → `run_observable_perm` → EINE result_ingest-Zeile nach stdout. **Literal verifiziert** (gegen `perm_adhoc_buildvariant.dll`: `perm_art_default;500;500;0;500;0;256;34192;11376;17;16;0;4;256`, exit 0) — löst den `.bin`/`.dll`-Mismatch („kein main()" → perm_runner IST das Executable). (2) **slurm_launcher .bin→.dll-Fix:** `ZihJobConfig::runner="perm_runner"`; sbatch emittiert jetzt `singularity exec comdare-ce.sif perm_runner "$BIN"` über `perm_*.dll`; `test_kf12_kf13_slurm_prepare` erweitert + grün. (3) **Container-Rezept-TEXT** (NEU, `deploy/comdare-ce.def` + `build_sif.sh`): Apptainer-Def (gcc:13, vendored-Boost-offline-%post baut perm_runner, runscript=perm_runner) + Build-Wrapper (bash-Syntax verifiziert); **apptainer build + gcc-13-Cross-Build der (MSVC-gebauten) cache-engine sind GATE-MAXIMAL/cluster-seitig, ehrlich markiert**. VERBLEIBT GATE-MAXIMAL (User-Freigabe): reale `apptainer build`-Ausführung + ZIH-sbatch-Submission + Webhook-Receiver-Deploy.

### L-MEAS — RuntimeVariableLoop: dyn. Variablen <1s je Binary, voller Mess-Lauf  · Aufwand: M
- **SOLL:** Doc 28 §5 (je Binary ALLE dyn. Variablen + Testdaten <1s, RAM-resident), messarchitektur_v5_design §4/§5.
- **IST (literal):** `runtime_variable_loop.hpp` vollständig für Resource-Control-Variablen (thread_count/prefetch/pool_budget/batch_size via `tier_query_resource_caps`+`tier_apply_resource_control`), ABER der Visitor MISST nichts — er muss je Setting die Zwei-Phasen-Op-Schleife + Workload fahren + Snapshot erzeugen.
- **Verifikations-Kriterium:** 1 geladener Pilot-Tier × dyn. Kartesik (z.B. thread {1,2,4} × prefetch {0,8}) = 6 Settings × 3 Repeats = 18 Snapshots, EINE DLL, kein Reload, <1s, `observer_real==true`.
- **Neue Datei:** `builder/experiment_tree/runtime_measure_visitor.hpp` (verbindet RuntimeVariableLoop + `drive_two_phase_op_loop` (I7 done) + `WorkloadGenerator` (done)). **Status (2026-06-03): ✅ DONE (real-DLL)** — Mechanik war schon ✅ (test_d13 vs MockTier); **NEU:** der `SearchAlgorithmAbiAdapter` implementiert jetzt `IResourceControllableTier` (KF-4, IMMER/nicht messungs-gated, eigenständiges Sub-Interface → dynamic_cast-erreichbar über die DLL; 5 genus-invariante steuerbare Achsen concurrency/prefetch/allocator/traversal/value_handle mit Caps + apply-Clamp). **`test_d13_dll_runtime_measure` literal grün über die ECHTE .dll-Grenze:** lädt SearchAlgorithm-adhoc-DLL → `dynamic_cast<IObservableTier*>` + `dynamic_cast<IResourceControllableTier*>` beide ≠ null → Host-Bridge → `RuntimeMeasureVisitor`: caps.controllable_axis_count==5/thread_count==64 über DLL, 9 Mess-Punkte (3 thread_count-Einstellungen × 3 Wdh), `search_insert_count>0` (real getrieben über DLL), `applied_axis_count>0`, repeat 0/1/2 je 3× (KF-10), `tier_size>0` (KEIN Reload). **Regress-frei:** test_adhoc_buildvariant_dll + test_buildvariant_dll grün nach dem Adapter-Change (IAnatomyBase=erste Basis, Layout unverändert; dynamic_cast RTTI-basiert). VERBLEIBT (separat): PMC (R5.D, plattform-/hardware-gebunden, ehrlich „operativ-PMC-pending").

### L-BACKLOG — Doku-only Backlog (NICHT Teil der 6 Gates)  · Aufwand: variabel
- Doc 16 axis_05-IMC-Wrapper; Doc 15 axis_09b-Schichten + 15 AVX-512-Sub-Flags (VNNI/BF16/FP16); Original*SearchAlgo s4-echtes-Linking (heute s2-Stubs); R5.D PMC (Per-Achsen-Differenzierung jenseits Wall-Clock). **Status:** OFFEN (V42-nah, nicht Gate-blockierend).

---

## §2 Pre-Sprint-Klärungen — AUFGELÖST (genaues Doku-14-§28-Lesen, 2026-06-02)

**Auflösung (Planrunde, NICHT User gefragt):** Die explizite Achsen-Matrix Doku 14 **§28** (jede Achse je Gattung einzeln ✓-markiert) ist maßgeblich; die zusammenfassende §32.2 hat bei Set/Sequence ein systematisches **Off-by-one** (−1). Exakte Zählung der ✓:

| ID | Frage | AUFLÖSUNG (§28-Zählung) |
|----|-------|--------------------------|
| K-A | Set 14 vs 15 | **15 geteilte Achsen** ✓ (search_algo, cache_traversal, path_compression, node_type, memory_layout, allocator, prefetch, concurrency, serialization, telemetry, isa, index_organization, io_dispatch, migration_policy, filter); **kein** mapping/value_handle (K=V). §32.2-„14" = Off-by-one. |
| K-B | Sequence 9 vs 10 | **10 geteilte** ✓ (memory_layout, allocator, prefetch, concurrency, serialization, telemetry, value_handle, isa, io_dispatch, migration_policy) **+ axis_growth** (eigene). §32.2-„9" = Off-by-one. |
| K-C | View 7 | **7 gesamt** = 4 geteilte (memory_layout, telemetry, value_handle, isa) + 3 eigene (axis_extent, axis_layout, axis_accessor). §28 bestätigt; non-owning → kein allocator/concurrency/insert. |

## §2.6 PHASE E — Abschluss-Audit (2026-06-03): autonomer Umfang ERLEDIGT

**Finaler adversarialer Audit** in `docs/sessions/20260603-goal-v6-phase-e-abschluss-audit.md`. Konsolidierte Re-
Verifikation: **11/11 Session-Tests grün als offizielle CMake-Binaries** (Direkt-Lauf). **Schlussfolgerung:** der
AUTONOM abschließbare Umfang von Goal V6 ist VOLLSTÄNDIG + literal verifiziert (#74 GATE-4 · L-76 alle 5 Gattungen ·
L-74c-Kern · DLL-Round-Trips CMake-MODULE · L-MEAS-real-DLL · L-CLUSTER-gate-frei). **Residual = AUSSCHLIESSLICH
(a) PMC hardware-blockiert · (b) GATE-MAXIMAL ZIH user-gated (absprachepflichtig, CLAUDE.md) · (c) ctest-gtest-
Enumeration bewusster CMake-4.2-Trade-off** — keiner autonom abschließbar; „erledigt" darauf wäre falsche Erfolgsmarke
bzw. GATE-Verletzung. **Nächster nicht-autonomer Schritt: User-Absprache über GATE-MAXIMAL-ZIH**, dann apptainer/sbatch/PMC.

## §2.7 L-MEAS-THESIS — echte E2E-Messung 8 SearchAlgorithm-DLL-Tiere (2026-06-03, User-Auftrag)

**Auftrag (User 2026-06-03):** echter E2E-Build + Messung → echte Thesis-Werte + echte DLL-Tiere für die Gattung
Suchalgorithmen. **DONE + committet (cache-engine 8fe7583, superproject 0599f47).** Vollbeleg: `docs/sessions/
20260603-l-meas-thesis-searchalgo-tiere.md`. **8 echte SearchAlgorithm-DLL-Tiere** gebaut (cl /LD +
`COMDARE_MEASUREMENT_ON=1` + voller ADHOC-Include-Satz) + **24 literale `result_ingest`-Messzeilen** via `perm_runner`
(`run_observable_perm`: n_ops insert+lookup → tier_observe). **Befund A** (axis_03a variiert, 16 fix): reale 2-Regime-
Taxonomie — kapazitätsgebunden (Array256/VectorU8U8: peak/fill=256) vs elementlinear (LinearScan/BST/BTree: =n).
**Befund B** (axis_04 variiert, Array256 fix): Node256/48/16/4 messen identisch → ehrliche Null-Kontrolle (Achsen-
Orthogonalität). Artefakte committet+reproduzierbar (`tests/unit/thesis_tiere/`). **Lektion (verankert):**
`COMDARE_MEASUREMENT_ON=1` ist Pflicht-Define für die `IObservableTier`-Ebene (`abi_adapter.hpp:88`) — sonst baut die
DLL „MESSUNG-AUS" (nur IDriveableTier) → perm_runner exit 1.

**#84 Allokator-Real-Link — literaler IST (2026-06-03):** im `build/msvc-release`-Cache ALLE `COMDARE_HAVE_<vendor>` =
**OFF** → axis_06-Wrapper im C++23-Re-Impl-Fallback (`is_original_module()=false`); deshalb sind `alloc_*` über alle
Tiere gleich. Mechanik real bewiesen für **mimalloc/snmalloc/dlmalloc** (self-contained, lokal aktivierbar via
`-DCOMDARE_BUILD_PERMUTATIONS=ON`); **4 harte Vendor** (jemalloc/tcmalloc/hoard/scalloc) brauchen vcpkg/MSYS2/ZIH-Linux
→ **GOAL-K78 CE-D4** (an Infra-Agent delegiert, K78 §8). Spec unverändert gültig: `docs/sessions/20260601-19-...`.

## §2.5 Phase-D-Fortschritt + Verifikations-Build-Rezeptur (Kontinuität)

**Erledigt (literal grün, committet):** D1 L-SEL · D2 L-73 · D3 L-77 · D4 L-75 (Container q2+DLL) · **#76 KOMPLETT: D9 Set (15) · D10 Sequence (10+axis_growth) · D11 View (7 non-owning) → ALLE 5 Anatomie-Gattungen GenusBound==true + in-process (test_genus_binding 5/5) + DLL-Round-Trip (test_dgenus_dll: Sequence/View/Set über gattungs-agnostischen Loader; Container via D4b; SearchAlgorithm via BR-4) · D12 L-76d Viren (IVirusExecutionEngine + GraphBfs, echte BFS, test_d12_virus).** Pre-Sprint K-A/B/C aufgelöst (§28 maßgeblich). **Tasks erledigt: #73, #75, #76, #77** (+ Phase A/B/C). **#74 TEIL-erledigt (ZURÜCKGESTUFT nach Phase-E-Zwischenaudit 20260602-goal-v6-phase-e-zwischenaudit.md):** **L-74b** ✅ (BuildVariantDefinitionV1 + Reader, test_d7) · **L-74c** ✅ (R5.B-Operabilitäts-Klassifikation 2+4+11==17, anti-Fake, test_d8) · **L-74a TEIL** (nur standalone `COMDARE_DEFINE_BUILD_VARIANT_INSPECTION`-Symbol, **stub-getestet** test_d7a — **OFFEN: `COMDARE_DEFINE_ANATOMY_MODULE_ADHOC_BUILDVARIANT` 17+3-DLL + hw_cross_constraint.hpp 96→~25 + build_variant_args + reale comdare_build_*-Symbole NICHT gebaut**). **L-MEAS** ✅-Mechanik (test_d13 — **Caveat: gegen MockTier, nicht geladene DLL**). **L-CLUSTER gate-frei NUR result_ingest** ✅ (test_d14 — **OFFEN: e2e_pipeline/apps/perm_runner/build_sif.sh/comdare-ce.def NICHT gebaut**, ~1/4 des gate-freien Teils).

**Solide grün:** #73, #75, #76 (5 Gattungen je 1 reales Kern-Organ + int-Platzhalter-Slots, ehrlich deklariert; Viren echte BFS), L-74b, L-74c. **CTest-Registrierung ✅ BEHOBEN** (tests/unit/CMakeLists.txt: 14 D-Tests via add_executable+add_test; cmake-configure grün + test_d8_operability im CMake/MSBuild-Kontext gebaut+gelaufen ALLE OK — Audit-Reproduzierbarkeits-Vorbehalt erledigt; offen: DLL-Round-Trip-Tests + vorbestehendes gtest-discover-Artefakt test_pressure_state[1]_include). **L-CLUSTER gate-frei ✅ weitgehend komplett:** result_ingest (D14) + perm_runner (D14b, Round-Trip) + e2e_pipeline (D14c, durchgängige Pipeline O(K): 3 aus 1e9, kein OOM) — alle CTest-registriert + literal grün. **Echt verbleibend:** (a) **L-74a-Voll** (BUILDVARIANT-DLL + Cross-Constraint) · **comdare-ce.def/build_sif-Skript-TEXT** (Apptainer-Rezept, gate-frei-Text; reale Ausführung GATE-MAXIMAL). (b) **V42-nah/schwer (Composition-Driver):** #74-Voll-Verdrahtung der 4 OperativeCapable-Achsen real in tier_observe + voller Build-Achsen-DLL-Einbau; per-Gattung Pruef-Docks + PermutationEngines (Doku 14 §32.6, „TODO V42"). (c) **GATE-MAXIMAL** (NICHT autonom, User-Freigabe): reale ZIH-sbatch-Submission + apptainer comdare-ce.sif + Webhook-Receiver-Deploy. (d) **Phase E Abschluss-Audit** (erst nach a+b+c). **Folge-Integration (Doku 14 §32.6, V42):** per-Gattung Pruef-Docks (Set/Sequence/View/Graph) + PermutationEngines. **Dann Phase E** (finaler adversarialer Audit). **Vorlage = set_/sequence_/view_-Ketten + Container/genus-DLL-Pfade.**

**Verifikations-Build (header-only Standalone-Tests, RAM-Watchdog):** `build/scratch_compile_test.ps1 -Test <name> [-Boost] [-Extra @("libs\cache_engine\src","build\generated")]` (cl via vcvars64+vswhere, response-file wegen Pfad-Spaces, cl-Kill < 2.5 GB frei). Include-Roots-Default: `libs/cache_engine`, `libs/cache_engine/include`, `libs/common`. Achsen-Tests (q1/q2 etc.) brauchen `-Boost` + `-Extra src,generated`. DLL-Round-Trip (3-Phasen) = `build/scratch_d4b_dll.ps1` (DLL `/LD /DCOMDARE_ANATOMY_MODULE_BUILD`; Loader-Test linkt `anatomy_module_loader.cpp`; /Fo-Verzeichnis braucht `\\"`). Scratch-Skripte sind git-ignoriert (build/), bei Verlust aus dieser Rezeptur reproduzierbar.

## §3 Verdikt Phase B

Gegenüber Doc 28 §6 ist das Ledger nun **lückenlos granular** (L-73, L-SEL, L-77, L-75, L-74a/b/c, L-76a/b/c/d, L-CLUSTER, L-MEAS, L-BACKLOG), je mit literalem IST-Code-Stand (Datei:Zeile) + Verifikations-Kriterium. **Solide grün** bleiben Gate-1/2/5/6 + SearchAlgorithm-BR-4 (Pilot). **„ABSOLUTE Vollständigkeit"** = Schließung aller obigen Lücken (Phase D), bestätigt durch Phase E (finaler adversarialer Audit). Reihenfolge/Sprints: → Planungssession.
