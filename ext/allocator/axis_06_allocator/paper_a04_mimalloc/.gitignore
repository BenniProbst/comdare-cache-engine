# V41.F.6.1.P2.B legacy_code/paper_a04_mimalloc/ — Build-Time-Init Konvention
# Source + Include werden zur Build-Time aus ext/A04-mimalloc/ kopiert
# (siehe cmake/is_original_codegen.cmake comdare_paper_init() — [[paper-original-code-pattern]]).
# User-Original-Distribution bleibt unangetastet in ext/, hier nur kuratierte Kopie.

# Ignore extracted sources (will be regenerated by comdare_paper_init)
src/
include/
.extracted.marker

# Tool-generated Lock-File wird MANUELL committed (Diff-bare First-Build-Init):
# !sha256_locked.txt  (wird NICHT ignoriert — siehe README.md)
